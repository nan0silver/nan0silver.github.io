---
layout: post
collection: project_diary
title: 친구하자 프로젝트 일기 - JPQL에서 매핑되지 않은 관계의 LEFT JOIN 한계
description: >
  Call과 CallEmotionResult를 SQL처럼 LEFT JOIN하고 싶지만 JPQL은 연관관계 기반 JOIN이 기본이라 막히는 문제,
  네이티브 쿼리·두 쿼리 분리 후 Java 병합 등 대안과 선택 기준을 정리합니다.
sitemap: false
---

# [친구하자] JPQL에서 매핑되지 않은 관계의 LEFT JOIN 한계

> `Call`과 `CallEmotionResult`를 SQL처럼 `LEFT JOIN`하고 싶은데, 엔티티 간 연관관계를 두지 않은 설계에서는 JPQL만으로는 기대한 한 방 쿼리가 어렵다. SQL과 JPQL의 차이, 우리가 택한 두 쿼리 + 병합 방식까지 정리한다.

- [들어가며](#들어가며)
- [1. JPQL LEFT JOIN의 제약](#1-jpql-left-join의-제약)
- [2. 연관관계를 선언하지 않은 이유](#2-연관관계를-선언하지-않은-이유)
- [3. 임의 조건 JOIN을 JPQL로 옮기려 할 때](#3-임의-조건-join을-jpql로-옮기려-할-때)
- [4. 해결 방법 1: 네이티브 쿼리](#4-해결-방법-1-네이티브-쿼리)
- [5. 해결 방법 2: 두 쿼리 분리 후 Java에서 병합](#5-해결-방법-2-두-쿼리-분리-후-java에서-병합)
- [6. 두 접근 방식 비교](#6-두-접근-방식-비교)
- [7. 어떤 경우에 어떤 방식을 선택할까](#7-어떤-경우에-어떤-방식을-선택할까)
- [8. 실무에서 비슷한 패턴](#8-실무에서-비슷한-패턴)
- [마무리](#마무리)

---

## 들어가며

JPA를 쓰다 보면 이런 요구를 만난다.

- "`Call`과 `Call_emotion_results`를 `LEFT JOIN`해서 감정 분석이 **없는** 통화도 목록에 넣고 싶다."

SQL로는 대략 다음처럼 쓸 수 있다.

```sql
SELECT c.*, cer.*
FROM calls c
LEFT JOIN call_emotion_results cer
  ON c.id = cer.call_id AND cer.user_id = ?
WHERE c.user1_id = ? OR c.user2_id = ?
```

그런데 이걸 JPQL로 그대로 옮기려 하면, 포터블한 JPQL만으로는 예상과 다른 벽에 부딪히는 경우가 많다.

---

## 1. JPQL LEFT JOIN의 제약

표준 JPQL에서 `JOIN`은 보통 **엔티티에 선언된 연관관계**(`@OneToMany`, `@ManyToOne` 등)를 따라간다.

```java
// ✅ 연관관계가 있으면 JPQL LEFT JOIN 가능
@Entity
public class Call {
    @OneToMany(mappedBy = "call")
    private List<CallEmotionResult> emotionResults;
}
```

```java
@Query("""
    SELECT c FROM Call c
    LEFT JOIN c.emotionResults cer
    WHERE (c.user1.id = :userId OR c.user2.id = :userId)
    """)
List<Call> findWithEmotionResults(@Param("userId") Long userId);
```

연관관계가 **없으면** `c.emotionResults` 같은 경로 자체가 없어서, 위와 같은 객체 그래프 기반 `LEFT JOIN`을 쓸 수 없다.

---

## 2. 연관관계를 선언하지 않은 이유

`Call`에 `@OneToMany List<CallEmotionResult>`를 붙이지 않은 데는 실무적인 이유가 있다.

1. **결합도** — 통화 엔티티가 분석 결과 컬렉션을 직접 들고 있으면, 통화 흐름만 다룰 때도 분석 도메인이 끼어들 여지가 커진다.
2. **N+1·실수 여지** — `@OneToMany`는 기본이 `LAZY`지만, `fetch` 없이 접근하면 N+1이 나기 쉽다.
3. **방향성** — “분석 결과 조회”는 분석 쪽 책임으로 두고, `Call`이 `CallEmotionResult`를 알 필요가 없다고 본다.

의도적으로 JPA 연관을 끊어 두는 선택은 흔하다. 대신 그만큼 **JPQL 한 줄로 SQL식 LEFT JOIN**을 재현하기 어려워진다.

---

## 3. 임의 조건 JOIN을 JPQL로 옮기려 할 때

연관관계 없이 SQL처럼 `ON` 절만으로 붙이려는 시도 예시는 다음과 같다.

```java
// ❌ 표준 JPQL 관점에서는 기대하기 어렵다 / 구현체·버전에 따라 다름
@Query("""
    SELECT c FROM Call c
    LEFT JOIN CallEmotionResult cer ON c.id = cer.callId AND cer.userId = :userId
    WHERE c.user1.id = :userId OR c.user2.id = :userId
    """)
```

과거 Hibernate 등에서는 이런 **임의 조건 JOIN** 문법이 약하거나 없어서 파싱·실행 단계에서 막히는 경우가 많았다. 최근 구현체에서는 문법이 늘어나는 방향이지만, **Spring Boot·Hibernate 조합과 버전**에 따라 달라지고, **표준 JPQL만** 고려하면 여전히 “연관관계 없는 임의 JOIN”에 의존하기 어렵다.

근본적으로 JPQL은 **객체 그래프 탐색**에 가깝고, SQL처럼 임의 컬럼 조합으로 자유롭게 JOIN하는 언어가 아니다.

---

## 4. 해결 방법 1: 네이티브 쿼리

가장 직접적인 방법은 `@Query(nativeQuery = true)`로 SQL을 그대로 쓰는 것이다.

```java
@Query(value = """
    SELECT c.id, c.start_at, c.end_at, c.duration_seconds,
           cer.all_scores, cer.dominant_emotion
    FROM calls c
    LEFT JOIN call_emotion_results cer
      ON c.id = cer.call_id AND cer.user_id = :userId
    WHERE (c.user1_id = :userId OR c.user2_id = :userId)
      AND c.call_status = 'COMPLETED'
    """, nativeQuery = true)
List<Object[]> findMonthlyRaw(@Param("userId") Long userId);
```

**단점 요약**

- 반환 타입이 `Object[]`·프로젝션 DTO 등으로 제한되기 쉽다.
- DB 방언·스키마에 직접 의존한다.
- 컴파일 타임 검증이 약하고, JSON 컬럼 등은 별도 역직렬화가 필요할 수 있다.

---

## 5. 해결 방법 2: 두 쿼리 분리 후 Java에서 병합

이 프로젝트에서 택한 방식이다. 레포지토리에서 각각 조회한 뒤 서비스에서 합친다.

**Step 1 — 완료 통화만 JPQL로 조회**

```java
@Query("""
    SELECT c FROM Call c
    WHERE (c.user1.id = :userId OR c.user2.id = :userId)
      AND c.callStatus = 'COMPLETED'
      AND c.endAt >= :startDate AND c.endAt < :endDate
    ORDER BY c.endAt, c.id
    """)
List<Call> findCompletedCallsByUserInDateRange(
        @Param("userId") Long userId,
        @Param("startDate") LocalDateTime startDate,
        @Param("endDate") LocalDateTime endDate);
```

**Step 2 — 감정 결과만 별도 조회**

```java
List<CallEmotionResult> findByCallIdInAndUserId(List<Long> callIds, Long userId);
```

**Step 3 — 서비스에서 Map으로 매칭 (LEFT JOIN과 같은 효과)**

```java
public List<DailyEmotionSummaryResponse> getMonthlyEmotionResults(
        Long userId, int year, int month) {

    LocalDateTime startDate = LocalDate.of(year, month, 1).atStartOfDay();
    LocalDateTime endDate = startDate.plusMonths(1);

    List<Call> calls = callRepository.findCompletedCallsByUserInDateRange(
            userId, startDate, endDate);
    List<Long> callIds = calls.stream().map(Call::getId).toList();

    Map<Long, CallEmotionResult> resultByCallId =
            callEmotionResultRepository.findByCallIdInAndUserId(callIds, userId).stream()
                    .collect(Collectors.toMap(CallEmotionResult::getCallId, r -> r));

    return buildDailySummaries(calls, resultByCallId);
}
```

분석이 없는 통화는 `resultByCallId.get(callId)`가 `null`이 되어, SQL `LEFT JOIN`과 같은 “한쪽만 있어도 행이 살아 있는” 형태를 애플리케이션에서 재현한다.

---

## 6. 두 접근 방식 비교

| 항목           | DB JOIN (네이티브) | 두 쿼리 + Java 병합   |
| -------------- | ------------------ | --------------------- |
| 쿼리 횟수      | 1회                | 2회                   |
| 반환 타입      | `Object[]` 등      | 엔티티(타입 안전)     |
| DB 방언 의존 | 큼                 | JPQL 쪽은 상대적으로 작음 |
| 스키마 변경    | SQL 수정           | 엔티티·쿼리 조정      |
| 데이터 규모    | DB가 조인 최적화   | `callIds` 많으면 메모리 부담 |
| 테스트         | 통합 비중 큼       | 단위·통합 모두 구성하기 쉬움 |

---

## 7. 어떤 경우에 어떤 방식을 선택할까

**두 쿼리 + Java 병합이 나은 경우**

- 한 유저·한 기간 등으로 **건수가 수백~수천** 수준이고, 각 쿼리가 인덱스를 잘 탄다.
- 도메인을 느슨하게 유지하고 싶다.
- 엔티티 매핑·타입 안전성을 유지하고 싶다.

**네이티브 쿼리 또는 연관관계 추가가 나은 경우**

- 조인·집계가 커서 **DB 한 번에** 처리하는 편이 확실히 유리하다.
- 두 테이블이 항상 함께 쓰이고, 분리 조회 이점이 크지 않다.

---

## 8. 실무에서 비슷한 패턴

“한쪽은 없어도 다른 쪽은 나와야 한다(LEFT JOIN 의미)”인데, “JPA로 두 도메인을 굳이 묶고 싶지 않다”가 겹칠 때 자주 나온다.

- `Order` ↔ `Review`: 리뷰 없는 주문도 목록에 넣을 때
- `User` ↔ `PushNotificationToken`: 토큰이 없을 수 있을 때
- `Post` ↔ `ReadHistory`: 읽음을 별도 테이블로 둘 때

이때 **두 쿼리 + `Map` 매칭**은 단순하면서 타입 안전성을 지키기 쉬운 실용적인 선택이다.

---

## 마무리

JPQL의 `LEFT JOIN`은 **연관관계가 엔티티에 열려 있을 때** 가장 자연스럽다. 설계상 연관을 두지 않았다면, SQL 한 방을 고집하기보다 **네이티브 쿼리**로 감수할지, **조회를 쪼개고 서비스에서 병합**할지를 트레이드오프로 선택하면 된다.

“SQL로는 되는데 JPQL로는 왜 안 되지?”라는 순간에, JPQL이 **객체 그래프용 질의**에 가깝다는 점을 떠올리면 원인 정리와 다음 설계 결정이 빨라진다.
