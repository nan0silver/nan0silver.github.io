<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR, en-US"><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="https://nan0silver.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://nan0silver.github.io/" rel="alternate" type="text/html" hreflang="ko-KR, en-US" /><updated>2026-03-09T02:07:14+00:00</updated><id>https://nan0silver.github.io/feed.xml</id><title type="html">NAHYUN BLOG</title><subtitle>Nahyun Eun (은나현). Full-stack developer building products solo, preparing for startup. Java, Spring Boot, Python, AI. Project diaries, backend/infra notes, and TIL.
</subtitle><author><name>Nahyun Eun 은나현</name></author><entry><title type="html">[TIL] 멀티스레드 Race Condition과 AtomicBoolean (volatile의 한계)</title><link href="https://nan0silver.github.io/til/2026-03-07-til/" rel="alternate" type="text/html" title="[TIL] 멀티스레드 Race Condition과 AtomicBoolean (volatile의 한계)" /><published>2026-03-07T00:00:00+00:00</published><updated>2026-03-07T00:00:00+00:00</updated><id>https://nan0silver.github.io/til/til</id><content type="html" xml:base="https://nan0silver.github.io/til/2026-03-07-til/"><![CDATA[<blockquote>
  <p>📝 <strong>TIL (Today I Learned)</strong> \n+&gt; <code class="language-plaintext highlighter-rouge">volatile</code>만으로는 해결되지 않는 문제가 있다. "가시성"과 "원자성"은 다르고, <code class="language-plaintext highlighter-rouge">check-then-act</code> 패턴이 섞이면 레이스 컨디션이 터진다. 이걸 <code class="language-plaintext highlighter-rouge">AtomicBoolean.compareAndSet</code>으로 정리해 본다.</p>
</blockquote>

<hr />

<h2 id="-새롭게-배운-것">🍀 새롭게 배운 것</h2>

<ul>
  <li><code class="language-plaintext highlighter-rouge">volatile</code>은 <strong>가시성(visibility)</strong> 은 해결하지만, <strong>원자성(atomicity)</strong> 은 해결하지 못한다.</li>
  <li>"읽고 → 조건 확인하고 → 쓰는" <code class="language-plaintext highlighter-rouge">check-then-act</code>는 멀티스레드에서 레이스가 나기 쉽다.</li>
  <li><code class="language-plaintext highlighter-rouge">AtomicBoolean.compareAndSet(true, false)</code>는 <strong>한 번만 상태 전환</strong>되도록 만들어 주는 대표 패턴이다. (CAS 기반)</li>
</ul>

<hr />

<h2 id="1-배경-왜-이런-문제가-생기는가">1. 배경: 왜 이런 문제가 생기는가?</h2>

<p>Spring WebFlux는 <strong>비동기·논블로킹</strong> 스택이고, 소수의 Netty I/O 스레드가 많은 작업을 번갈아 처리한다. 그래서 같은 객체의 메서드가 <strong>서로 다른 스레드에서 거의 동시에</strong> 호출되는 상황이 자연스럽게 발생한다.</p>

<p>예를 들어 <code class="language-plaintext highlighter-rouge">GeminiLiveSession</code>에서 아래 흐름이 동시에 일어날 수 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[Netty I/O 스레드 A] → Gemini로부터 오디오 수신 → handleIncoming() → session.onError()
[Netty I/O 스레드 B] → WebSocket 연결 끊김 감지 → disconnect()     → session.close()
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">close()</code>와 <code class="language-plaintext highlighter-rouge">onError()</code>가 <strong>동시에 호출</strong>될 수 있고, 이때 둘 다 "세션 종료"를 시도하면 문제가 생긴다.</p>

<hr />

<h2 id="2-문제-1--가시성-visibility">2. 문제 1 — 가시성 (Visibility)</h2>

<p>멀티코어 CPU는 성능을 위해 <strong>각 코어마다 캐시(L1/L2 Cache)</strong> 를 가진다. 그래서 어떤 스레드가 값을 바꿔도, 다른 스레드는 <strong>캐시에 남아 있는 오래된 값</strong>을 읽을 수 있다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[CPU 코어 A]           [CPU 코어 B]
  캐시: active = true    캐시: active = true
        ↑ RAM 반영 전            ↑ RAM 반영 전
  RAM: active = false (이미 변경됨)
</code></pre></div></div>

<p>코어 A가 <code class="language-plaintext highlighter-rouge">active = false</code>로 바꿨더라도, 코어 B는 여전히 <code class="language-plaintext highlighter-rouge">true</code>를 볼 수 있다. 이게 <strong>가시성 문제</strong>다.</p>

<h3 id="volatile은-가시성을-해결한다"><code class="language-plaintext highlighter-rouge">volatile</code>은 가시성을 해결한다</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">volatile</span> <span class="kt">boolean</span> <span class="n">active</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">volatile</code>은 “이 변수를 읽고 쓸 때 <strong>항상 메인 메모리(RAM)에서</strong> 직접 읽고 써라”는 JVM에 대한 명령입니다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[CPU 코어 A]         RAM           [CPU 코어 B]
  write active=false ──→ RAM 즉시 반영
                              RAM에서 직접 읽음 ←── read active
</code></pre></div></div>

<p>코어 B는 항상 최신 값을 봅니다. <strong>가시성 문제 해결!</strong></p>

<hr />

<h2 id="3-문제-2--check-then-act-race-condition">3. 문제 2 — Check-Then-Act Race Condition</h2>

<p><code class="language-plaintext highlighter-rouge">volatile</code>로 가시성을 해결해도, 더 근본적인 문제가 남는다. 바로 <strong>check-then-act</strong> 패턴이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// volatile boolean 버전</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">close</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(!</span><span class="n">active</span><span class="o">)</span> <span class="k">return</span><span class="o">;</span>   <span class="c1">// ① check: active 읽기</span>
    <span class="n">active</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>        <span class="c1">// ② act: active 쓰기</span>
    <span class="n">outboundSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>
    <span class="n">inboundAudioSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p>①과 ②는 <strong>서로 다른 연산</strong>이다. 그 사이에 다른 스레드가 끼어들 수 있다.</p>

<h3 id="문제가-되는-시나리오">문제가 되는 시나리오</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>시간 →

스레드 A (close 호출)         스레드 B (onError 호출)
────────────────────────────────────────────────────
① active 읽음 → true
                           ① active 읽음 → true  ← volatile이라 최신값(true) 보임
② active = false 씀
                           ② active = false 씀   ← 이미 false지만 또 씀 (괜찮음)
outboundSink.tryEmitComplete()
                           outboundSink.tryEmitError()  ← 💥 이미 complete된 sink에 error!
inboundAudioSink.tryEmitComplete()
                           inboundAudioSink.tryEmitError() ← 💥 또 충돌!
</code></pre></div></div>

<p>두 스레드 모두 ①에서 <code class="language-plaintext highlighter-rouge">true</code>를 읽고 통과한다. <code class="language-plaintext highlighter-rouge">volatile</code>은 최신 값을 보장하지만, <strong>“내가 읽은 순간부터 내가 쓸 때까지 다른 스레드가 건드리지 않는다”는 보장은 없다.</strong></p>

<p>이것이 <strong>Check-Then-Act Race Condition</strong></p>

<ul>
  <li><strong>Check</strong>: <code class="language-plaintext highlighter-rouge">if (!active)</code> — 조건 확인</li>
  <li><strong>Then-Act</strong>: <code class="language-plaintext highlighter-rouge">active = false</code> — 상태 변경</li>
</ul>

<p>그래서 <code class="language-plaintext highlighter-rouge">check-then-act</code>가 <strong>원자적(Atomic)</strong> 이지 않으면 레이스 컨디션이 발생한다.</p>

<blockquote>
  <p>💡 <strong>원자성(Atomicity)</strong>: "쪼갤 수 없는 한 덩어리"처럼 실행되는 성질. 중간에 다른 스레드가 끼어들 수 없음.</p>
</blockquote>

<hr />

<h2 id="4-해결책--atomicboolean과-compareandset-cas">4. 해결책 — <code class="language-plaintext highlighter-rouge">AtomicBoolean</code>과 <code class="language-plaintext highlighter-rouge">compareAndSet</code> (CAS)</h2>

<h3 id="atomicboolean이란"><code class="language-plaintext highlighter-rouge">AtomicBoolean</code>이란?</h3>

<p><code class="language-plaintext highlighter-rouge">java.util.concurrent.atomic</code> 패키지의 클래스로, boolean에 대해 <strong>원자적 연산</strong>을 제공한다. 내부적으로 CPU의 <strong>CAS(Compare-And-Swap)</strong> 를 사용한다.</p>

<h3 id="compareandsetexpect-update이-하는-일"><code class="language-plaintext highlighter-rouge">compareAndSet(expect, update)</code>이 하는 일</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">atomicBoolean</span><span class="o">.</span><span class="na">compareAndSet</span><span class="o">(</span><span class="kc">true</span><span class="o">,</span> <span class="kc">false</span><span class="o">)</span>
</code></pre></div></div>

<p>이 한 줄이 하드웨어 수준에서 <strong>원자적으로</strong> 실행하는 작업:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>① 현재 값을 읽는다
② 현재 값 == expect(true) 이면?
    → 값을 update(false)로 바꾸고 true 반환
③ 현재 값 != expect(true) 이면?
    → 아무것도 안 하고 false 반환
</code></pre></div></div>

<p>이 세 단계가 <strong>나눌 수 없는 하나의 연산</strong>으로 실행된다. 중간에 다른 스레드가 절대 끼어들 수 없다.</p>

<h3 id="같은-시나리오를-다시-보면">같은 시나리오를 다시 보면</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// AtomicBoolean 버전</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">close</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(!</span><span class="n">active</span><span class="o">.</span><span class="na">compareAndSet</span><span class="o">(</span><span class="kc">true</span><span class="o">,</span> <span class="kc">false</span><span class="o">))</span> <span class="k">return</span><span class="o">;</span>  <span class="c1">// 원자적으로 check + act</span>
    <span class="n">outboundSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>
    <span class="n">inboundAudioSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>시간 →

스레드 A (close 호출)              스레드 B (onError 호출)
───────────────────────────────────────────────────────────
compareAndSet(true→false) 성공 ✅
  → active가 false로 변경됨
  → true 반환, 계속 실행
                                   compareAndSet(true→false) 실패 ❌
                                     → 현재값이 이미 false
                                     → false 반환, return으로 탈출
outboundSink.tryEmitComplete()     (아무것도 안 함)
inboundAudioSink.tryEmitComplete() (아무것도 안 함)
</code></pre></div></div>

<p>스레드 A만 통과하고, 스레드 B는 자동으로 탈출한다. <strong>정확히 하나만 실행!</strong></p>

<hr />

<h2 id="5-최종-코드-비교">5. 최종 코드 비교</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ❌ Before: volatile — 가시성만 해결, race condition 존재</span>
<span class="kd">private</span> <span class="kd">volatile</span> <span class="kt">boolean</span> <span class="n">active</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">close</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(!</span><span class="n">active</span><span class="o">)</span> <span class="k">return</span><span class="o">;</span>   <span class="c1">// check</span>
    <span class="n">active</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>        <span class="c1">// act  ← 이 사이에 다른 스레드 침입 가능</span>
    <span class="n">outboundSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>
    <span class="n">inboundAudioSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">onError</span><span class="o">(</span><span class="nc">Throwable</span> <span class="n">error</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(!</span><span class="n">active</span><span class="o">)</span> <span class="k">return</span><span class="o">;</span>   <span class="c1">// 두 스레드 모두 통과 가능</span>
    <span class="n">active</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
    <span class="n">outboundSink</span><span class="o">.</span><span class="na">tryEmitError</span><span class="o">(</span><span class="n">error</span><span class="o">);</span>     <span class="c1">// close()와 충돌</span>
    <span class="n">inboundAudioSink</span><span class="o">.</span><span class="na">tryEmitError</span><span class="o">(</span><span class="n">error</span><span class="o">);</span> <span class="c1">// close()와 충돌</span>
<span class="o">}</span>
</code></pre></div></div>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ✅ After: AtomicBoolean — 가시성 + 원자성 모두 해결</span>
<span class="kd">private</span> <span class="kd">final</span> <span class="nc">AtomicBoolean</span> <span class="n">active</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">AtomicBoolean</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">close</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(!</span><span class="n">active</span><span class="o">.</span><span class="na">compareAndSet</span><span class="o">(</span><span class="kc">true</span><span class="o">,</span> <span class="kc">false</span><span class="o">))</span> <span class="k">return</span><span class="o">;</span>  <span class="c1">// check + act = 원자적</span>
    <span class="n">outboundSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>   <span class="c1">// 오직 하나의 스레드만 도달</span>
    <span class="n">inboundAudioSink</span><span class="o">.</span><span class="na">tryEmitComplete</span><span class="o">();</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">onError</span><span class="o">(</span><span class="nc">Throwable</span> <span class="n">error</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(!</span><span class="n">active</span><span class="o">.</span><span class="na">compareAndSet</span><span class="o">(</span><span class="kc">true</span><span class="o">,</span> <span class="kc">false</span><span class="o">))</span> <span class="k">return</span><span class="o">;</span>  <span class="c1">// 나머지 스레드는 여기서 탈출</span>
    <span class="n">outboundSink</span><span class="o">.</span><span class="na">tryEmitError</span><span class="o">(</span><span class="n">error</span><span class="o">);</span>
    <span class="n">inboundAudioSink</span><span class="o">.</span><span class="na">tryEmitError</span><span class="o">(</span><span class="n">error</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<p>이렇게 하면 <code class="language-plaintext highlighter-rouge">close()</code>와 <code class="language-plaintext highlighter-rouge">onError()</code> 중 <strong>둘 중 하나만</strong> 본문을 실행한다. 나머지는 <code class="language-plaintext highlighter-rouge">compareAndSet</code>이 실패하면서 즉시 return 된다.</p>

<p>즉, "세션 종료"라는 상태 전환이 <strong>정확히 한 번만</strong> 일어나게 된다.</p>

<hr />

<h2 id="6-정리-spring-개발자가-기억할-것">6. 정리: Spring 개발자가 기억할 것</h2>

<table>
  <thead>
    <tr>
      <th> </th>
      <th><code class="language-plaintext highlighter-rouge">volatile</code></th>
      <th><code class="language-plaintext highlighter-rouge">AtomicBoolean</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>해결하는 문제</strong></td>
      <td>가시성 (최신 값 보장)</td>
      <td>가시성 + 원자성</td>
    </tr>
    <tr>
      <td><strong>check-then-act</strong></td>
      <td>❌ 안전하지 않음</td>
      <td>✅ <code class="language-plaintext highlighter-rouge">compareAndSet</code>으로 안전</td>
    </tr>
    <tr>
      <td><strong>주요 용도</strong></td>
      <td>단순 플래그 읽기/쓰기</td>
      <td>상태 전환이 <strong>딱 한 번</strong> 일어나야 할 때</td>
    </tr>
  </tbody>
</table>

<h3 id="언제-atomicboolean을-써야-하나">언제 <code class="language-plaintext highlighter-rouge">AtomicBoolean</code>을 써야 하나?</h3>

<p>"읽고 → 조건 확인하고 → 쓰는" 흐름이 있고, 그 흐름이 <strong>한 번만</strong> 일어나야 한다면 <code class="language-plaintext highlighter-rouge">AtomicBoolean</code>을 먼저 떠올리면 좋다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 이런 패턴이 보이면 AtomicBoolean 고려</span>
<span class="k">if</span> <span class="o">(!</span><span class="n">flag</span><span class="o">)</span> <span class="k">return</span><span class="o">;</span>  <span class="c1">// check</span>
<span class="n">flag</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>        <span class="c1">// act</span>
<span class="n">doSomethingOnce</span><span class="o">();</span>  <span class="c1">// 딱 한 번만!</span>
</code></pre></div></div>

<p>실제로 Spring 애플리케이션에서는 이런 상황이 자주 등장한다.</p>

<ul>
  <li>리소스를 <strong>딱 한 번만</strong> 초기화/해제할 때</li>
  <li>WebSocket/SSE 연결을 <strong>한 번만</strong> 닫을 때</li>
  <li>이벤트를 <strong>중복 없이</strong> 발행할 때</li>
</ul>

<hr />

<blockquote>
  <p>📌 <strong>핵심 한 줄 요약</strong> \n+&gt; <code class="language-plaintext highlighter-rouge">volatile</code>은 "다른 스레드가 쓴 값을 내가 볼 수 있게" 해주고, \n+&gt; <code class="language-plaintext highlighter-rouge">AtomicBoolean.compareAndSet</code>은 "check-then-act를 아무도 못 끼어드는 하나의 동작"으로 만들어준다.</p>
</blockquote>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="til" /><category term="TIL" /><category term="Java" /><category term="동시성" /><category term="RaceCondition" /><category term="volatile" /><category term="AtomicBoolean" /><category term="CAS" /><category term="WebFlux" /><summary type="html"><![CDATA[멀티스레드 환경에서 volatile의 한계(가시성 vs 원자성)와 check-then-act 레이스 컨디션을 AtomicBoolean.compareAndSet(CAS)으로 해결하는 방법을 정리했습니다.]]></summary></entry><entry><title type="html">[친구하자] OkHttp vs Spring WebFlux WebSocket — 우리가 starter-webflux를 선택한 이유</title><link href="https://nan0silver.github.io/projectdiary/2026-03-06-diary/" rel="alternate" type="text/html" title="[친구하자] OkHttp vs Spring WebFlux WebSocket — 우리가 starter-webflux를 선택한 이유" /><published>2026-03-06T00:00:00+00:00</published><updated>2026-03-09T02:06:30+00:00</updated><id>https://nan0silver.github.io/projectdiary/diary</id><content type="html" xml:base="https://nan0silver.github.io/projectdiary/2026-03-06-diary/"><![CDATA[<blockquote>
  <p>Gemini Live API를 WebSocket으로 연동하면서 <strong>클라이언트 라이브러리 선택</strong>에 대한 고민을 정리합니다.</p>
</blockquote>

<ul>
  <li><a href="#들어가며">들어가며</a></li>
  <li><a href="#1-배경-gemini와-websocket-클라이언트">1. 배경: Gemini와 WebSocket 클라이언트</a></li>
  <li><a href="#2-okhttp란">2. OkHttp란?</a></li>
  <li><a href="#3-spring-webflux-websocket-client란">3. Spring WebFlux WebSocket Client란?</a></li>
  <li><a href="#4-starter-web-vs-starter-webflux--뭐가-다른가">4. starter-web vs starter-webflux — 뭐가 다른가?</a></li>
  <li><a href="#5-왜-okhttp-대신-starter-webflux를-선택했는가">5. 왜 OkHttp 대신 starter-webflux를 선택했는가?</a></li>
  <li><a href="#6-wss와-tls--reactor-netty가-해주는-일">6. wss://와 TLS — Reactor Netty가 해주는 일</a></li>
  <li><a href="#정리">정리</a></li>
</ul>

<hr />

<h2 id="들어가며">들어가며</h2>

<p>Gemini Live API를 붙이려고 보니, 우리 서버가 <strong>Gemini 쪽에 WebSocket 클라이언트</strong>로 접속해야 하는 구조였다. 브라우저나 앱이 우리 서버에 WebSocket으로 붙는 것과는 반대 방향이다. 그래서 “서버에서 외부 WebSocket에 붙을 때 뭘 쓰지?”를 검토하게 됐고, 후보는 <strong>OkHttp</strong>와 <strong>Spring WebFlux의 WebSocket 클라이언트</strong> 두 가지였다.</p>

<p>결론부터 말하면, 이미 프로젝트에 <code class="language-plaintext highlighter-rouge">spring-boot-starter-webflux</code>가 있어서 <strong>OkHttp를 새로 넣지 않고</strong> WebFlux의 <code class="language-plaintext highlighter-rouge">ReactorNettyWebSocketClient</code>를 쓰기로 했다. 이 글에서는 그때 비교했던 내용과, 왜 이 선택이 맞다고 봤는지 정리한다.</p>

<hr />

<h2 id="1-배경-gemini와-websocket-클라이언트">1. 배경: Gemini와 WebSocket 클라이언트</h2>

<p>Gemini Live API는 <strong><code class="language-plaintext highlighter-rouge">wss://</code>(WebSocket Secure)</strong> 로 통신한다. 음성·영상 같은 스트리밍 데이터를 실시간으로 주고받기 위해서다.</p>

<p>우리 아키텍처에서는:</p>

<ul>
  <li><strong>사용자(브라우저/앱)</strong> ↔ <strong>우리 서버</strong>: 기존처럼 REST, WebSocket(우리 서버가 <strong>서버</strong> 역할)</li>
  <li><strong>우리 서버</strong> ↔ <strong>Gemini</strong>: 우리 서버가 <strong>클라이언트</strong>가 되어 Gemini에 WebSocket으로 접속</li>
</ul>

<p>즉, “우리 백엔드가 Gemini에 <strong>WebSocket 클라이언트</strong>로 붙어서 오디오 스트림을 주고받는다”는 요구가 생긴 것이다. 이걸 구현하려면 <strong>JVM에서 동작하는 WebSocket 클라이언트 라이브러리</strong>가 필요했다.</p>

<p>검토한 선택지는 두 가지였다.</p>

<ul>
  <li><strong>OkHttp</strong> — Square(현 Block)의 HTTP/WebSocket 클라이언트</li>
  <li><strong>Spring WebFlux의 WebSocketClient</strong> — Spring이 제공하는 리액티브 WebSocket 클라이언트 (<code class="language-plaintext highlighter-rouge">ReactorNettyWebSocketClient</code> 등)</li>
</ul>

<hr />

<h2 id="2-okhttp란">2. OkHttp란?</h2>

<p>OkHttp는 Square(현 Block)에서 만든 <strong>HTTP/WebSocket 클라이언트 라이브러리</strong>다. Android에서 HTTP 통신할 때 많이 쓰이고, 백엔드(JVM)에서도 외부 API 호출용으로 자주 쓴다.</p>

<p>WebSocket을 쓰려면 별도 의존성을 추가해야 한다.</p>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">implementation</span> <span class="s1">'com.squareup.okhttp3:okhttp:4.12.0'</span>
</code></pre></div></div>

<h3 id="동작-방식-콜백-기반">동작 방식: 콜백 기반</h3>

<p>OkHttp의 WebSocket API는 <strong>콜백</strong>이다. “연결됐을 때”, “메시지 왔을 때”, “에러 났을 때” 같은 이벤트를 <strong>리스너</strong>로 넘겨주는 방식이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">OkHttpClient</span> <span class="n">client</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">OkHttpClient</span><span class="o">();</span>

<span class="nc">Request</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Request</span><span class="o">.</span><span class="na">Builder</span><span class="o">()</span>
    <span class="o">.</span><span class="na">url</span><span class="o">(</span><span class="s">"wss://generativelanguage.googleapis.com/..."</span><span class="o">)</span>
    <span class="o">.</span><span class="na">build</span><span class="o">();</span>

<span class="n">client</span><span class="o">.</span><span class="na">newWebSocket</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="k">new</span> <span class="nc">WebSocketListener</span><span class="o">()</span> <span class="o">{</span>
    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">onOpen</span><span class="o">(</span><span class="nc">WebSocket</span> <span class="n">webSocket</span><span class="o">,</span> <span class="nc">Response</span> <span class="n">response</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* 연결 성공 */</span> <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">onMessage</span><span class="o">(</span><span class="nc">WebSocket</span> <span class="n">webSocket</span><span class="o">,</span> <span class="nc">String</span> <span class="n">text</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* 메시지 수신 */</span> <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">onFailure</span><span class="o">(</span><span class="nc">WebSocket</span> <span class="n">webSocket</span><span class="o">,</span> <span class="nc">Throwable</span> <span class="n">t</span><span class="o">,</span> <span class="nc">Response</span> <span class="n">response</span><span class="o">)</span> <span class="o">{</span> <span class="cm">/* 오류 */</span> <span class="o">}</span>
<span class="o">});</span>
</code></pre></div></div>

<p>직관적이고 사용하기 쉽다. 다만 <strong>Spring WebFlux</strong>처럼 <code class="language-plaintext highlighter-rouge">Mono</code>/<code class="language-plaintext highlighter-rouge">Flux</code>로 짜인 코드와는 성격이 다르다. WebFlux에서는 “스트림”을 <code class="language-plaintext highlighter-rouge">Flux</code>로 표현하고, 에러 처리나 백프레셔(흐름 제어)를 연산자로 조합하는데, OkHttp는 “이벤트가 올 때마다 콜백 호출”이라서 <strong>콜백을 Reactor 타입(Mono/Flux)으로 감싸는 변환 레이어</strong>를 따로 만들지 않으면 리액티브 파이프라인과 자연스럽게 이어지지 않는다.</p>

<h3 id="요약">요약</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>프로그래밍 모델</td>
      <td>동기(Sync) / <strong>콜백(Callback)</strong> 기반</td>
    </tr>
    <tr>
      <td>WebSocket 지원</td>
      <td><code class="language-plaintext highlighter-rouge">OkHttpClient.newWebSocket()</code></td>
    </tr>
    <tr>
      <td>TLS (wss://)</td>
      <td>OkHttp 내부에서 처리</td>
    </tr>
    <tr>
      <td>Spring 통합</td>
      <td>없음 (필요하면 직접 연동)</td>
    </tr>
    <tr>
      <td>의존성 크기</td>
      <td>경량 (약 700KB)</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="3-spring-webflux-websocket-client란">3. Spring WebFlux WebSocket Client란?</h2>

<p>Spring WebFlux는 Spring 5부터 나온 <strong>리액티브(Reactive) 웹 스택</strong>이다. 내부적으로 <strong>Project Reactor</strong>(<code class="language-plaintext highlighter-rouge">Mono</code>, <code class="language-plaintext highlighter-rouge">Flux</code>)를 쓰고, 기본 네트워크 엔진으로 <strong>Reactor Netty</strong>를 사용한다. WebSocket <strong>클라이언트</strong>도 이 스택 위에 있다. 대표적으로 <code class="language-plaintext highlighter-rouge">ReactorNettyWebSocketClient</code>를 쓰면 된다.</p>

<p>코드는 <strong>스트림을 선언적으로</strong> 다루는 형태다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">WebSocketClient</span> <span class="n">client</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ReactorNettyWebSocketClient</span><span class="o">();</span>

<span class="n">client</span><span class="o">.</span><span class="na">execute</span><span class="o">(</span>
    <span class="no">URI</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"wss://generativelanguage.googleapis.com/..."</span><span class="o">),</span>
    <span class="n">session</span> <span class="o">-&gt;</span> <span class="n">session</span>
        <span class="o">.</span><span class="na">send</span><span class="o">(</span><span class="nc">Flux</span><span class="o">.</span><span class="na">just</span><span class="o">(</span><span class="n">session</span><span class="o">.</span><span class="na">textMessage</span><span class="o">(</span><span class="s">"hello"</span><span class="o">)))</span>
        <span class="o">.</span><span class="na">then</span><span class="o">(</span><span class="n">session</span><span class="o">.</span><span class="na">receive</span><span class="o">()</span>
            <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="nl">WebSocketMessage:</span><span class="o">:</span><span class="n">getPayloadAsText</span><span class="o">)</span>
            <span class="o">.</span><span class="na">doOnNext</span><span class="o">(</span><span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">::</span><span class="n">println</span><span class="o">)</span>
            <span class="o">.</span><span class="na">then</span><span class="o">())</span>
<span class="o">).</span><span class="na">subscribe</span><span class="o">();</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">session.receive()</code>가 곧바로 <strong>Flux</strong>(메시지 스트림)를 주기 때문에, 다른 리액티브 스트림(예: 오디오 청크를 보내는 Flux)과 <code class="language-plaintext highlighter-rouge">merge</code>, <code class="language-plaintext highlighter-rouge">zip</code>, <code class="language-plaintext highlighter-rouge">flatMap</code> 등으로 조합하기 쉽다. 에러 처리도 <code class="language-plaintext highlighter-rouge">onErrorResume</code>, <code class="language-plaintext highlighter-rouge">retry</code> 같은 Reactor 연산자로 일관되게 할 수 있다.</p>

<h3 id="리액티브가-여기서-의미하는-것">“리액티브”가 여기서 의미하는 것</h3>

<ul>
  <li><strong>논블로킹</strong>: 한 요청이 I/O를 기다리는 동안 스레드를 잡아두지 않고, 이벤트가 오면 그때 처리한다.</li>
  <li><strong>스트림 모델</strong>: 데이터가 “한 번에 한 덩어리”가 아니라 “시간에 걸쳐 흘러오는 스트림”으로 표현된다. 그래서 <code class="language-plaintext highlighter-rouge">Flux</code>로 “들어오는 메시지 스트림”, “나가는 메시지 스트림”을 다루기 좋다.</li>
  <li><strong>백프레셔</strong>: 상대가 보내는 속도가 우리가 처리하는 속도보다 빠를 때, “잠깐만 보내지 마” 같은 신호를 줄 수 있어서, 무한 버퍼 없이 흐름을 제어할 수 있다.</li>
</ul>

<p>Gemini Live처럼 <strong>오디오 스트리밍</strong>을 다룰 때는 “연속적인 데이터 흐름”을 하나의 파이프라인으로 표현하는 게 유리해서, 리액티브 모델이 잘 맞는다.</p>

<h3 id="요약-1">요약</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>프로그래밍 모델</td>
      <td><strong>리액티브 (Mono/Flux)</strong></td>
    </tr>
    <tr>
      <td>WebSocket 지원</td>
      <td><code class="language-plaintext highlighter-rouge">ReactorNettyWebSocketClient</code></td>
    </tr>
    <tr>
      <td>TLS (wss://)</td>
      <td>Reactor Netty가 처리</td>
    </tr>
    <tr>
      <td>Spring 통합</td>
      <td>Spring 생태계 일부 (빈, 설정, 보안 등과 자연스럽게 연동)</td>
    </tr>
    <tr>
      <td>의존성</td>
      <td><code class="language-plaintext highlighter-rouge">spring-boot-starter-webflux</code>에 이미 포함</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="4-starter-web-vs-starter-webflux--뭐가-다른가">4. starter-web vs starter-webflux — 뭐가 다른가?</h2>

<p>“WebFlux를 쓰면 기존 Spring MVC랑 충돌하지 않나?”라고 할 수 있다. 실제로 <strong>starter-web</strong>과 <strong>starter-webflux</strong>는 역할이 다르고, 한 프로젝트에 둘 다 들어갈 수 있다.</p>

<h3 id="spring-boot-starter-web-우리가-아는-그-환경"><code class="language-plaintext highlighter-rouge">spring-boot-starter-web</code> (우리가 아는 그 환경)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>spring-boot-starter-web
  └── Spring MVC (서블릿 기반)
  └── Tomcat (기본 내장 서버)
  └── Jackson (JSON 직렬화)
</code></pre></div></div>

<ul>
  <li><strong>동기/블로킹</strong> 모델. 요청 하나당 스레드 하나가 맡아서 처리한다.</li>
  <li><code class="language-plaintext highlighter-rouge">@RestController</code>, <code class="language-plaintext highlighter-rouge">@GetMapping</code> 같은 걸 쓰는 그 환경이다.</li>
  <li>우리 서버가 <strong>REST API를 제공</strong>하고, <strong>WebSocket 서버</strong>로 브라우저/앱과 연결하는 쪽은 이 스택 위에 있다.</li>
</ul>

<h3 id="spring-boot-starter-webflux"><code class="language-plaintext highlighter-rouge">spring-boot-starter-webflux</code></h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>spring-boot-starter-webflux
  └── Spring WebFlux (리액티브)
  └── Reactor Netty (서버/클라이언트 엔진)
  └── Project Reactor (Mono, Flux)
  └── Jackson (JSON 직렬화)
</code></pre></div></div>

<ul>
  <li><strong>비동기/논블로킹</strong> 모델. 이벤트 루프 기반에 가깝다.</li>
  <li><strong>WebClient</strong>(리액티브 HTTP 클라이언트), <strong>ReactorNettyWebSocketClient</strong>(WebSocket 클라이언트) 등이 이 스택에 포함된다.</li>
  <li>우리는 <strong>서버</strong>는 MVC로 두고, <strong>Gemini에 나가는 WebSocket 클라이언트</strong>만 WebFlux 쪽을 쓰는 식으로 가져갔다.</li>
</ul>

<h3 id="우리-프로젝트의-buildgradle">우리 프로젝트의 build.gradle</h3>

<div class="language-groovy highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">implementation</span> <span class="s1">'org.springframework.boot:spring-boot-starter-web'</span>       <span class="c1">// MVC (우리 서버)</span>
<span class="n">implementation</span> <span class="s1">'org.springframework.boot:spring-boot-starter-webflux'</span>   <span class="c1">// WebFlux (Gemini 클라이언트용)</span>
<span class="n">implementation</span> <span class="s1">'org.springframework.boot:spring-boot-starter-websocket'</span> <span class="c1">// WebSocket 서버 (브라우저/앱 연결용)</span>
</code></pre></div></div>

<ul>
  <li><strong>starter-web</strong> → 사용자 요청을 받는 <strong>서버</strong>(REST, 일반 HTTP).</li>
  <li><strong>starter-websocket</strong> → 사용자와의 <strong>WebSocket 서버</strong> (매칭 알림 등).</li>
  <li><strong>starter-webflux</strong> → <strong>Gemini에 WebSocket 클라이언트</strong>로 접속할 때 사용.</li>
</ul>

<p>역할이 나뉘어 있어서, “MVC만 쓰다가 WebFlux 일부만 가져와서 클라이언트로만 쓴다”는 구성이 가능하다.</p>

<hr />

<h2 id="5-왜-okhttp-대신-starter-webflux를-선택했는가">5. 왜 OkHttp 대신 starter-webflux를 선택했는가?</h2>

<h3 id="이유-1-이미-있는-의존성으로-충분하다">이유 1: 이미 있는 의존성으로 충분하다</h3>

<p>가장 실질적인 이유다. <code class="language-plaintext highlighter-rouge">spring-boot-starter-webflux</code>가 이미 들어 있어서, <strong>같은 목적(외부 WebSocket 클라이언트)</strong> 을 위해 OkHttp를 추가할 필요가 없었다. OkHttp를 넣으면 “WebSocket 클라이언트 라이브러리”가 두 개가 되고, 빌드 크기와 관리할 포인트만 늘어난다.</p>

<h3 id="이유-2-spring-생태계와-한-줄로-맞출-수-있다">이유 2: Spring 생태계와 한 줄로 맞출 수 있다</h3>

<p><code class="language-plaintext highlighter-rouge">ReactorNettyWebSocketClient</code>는 Spring이 제공·관리하는 빈으로 쓰기 쉽고, Spring Security, 설정 프로퍼티(<code class="language-plaintext highlighter-rouge">@ConfigurationProperties</code>), 빈 라이프사이클과 자연스럽게 맞는다. OkHttp를 쓰면 “연결 풀 설정”, “타임아웃”, “TLS 설정” 등을 Spring 쪽과 어떻게 맞출지 직접 설계해야 한다.</p>

<h3 id="이유-3-리액티브-파이프라인과-같은-언어로-짤-수-있다">이유 3: 리액티브 파이프라인과 같은 언어로 짤 수 있다</h3>

<p>Gemini Live는 <strong>오디오 스트리밍</strong>처럼 연속적인 데이터를 주고받는다. 이걸 <code class="language-plaintext highlighter-rouge">Flux</code>로 표현하면:</p>

<ul>
  <li>스트림 조합(예: 마이크 입력 Flux → 인코딩 → WebSocket send)</li>
  <li>백프레셔, 에러 핸들링, 재시도</li>
</ul>

<p>를 Reactor 연산자로 선언적으로 처리할 수 있다. OkHttp는 “메시지 올 때마다 콜백”이라서, 이 흐름을 Reactor와 이어 주는 래퍼를 만들지 않으면 스트리밍 로직이 나뉘고, 예외 전파·취소 처리도 직접 신경 써야 한다.</p>

<p>정리하면, <strong>이미 WebFlux가 있고, 스트리밍이 핵심</strong>이라서 OkHttp를 추가하지 않고 WebFlux WebSocket 클라이언트를 쓰는 쪽이 더 자연스러웠다.</p>

<hr />

<h2 id="6-wss와-tls--reactor-netty가-해주는-일">6. wss://와 TLS — Reactor Netty가 해주는 일</h2>

<h3 id="wss와-tls가-뭔지">wss://와 TLS가 뭔지</h3>

<p><code class="language-plaintext highlighter-rouge">wss://</code>는 WebSocket <strong>Secure</strong>다. <code class="language-plaintext highlighter-rouge">ws://</code>(암호화 없음)에 <strong>TLS(Transport Layer Security)</strong> 를 붙인 것이다. <code class="language-plaintext highlighter-rouge">https://</code>와 <code class="language-plaintext highlighter-rouge">http://</code> 관계와 같다.</p>

<p>클라이언트가 <code class="language-plaintext highlighter-rouge">wss://</code> 서버에 접속하는 대략적인 단계는 다음과 같다.</p>

<ol>
  <li><strong>TCP 연결</strong> 수립</li>
  <li><strong>TLS 핸드셰이크</strong> — 서버 인증서 검증, 암호화 키 교환</li>
  <li>그 위에서 <strong>WebSocket 업그레이드</strong> (HTTP → WebSocket)</li>
  <li>이후 WebSocket 프레임이 모두 암호화된 채널 위에서 오간다</li>
</ol>

<p>TLS 핸드셰이크를 직접 다루려면 <code class="language-plaintext highlighter-rouge">SSLContext</code>, <code class="language-plaintext highlighter-rouge">SSLEngine</code> 같은 걸 건드려야 해서 꽤 번거롭다.</p>

<h3 id="reactor-netty는-wss를-그대로-받아서-처리한다">Reactor Netty는 wss://를 그대로 받아서 처리한다</h3>

<p><code class="language-plaintext highlighter-rouge">ReactorNettyWebSocketClient</code>에 <code class="language-plaintext highlighter-rouge">wss://</code> URI만 넘기면, <strong>추가 설정 없이</strong> TLS가 적용된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">client</span><span class="o">.</span><span class="na">execute</span><span class="o">(</span>
    <span class="no">URI</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="s">"wss://generativelanguage.googleapis.com/ws/..."</span><span class="o">),</span>
    <span class="n">headers</span> <span class="o">-&gt;</span> <span class="n">headers</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"x-goog-api-key"</span><span class="o">,</span> <span class="n">apiKey</span><span class="o">),</span>
    <span class="n">session</span> <span class="o">-&gt;</span> <span class="cm">/* ... */</span>
<span class="o">);</span>
</code></pre></div></div>

<p>내부적으로 Reactor Netty는:</p>

<ul>
  <li>URI 스킴이 <code class="language-plaintext highlighter-rouge">wss://</code>인지 보고</li>
  <li>JDK 기본 TrustStore(신뢰할 CA 목록)로 서버 인증서를 검증하고</li>
  <li>TLS 핸드셰이크를 수행한 뒤</li>
  <li>WebSocket 프로토콜을 진행한다</li>
</ul>

<p>Google 서버는 공개 CA가 서명한 인증서를 쓰기 때문에, 기본 TrustStore만으로 검증이 통과한다. 그래서 <strong>SSL 관련 코드를 따로 작성할 필요가 없다</strong>.</p>

<p>OkHttp도 TLS는 비슷하게 자동으로 처리해 준다. 차이는, Reactor Netty 쪽은 <strong>비동기 I/O(NIO)</strong> 로 동작해서 TLS 핸드셰이크 중에도 스레드를 블로킹하지 않고 이벤트 루프 위에서 처리한다는 점이다. “TLS가 편한가”보다는 “같은 스택에서 리액티브하게 끝까지 가져갈 수 있는가”가 선택 이유에 가깝다.</p>

<hr />

<h2 id="정리">정리</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>OkHttp</th>
      <th>ReactorNettyWebSocketClient</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>별도 의존성</td>
      <td>필요</td>
      <td>불필요 (starter-webflux에 포함)</td>
    </tr>
    <tr>
      <td>프로그래밍 모델</td>
      <td>콜백</td>
      <td>리액티브 (Mono/Flux)</td>
    </tr>
    <tr>
      <td>Spring 통합</td>
      <td>수동 연동 필요</td>
      <td>자연스러운 통합</td>
    </tr>
    <tr>
      <td>TLS (wss://)</td>
      <td>자동 처리</td>
      <td>자동 처리</td>
    </tr>
    <tr>
      <td>스트리밍/파이프라인</td>
      <td>Reactor와 연결하려면 래퍼 필요</td>
      <td>Flux로 선언적 처리 가능</td>
    </tr>
    <tr>
      <td>잘 맞는 상황</td>
      <td>단순 HTTP 호출, Android 앱 등</td>
      <td>Spring WebFlux 기반, 스트리밍 연동</td>
    </tr>
  </tbody>
</table>

<p>OkHttp 자체가 나쁜 선택은 아니다. 다만 <strong>이미 starter-webflux가 있고</strong>, <strong>Gemini처럼 스트리밍이 중요한 WebSocket 클라이언트</strong>를 만든다면, 새 의존성 없이 Spring 생태계와 리액티브 파이프라인으로 맞추는 쪽이 우리 상황에서는 더 자연스러웠고, 그래서 <strong>ReactorNettyWebSocketClient</strong>를 선택했다.</p>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="projectdiary" /><summary type="html"><![CDATA[Gemini Live API를 WebSocket으로 연동하면서 OkHttp와 Spring WebFlux WebSocket 클라이언트를 비교하고, 우리가 spring-boot-starter-webflux를 선택한 이유를 정리했습니다.]]></summary></entry><entry><title type="html">[TIL] ArrayDeque vs ArrayBlockingQueue — 코드리뷰로 배우는 Java Queue 정리</title><link href="https://nan0silver.github.io/til/2026-03-06-til/" rel="alternate" type="text/html" title="[TIL] ArrayDeque vs ArrayBlockingQueue — 코드리뷰로 배우는 Java Queue 정리" /><published>2026-03-06T00:00:00+00:00</published><updated>2026-03-06T00:00:00+00:00</updated><id>https://nan0silver.github.io/til/til</id><content type="html" xml:base="https://nan0silver.github.io/til/2026-03-06-til/"><![CDATA[<blockquote>
  <p>📝 <strong>TIL (Today I Learned)</strong><br />
“OOM 방지”라고 주석을 달았는데, 실제로는 OOM이 날 수 있는 코드가 되어 있었습니다. 코드리뷰 한 줄이 Java Queue의 핵심 차이를 알려 준 경험을 정리합니다.</p>
</blockquote>

<hr />

<h2 id="-새롭게-배운-것">🍀 새롭게 배운 것</h2>

<ul>
  <li><strong>ArrayDeque</strong>와 <strong>ArrayBlockingQueue</strong>는 이름은 비슷하지만 <strong>설계 목적</strong>이 다르다.</li>
  <li><code class="language-plaintext highlighter-rouge">ArrayDeque(256)</code>의 256은 <strong>초기 용량</strong>일 뿐, 넘치면 자동으로 확장된다. 반면 <code class="language-plaintext highlighter-rouge">ArrayBlockingQueue(256)</code>의 256은 <strong>최대 용량</strong>으로, 절대 늘어나지 않는다.</li>
  <li>리액티브 스트림의 버퍼처럼 <strong>크기를 제한</strong>해야 할 때는 ArrayBlockingQueue가 적합하고, 단순 스택/큐 용도면 ArrayDeque가 더 가볍고 빠르다.</li>
</ul>

<hr />

<h2 id="-oom이란">💥 OOM이란?</h2>

<p><strong>OOM</strong>은 <strong>Out Of Memory</strong>의 줄임말로, JVM이 더 이상 객체를 만들 수 없을 정도로 메모리가 부족해질 때 발생하는 에러다.</p>

<ul>
  <li><strong>대표 에러</strong>: <code class="language-plaintext highlighter-rouge">java.lang.OutOfMemoryError</code></li>
  <li><strong>주로 터지는 곳</strong>: JVM <strong>힙(Heap)</strong> 메모리 (객체가 쌓이는 공간)</li>
  <li><strong>전형적인 원인</strong>: 리스트/큐/맵 같은 버퍼가 <strong>끝없이 커지면서</strong> 메모리를 다 써버리는 경우</li>
</ul>

<p>그래서 코드에서 "OOM 방지"라고 쓴 의도는 보통 <strong>"버퍼가 무한히 커지지 않게 상한을 둬서, 프로세스가 메모리 때문에 죽지 않게 하자"</strong> 는 의미다.</p>

<hr />

<h2 id="-왜-oom을-방지하려고-했나">🧯 왜 OOM을 방지하려고 했나</h2>

<p>여기서 <code class="language-plaintext highlighter-rouge">outboundSink</code>는 <strong>Gemini로 보낼 JSON 메시지를 임시로 쌓아두는 큐(버퍼)</strong> 역할을 한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>사용자 음성 → (PCM → Base64 변환) → outboundSink 큐 → WebSocket → Gemini
                     빠름 ↑                              느림 ↓
</code></pre></div></div>

<p>문제는 <strong>생산 속도 &gt; 소비 속도</strong>인 상황에서 터진다.</p>

<ul>
  <li>Gemini 서버가 느리거나, 네트워크가 불안정하거나, WebSocket send 루프가 잠깐 막히면</li>
  <li>앱은 계속 사용자 음성을 변환해서 큐에 넣으려 하고</li>
  <li><code class="language-plaintext highlighter-rouge">ArrayDeque</code>는 가득 차도 내부 배열을 <strong>계속 키우며 수용</strong>한다 → 힙을 고갈시킬 때까지 성장할 수 있다</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ArrayDeque:         [256] → [512] → [1024] → ... → OOM 💥
ArrayBlockingQueue: [256] → FAIL_OVERFLOW (메시지 드롭, 경고 로그) ✅
</code></pre></div></div>

<p>특히 AI 통화처럼 오디오 데이터가 <strong>실시간·연속적</strong>으로 생성되는 경우, Gemini가 1초만 늦어져도 청크가 빠르게 쌓이고 통화가 길어질수록 누적되기 쉽다.</p>

<p>그래서 <strong>256개 상한</strong>은 단순히 "메모리 아끼자"가 아니라, <strong>"최대 이 정도까지만 버퍼링하고 그 이상은 드롭해서 최신 음성 우선"</strong> 이라는 <strong>의도적인 backpressure 전략</strong>이기도 하다. (음성 통화에서는 오래된 오디오를 지연 전송하는 것보다 드롭하는 편이 UX가 더 낫다.)</p>

<h2 id="-발단-코드리뷰-한-줄">📖 발단: 코드리뷰 한 줄</h2>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 수정 전</span>
<span class="k">this</span><span class="o">.</span><span class="na">outboundSink</span> <span class="o">=</span> <span class="nc">Sinks</span><span class="o">.</span><span class="na">many</span><span class="o">().</span><span class="na">unicast</span><span class="o">().</span><span class="na">onBackpressureBuffer</span><span class="o">(</span><span class="k">new</span> <span class="nc">ArrayDeque</span><span class="o">&lt;&gt;(</span><span class="mi">256</span><span class="o">));</span>

<span class="c1">// 수정 후</span>
<span class="k">this</span><span class="o">.</span><span class="na">outboundSink</span> <span class="o">=</span> <span class="nc">Sinks</span><span class="o">.</span><span class="na">many</span><span class="o">().</span><span class="na">unicast</span><span class="o">().</span><span class="na">onBackpressureBuffer</span><span class="o">(</span><span class="k">new</span> <span class="nc">ArrayBlockingQueue</span><span class="o">&lt;&gt;(</span><span class="mi">256</span><span class="o">));</span>
</code></pre></div></div>

<p>리뷰어의 지적은 명확했습니다.</p>

<p><img src="https://github.com/nan0silver/nan0silver.github.io/blob/main/assets/img/blog/2026-03-06-pic1.png?raw=true" alt="코드리뷰 이미지1" /></p>

<p><code class="language-plaintext highlighter-rouge">256</code>이라는 숫자를 넣었으니 “최대 256개”라고 생각했지만, <strong>ArrayDeque에서는 그렇지 않다</strong>는 걸 몰랐다. 왜 틀렸는지, 두 클래스가 어떻게 다른지부터 정리해 본다.</p>

<hr />

<h2 id="1-java의-queue-계층-구조">1. Java의 Queue 계층 구조</h2>

<p>먼저 전체 그림을 보자.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java.util.Collection
  └── java.util.Queue (interface)
        ├── java.util.Deque (interface)
        │     └── java.util.ArrayDeque (class) ← 오늘의 주인공 1
        └── java.util.concurrent.BlockingQueue (interface)
              └── java.util.concurrent.ArrayBlockingQueue (class) ← 오늘의 주인공 2
</code></pre></div></div>

<p>이름이 비슷해 보이지만, <strong>패키지도 다르고 설계 목적 자체가 다르다.</strong></p>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>ArrayDeque</th>
      <th>ArrayBlockingQueue</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>패키지</td>
      <td><code class="language-plaintext highlighter-rouge">java.util</code></td>
      <td><code class="language-plaintext highlighter-rouge">java.util.concurrent</code></td>
    </tr>
    <tr>
      <td>용량</td>
      <td><strong>무제한</strong> (자동 확장)</td>
      <td><strong>고정</strong> (생성자에서 결정)</td>
    </tr>
    <tr>
      <td>스레드 안전</td>
      <td><strong>아님</strong></td>
      <td><strong>안전</strong> (내부 Lock)</td>
    </tr>
    <tr>
      <td>블로킹</td>
      <td>없음</td>
      <td><code class="language-plaintext highlighter-rouge">put()</code> / <code class="language-plaintext highlighter-rouge">take()</code> 블로킹 지원</td>
    </tr>
    <tr>
      <td>주 용도</td>
      <td>단일 스레드 자료구조</td>
      <td>멀티스레드 생산자-소비자 패턴</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="2-arraydeque--빠르지만-최대-개수-제한이-없다">2. ArrayDeque — 빠르지만 “최대 개수” 제한이 없다</h2>

<h3 id="21-내부-구조-원형-배열">2.1 내부 구조: 원형 배열</h3>

<p>ArrayDeque는 <strong>원형 배열(circular array)</strong> 로 구현된다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>초기 상태 (capacity=256으로 생성했을 때):
[ ][ ][ ][ ]...[ ] (256칸)
 ↑
head = tail = 0
</code></pre></div></div>

<p>데이터를 계속 추가하면:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[A][B][C]...[Z][?]...
                ↑
           용량이 꽉 차면?
</code></pre></div></div>

<p><strong>배열을 두 배로 키운다(grow).</strong> 내부에서 <code class="language-plaintext highlighter-rouge">grow()</code>가 호출되며 <code class="language-plaintext highlighter-rouge">Arrays.copyOf</code>로 더 큰 배열을 만들고 기존 요소를 복사한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// OpenJDK ArrayDeque 내부 코드 (실제 구현 일부)</span>
<span class="kd">private</span> <span class="kt">void</span> <span class="nf">grow</span><span class="o">(</span><span class="kt">int</span> <span class="n">needed</span><span class="o">)</span> <span class="o">{</span>
    <span class="kt">int</span> <span class="n">oldCapacity</span> <span class="o">=</span> <span class="n">elements</span><span class="o">.</span><span class="na">length</span><span class="o">;</span>
    <span class="kt">int</span> <span class="n">newCapacity</span><span class="o">;</span>
    <span class="kt">int</span> <span class="n">jump</span> <span class="o">=</span> <span class="o">(</span><span class="n">oldCapacity</span> <span class="o">&lt;</span> <span class="mi">64</span><span class="o">)</span> <span class="o">?</span> <span class="o">(</span><span class="n">oldCapacity</span> <span class="o">+</span> <span class="mi">2</span><span class="o">)</span> <span class="o">:</span> <span class="o">(</span><span class="n">oldCapacity</span> <span class="o">&gt;&gt;</span> <span class="mi">1</span><span class="o">);</span>
    <span class="n">newCapacity</span> <span class="o">=</span> <span class="n">oldCapacity</span> <span class="o">+</span> <span class="n">jump</span><span class="o">;</span>
    <span class="n">elements</span> <span class="o">=</span> <span class="nc">Arrays</span><span class="o">.</span><span class="na">copyOf</span><span class="o">(</span><span class="n">elements</span><span class="o">,</span> <span class="n">newCapacity</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="22-핵심-256은-초기-용량일-뿐">2.2 핵심: 256은 “초기 용량”일 뿐</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ArrayDeque</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">deque</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayDeque</span><span class="o">&lt;&gt;(</span><span class="mi">256</span><span class="o">);</span>
<span class="c1">// 이 256은 "초기 용량(initial capacity)"</span>
<span class="c1">// 256개가 꽉 차면 512, 1024, ... 로 자동 확장됨</span>
<span class="c1">// 메모리가 허용하는 한 무한 확장 가능</span>
</code></pre></div></div>

<p>그래서 <strong>“최대 256개로 막겠다”</strong> 는 의도와 맞지 않는다. OOM 방지라고 주석을 달았어도, 실제로는 버퍼가 무한히 커질 수 있어 OOM 위험이 남는다.</p>

<h3 id="23-arraydeque가-적합한-상황">2.3 ArrayDeque가 적합한 상황</h3>

<ul>
  <li>단일 스레드에서 스택/큐가 필요할 때</li>
  <li>크기를 제한할 필요가 없을 때</li>
  <li>성능이 최우선일 때 (Lock 없음)</li>
</ul>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 예: DFS 탐색에서 스택으로 사용</span>
<span class="nc">Deque</span><span class="o">&lt;</span><span class="nc">Node</span><span class="o">&gt;</span> <span class="n">stack</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayDeque</span><span class="o">&lt;&gt;();</span>
<span class="n">stack</span><span class="o">.</span><span class="na">push</span><span class="o">(</span><span class="n">root</span><span class="o">);</span>
<span class="k">while</span> <span class="o">(!</span><span class="n">stack</span><span class="o">.</span><span class="na">isEmpty</span><span class="o">())</span> <span class="o">{</span>
    <span class="nc">Node</span> <span class="n">current</span> <span class="o">=</span> <span class="n">stack</span><span class="o">.</span><span class="na">pop</span><span class="o">();</span>
    <span class="c1">// ...</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h2 id="3-arrayblockingqueue--최대-개수가-있고-멀티스레드에-안전하다">3. ArrayBlockingQueue — “최대 개수”가 있고, 멀티스레드에 안전하다</h2>

<p>ArrayBlockingQueue는 <strong>이름 그대로</strong> “배열 기반 + 블로킹이 가능한 큐”다. 처음 보면 “블로킹”이 무슨 의미인지 헷갈릴 수 있으니, 먼저 <strong>BlockingQueue</strong>가 뭔지부터 짚고 간다.</p>

<h3 id="31-blockingqueue란--가득-차면비면-기다리는-큐">3.1 BlockingQueue란? — “가득 차면/비면 기다리는 큐”</h3>

<p>일반 <code class="language-plaintext highlighter-rouge">Queue</code>는 “큐가 가득 찼을 때 넣으려 하면 예외나 false만 반환”한다. <strong>BlockingQueue</strong>는 여기에 <strong>“그럴 때 스레드를 멈추고(블로킹) 자리가 날 때까지 기다린다”</strong> 는 옵션을 추가한 인터페이스다.</p>

<ul>
  <li><strong>put(e)</strong><br />
큐가 <strong>가득 차 있으면</strong> “빈 자리가 생길 때까지” <strong>현재 스레드를 대기</strong>시킨다. (블로킹)</li>
  <li><strong>take()</strong><br />
큐가 <strong>비어 있으면</strong> “원소가 들어올 때까지” <strong>현재 스레드를 대기</strong>시킨다. (블로킹)</li>
</ul>

<p>그래서 <strong>생산자(데이터를 넣는 쪽)</strong> 와 <strong>소비자(데이터를 꺼내는 쪽)</strong> 가 서로 속도를 맞출 수 있다. “큐가 가득 찼는데 계속 넣으려 하면, 넣는 쪽이 잠시 멈춰 있다가 자리가 나면 다시 넣는다” 식으로 동작한다. 이걸 <strong>생산자-소비자(Producer-Consumer) 패턴</strong>이라고 부른다.</p>

<p>ArrayBlockingQueue는 이 BlockingQueue를 <strong>고정 크기 배열</strong>로 구현한 클래스다.</p>

<h3 id="32-내부-구조-고정-배열--lock--condition">3.2 내부 구조: 고정 배열 + Lock + Condition</h3>

<p>ArrayBlockingQueue도 <strong>원형 배열</strong>을 쓰지만, <strong>크기를 절대 늘리지 않는다.</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ArrayBlockingQueue</span><span class="o">&lt;</span><span class="no">E</span><span class="o">&gt;</span> <span class="kd">extends</span> <span class="nc">AbstractQueue</span><span class="o">&lt;</span><span class="no">E</span><span class="o">&gt;</span>
        <span class="kd">implements</span> <span class="nc">BlockingQueue</span><span class="o">&lt;</span><span class="no">E</span><span class="o">&gt;</span> <span class="o">{</span>

    <span class="kd">final</span> <span class="nc">Object</span><span class="o">[]</span> <span class="n">items</span><span class="o">;</span>    <span class="c1">// 고정 크기 배열 (생성 후 크기 변경 없음)</span>
    <span class="kt">int</span> <span class="n">takeIndex</span><span class="o">;</span>            <span class="c1">// 꺼낼 위치</span>
    <span class="kt">int</span> <span class="n">putIndex</span><span class="o">;</span>             <span class="c1">// 넣을 위치</span>
    <span class="kt">int</span> <span class="n">count</span><span class="o">;</span>                <span class="c1">// 현재 원소 수</span>

    <span class="kd">final</span> <span class="nc">ReentrantLock</span> <span class="n">lock</span><span class="o">;</span>         <span class="c1">// 하나의 Lock으로 전체 제어</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Condition</span> <span class="n">notEmpty</span><span class="o">;</span> <span class="c1">// "비어 있음" → take() 대기</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Condition</span> <span class="n">notFull</span><span class="o">;</span>  <span class="c1">// "가득 참" → put() 대기</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><strong>ReentrantLock</strong><br />
여러 스레드가 동시에 <code class="language-plaintext highlighter-rouge">offer</code>, <code class="language-plaintext highlighter-rouge">put</code>, <code class="language-plaintext highlighter-rouge">poll</code>, <code class="language-plaintext highlighter-rouge">take</code> 등을 호출해도, 한 번에 한 스레드만 큐를 수정하도록 보장한다. (스레드 안전)</li>
  <li><strong>Condition (notEmpty, notFull)</strong><br />
“꺼낼 게 생길 때까지 기다려” / “빈 자리가 생길 때까지 기다려” 를 표현한다. <code class="language-plaintext highlighter-rouge">take()</code>는 큐가 비어 있으면 <code class="language-plaintext highlighter-rouge">notEmpty.await()</code>로 대기하고, 누군가 <code class="language-plaintext highlighter-rouge">put()</code>하면 <code class="language-plaintext highlighter-rouge">notEmpty.signal()</code>로 깨운다. 반대로 <code class="language-plaintext highlighter-rouge">put()</code>은 가득 차면 <code class="language-plaintext highlighter-rouge">notFull.await()</code>로 대기한다.</li>
</ul>

<p>생성자에서 배열 크기가 <strong>한 번 정해지면 끝</strong>이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nf">ArrayBlockingQueue</span><span class="o">(</span><span class="kt">int</span> <span class="n">capacity</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">capacity</span> <span class="o">&lt;=</span> <span class="mi">0</span><span class="o">)</span> <span class="k">throw</span> <span class="k">new</span> <span class="nc">IllegalArgumentException</span><span class="o">();</span>
    <span class="k">this</span><span class="o">.</span><span class="na">items</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Object</span><span class="o">[</span><span class="n">capacity</span><span class="o">];</span> <span class="c1">// 이 크기로 고정. 절대 늘어나지 않음</span>
    <span class="k">this</span><span class="o">.</span><span class="na">lock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ReentrantLock</span><span class="o">();</span>
    <span class="k">this</span><span class="o">.</span><span class="na">notEmpty</span> <span class="o">=</span> <span class="n">lock</span><span class="o">.</span><span class="na">newCondition</span><span class="o">();</span>
    <span class="k">this</span><span class="o">.</span><span class="na">notFull</span> <span class="o">=</span> <span class="n">lock</span><span class="o">.</span><span class="na">newCondition</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="33-핵심-256은-최대-용량">3.3 핵심: 256은 “최대 용량”</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ArrayBlockingQueue</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">queue</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayBlockingQueue</span><span class="o">&lt;&gt;(</span><span class="mi">256</span><span class="o">);</span>
<span class="c1">// 이 256은 "최대 용량(maximum capacity)"</span>
<span class="c1">// 256개가 꽉 차면 더 이상 들어가지 않음 (또는 블로킹)</span>
<span class="c1">// 배열이 확장되는 일은 절대 없음</span>
</code></pre></div></div>

<p>그래서 “최대 256개까지만 쌓이게 하겠다”는 요구와 정확히 맞는다.</p>

<h3 id="34-넣을-때-네-가지-선택--add--offer--put--offertimeout">3.4 넣을 때 네 가지 선택 — add / offer / put / offer(timeout)</h3>

<p>“가득 찼을 때 어떻게 할지”를 메서드로 선택할 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ArrayBlockingQueue</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">queue</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayBlockingQueue</span><span class="o">&lt;&gt;(</span><span class="mi">3</span><span class="o">);</span>
<span class="n">queue</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"A"</span><span class="o">);</span>
<span class="n">queue</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"B"</span><span class="o">);</span>
<span class="n">queue</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"C"</span><span class="o">);</span>
<span class="c1">// 큐가 꽉 찬 상태</span>

<span class="c1">// 1. add() → 넣지 못하면 예외</span>
<span class="n">queue</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"D"</span><span class="o">);</span> <span class="c1">// IllegalStateException: Queue full</span>

<span class="c1">// 2. offer() → 넣지 못하면 false 반환 (논블로킹)</span>
<span class="kt">boolean</span> <span class="n">success</span> <span class="o">=</span> <span class="n">queue</span><span class="o">.</span><span class="na">offer</span><span class="o">(</span><span class="s">"D"</span><span class="o">);</span> <span class="c1">// false</span>

<span class="c1">// 3. put() → 빈 자리가 생길 때까지 현재 스레드 대기 (블로킹)</span>
<span class="n">queue</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"D"</span><span class="o">);</span> <span class="c1">// 스레드가 여기서 멈춤</span>

<span class="c1">// 4. offer(e, timeout, unit) → 지정 시간만큼 기다렸다가 실패</span>
<span class="kt">boolean</span> <span class="n">ok</span> <span class="o">=</span> <span class="n">queue</span><span class="o">.</span><span class="na">offer</span><span class="o">(</span><span class="s">"D"</span><span class="o">,</span> <span class="mi">1</span><span class="o">,</span> <span class="nc">TimeUnit</span><span class="o">.</span><span class="na">SECONDS</span><span class="o">);</span> <span class="c1">// 1초 후 false</span>
</code></pre></div></div>

<ul>
  <li><strong>add / offer</strong><br />
“지금 못 넣으면 그냥 실패” (예외 또는 false). 논블로킹.</li>
  <li><strong>put / offer(timeout)</strong><br />
“못 넣으면 기다린다” (블로킹). 생산자-소비자 패턴에서 “큐가 가득 차면 잠시 멈추고 자리 나면 넣기”를 할 때 사용한다.</li>
</ul>

<p>실무에서는 “반드시 넣어야 하는 경우”에는 <code class="language-plaintext highlighter-rouge">put()</code>, “넣을 수 없으면 포기해도 되는 경우”에는 <code class="language-plaintext highlighter-rouge">offer()</code> 또는 <code class="language-plaintext highlighter-rouge">offer(timeout)</code>을 쓰면 된다.</p>

<hr />

<h2 id="4-이번-코드에서-왜-중요한가--reactor-sinks와의-연결">4. 이번 코드에서 왜 중요한가 — Reactor Sinks와의 연결</h2>

<p>원래 코드로 돌아가 보자.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">this</span><span class="o">.</span><span class="na">outboundSink</span> <span class="o">=</span> <span class="nc">Sinks</span><span class="o">.</span><span class="na">many</span><span class="o">().</span><span class="na">unicast</span><span class="o">().</span><span class="na">onBackpressureBuffer</span><span class="o">(</span><span class="k">new</span> <span class="nc">ArrayBlockingQueue</span><span class="o">&lt;&gt;(</span><span class="mi">256</span><span class="o">));</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">Sinks.many().unicast().onBackpressureBuffer(Queue)</code>는 <strong>내부 버퍼로 사용할 Queue를 우리가 넘겨준다.</strong> Reactor는 이 큐에 “아직 보내지 못한 메시지”를 쌓아 두고, 보낼 수 있을 때 꺼내서 보낸다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Gemini로 보낼 메시지 생산 ──→ [outboundSink] ──→ WebSocket send()
                                    ↑
                            여기가 ArrayBlockingQueue
</code></pre></div></div>

<p>메시지를 <strong>보내는 속도</strong>보다 <strong>생산하는 속도</strong>가 빠르면, 큐가 버퍼 역할을 한다.</p>

<h3 id="arraydeque였다면-수정-전">ArrayDeque였다면 (수정 전)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>메시지가 계속 쌓임 → ArrayDeque 자동 확장 → 512 → 1024 → 2048...
→ OutOfMemoryError (OOM)
</code></pre></div></div>

<p>“256개로 막겠다”는 의도와 달리, 실제로는 무한히 커질 수 있어서 OOM 위험이 있다.</p>

<h3 id="arrayblockingqueue로-바꾼-후-수정-후">ArrayBlockingQueue로 바꾼 후 (수정 후)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>메시지가 256개 초과 시 → Queue가 더 받지 않음 (용량 고정)
→ Reactor가 FAIL_OVERFLOW 등을 감지
→ 정해진 오버플로우 처리(예: 로그 출력 후 해당 메시지만 버림)
→ 프로세스는 계속 실행
</code></pre></div></div>

<p>즉, <strong>OOM으로 서버 전체가 죽는 대신</strong>, 메시지 일부 유실로 피해를 최소화할 수 있다. 이게 <strong>백프레셔(Backpressure)</strong> 를 버퍼로 받을 때 “크기를 제한하는 것”의 의미다.</p>

<hr />

<h2 id="5-스레드-안전성--concurrent-패키지가-왜-필요한가">5. 스레드 안전성 — concurrent 패키지가 왜 필요한가</h2>

<h3 id="51-arraydeque는-스레드에-안전하지-않다">5.1 ArrayDeque는 스레드에 안전하지 않다</h3>

<p>여러 스레드가 같은 ArrayDeque에 동시에 <code class="language-plaintext highlighter-rouge">add</code>/<code class="language-plaintext highlighter-rouge">poll</code> 하면, 내부 인덱스가 꼬이면서 <strong>데이터 유실</strong>이나 <strong>ArrayIndexOutOfBoundsException</strong>이 날 수 있다. 단일 스레드 전용이다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 위험한 코드 (멀티스레드 환경)</span>
<span class="nc">ArrayDeque</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">deque</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayDeque</span><span class="o">&lt;&gt;();</span>
<span class="c1">// 스레드 A: deque.add("message");</span>
<span class="c1">// 스레드 B: deque.add("other");  // 동시 수정 → race condition</span>
</code></pre></div></div>

<h3 id="52-arrayblockingqueue는-reentrantlock으로-보호된다">5.2 ArrayBlockingQueue는 ReentrantLock으로 보호된다</h3>

<p><code class="language-plaintext highlighter-rouge">offer</code>, <code class="language-plaintext highlighter-rouge">put</code>, <code class="language-plaintext highlighter-rouge">poll</code>, <code class="language-plaintext highlighter-rouge">take</code> 등이 모두 <strong>같은 Lock</strong>을 걸고 들어가기 때문에, 여러 스레드가 동시에 접근해도 한 번에 한 스레드만 큐를 수정한다. Reactor Sinks는 내부적으로 멀티스레드 환경을 가정하므로, <strong>스레드 안전한 Queue</strong>를 넘겨줘야 한다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ArrayBlockingQueue 내부의 offer() 구현 (요지)</span>
<span class="kd">public</span> <span class="kt">boolean</span> <span class="nf">offer</span><span class="o">(</span><span class="no">E</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
    <span class="kd">final</span> <span class="nc">ReentrantLock</span> <span class="n">lock</span> <span class="o">=</span> <span class="k">this</span><span class="o">.</span><span class="na">lock</span><span class="o">;</span>
    <span class="n">lock</span><span class="o">.</span><span class="na">lock</span><span class="o">();</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">count</span> <span class="o">==</span> <span class="n">items</span><span class="o">.</span><span class="na">length</span><span class="o">)</span>
            <span class="k">return</span> <span class="kc">false</span><span class="o">;</span>
        <span class="n">enqueue</span><span class="o">(</span><span class="n">e</span><span class="o">);</span>
        <span class="k">return</span> <span class="kc">true</span><span class="o">;</span>
    <span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
        <span class="n">lock</span><span class="o">.</span><span class="na">unlock</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h2 id="6-실무에서의-선택-기준">6. 실무에서의 선택 기준</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌─────────────────────────────────────────────────────────┐
│                   Queue 선택 가이드                      │
│                                                         │
│  단일 스레드?                                            │
│    YES → ArrayDeque (빠름, Lock 없음)                   │
│    NO  ↓                                                │
│                                                         │
│  크기를 제한해야 함?                                     │
│    YES → ArrayBlockingQueue (고정 용량 + 스레드 안전)    │
│    NO  → LinkedBlockingQueue (무제한 + 스레드 안전)      │
│                                                         │
│  생산자/소비자 속도 차이가 큰 경우?                       │
│    → ArrayBlockingQueue + put()/take() 블로킹 활용       │
│                                                         │
│  리액티브 파이프라인 버퍼?                                │
│    → ArrayBlockingQueue (크기 제한 + 스레드 안전 둘 다 필요)│
└─────────────────────────────────────────────────────────┘
</code></pre></div></div>

<h3 id="대표적인-사용-사례">대표적인 사용 사례</h3>

<table>
  <thead>
    <tr>
      <th>상황</th>
      <th>적합한 Queue</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DFS/BFS 알고리즘, 역순 처리</td>
      <td><code class="language-plaintext highlighter-rouge">ArrayDeque</code></td>
    </tr>
    <tr>
      <td>함수 호출 스택 시뮬레이션</td>
      <td><code class="language-plaintext highlighter-rouge">ArrayDeque</code></td>
    </tr>
    <tr>
      <td>멀티스레드 작업 큐, ThreadPoolExecutor 내부</td>
      <td><code class="language-plaintext highlighter-rouge">ArrayBlockingQueue</code></td>
    </tr>
    <tr>
      <td>Reactor Sinks 버퍼 (크기 제한 필요)</td>
      <td><code class="language-plaintext highlighter-rouge">ArrayBlockingQueue</code></td>
    </tr>
    <tr>
      <td>로그 비동기 처리 (유실 허용, 크기 제한)</td>
      <td><code class="language-plaintext highlighter-rouge">ArrayBlockingQueue</code></td>
    </tr>
    <tr>
      <td>메시지 큐 (크기 제한 없음, 멀티스레드)</td>
      <td><code class="language-plaintext highlighter-rouge">LinkedBlockingQueue</code></td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-정리">✅ 정리</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>ArrayDeque</th>
      <th>ArrayBlockingQueue</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>핵심 특성</td>
      <td>빠른 단일 스레드 자료구조</td>
      <td>스레드 안전 + <strong>용량 제한</strong></td>
    </tr>
    <tr>
      <td>생성자 인자 의미</td>
      <td><strong>초기 용량</strong> (자동 확장)</td>
      <td><strong>최대 용량</strong> (절대 안 늘어남)</td>
    </tr>
    <tr>
      <td>스레드 안전</td>
      <td><strong>아님</strong></td>
      <td><strong>안전</strong> (ReentrantLock)</td>
    </tr>
    <tr>
      <td>가득 찼을 때</td>
      <td>배열 자동 확장</td>
      <td>false / 예외 / 블로킹</td>
    </tr>
    <tr>
      <td>OOM 가능성</td>
      <td><strong>있음</strong></td>
      <td><strong>없음</strong> (용량 고정)</td>
    </tr>
    <tr>
      <td>성능</td>
      <td>빠름</td>
      <td>상대적으로 느림 (Lock 비용)</td>
    </tr>
  </tbody>
</table>

<p><strong>한 줄 요약:</strong></p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">ArrayDeque(256)</code> → “처음에 256칸 만들어줘, 모자라면 늘릴게”</li>
  <li><code class="language-plaintext highlighter-rouge">ArrayBlockingQueue(256)</code> → “딱 256칸만 써. 넘치면 거절할게”</li>
</ul>

<p>코드리뷰가 알려준 것은 단순한 클래스 교체가 아니라, <strong>“크기 제한”과 “스레드 안전성”</strong> 을 의식하고 자료구조를 골라야 한다는 점이었다.</p>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="til" /><category term="TIL" /><category term="Java" /><category term="Queue" /><category term="ArrayDeque" /><category term="ArrayBlockingQueue" /><category term="Reactor" /><summary type="html"><![CDATA[ArrayDeque와 ArrayBlockingQueue의 차이, 코드리뷰에서 발견한 OOM 위험, 그리고 Reactor Sinks 버퍼로 ArrayBlockingQueue를 선택한 이유를 정리했습니다.]]></summary></entry><entry><title type="html">[TIL] Android(Capacitor) HTTPS 전용 설정 · Manifest merger · usesCleartextTraffic과 CORS</title><link href="https://nan0silver.github.io/til/2026-03-05-android-https-cleartext-capacitor/" rel="alternate" type="text/html" title="[TIL] Android(Capacitor) HTTPS 전용 설정 · Manifest merger · usesCleartextTraffic과 CORS" /><published>2026-03-05T00:00:00+00:00</published><updated>2026-03-05T00:00:00+00:00</updated><id>https://nan0silver.github.io/til/android-https-cleartext-capacitor</id><content type="html" xml:base="https://nan0silver.github.io/til/2026-03-05-android-https-cleartext-capacitor/"><![CDATA[<blockquote>
  <p>📝 <strong>TIL (Today I Learned)</strong><br />
Capacitor로 빌드한 Android 앱에서 <strong>HTTPS만 사용하도록</strong> 보안 설정을 바꾸다가, 카카오 SDK와 Manifest merger 충돌이 났고, <code class="language-plaintext highlighter-rouge">tools:replace</code>로 해결했다.<br />
이 과정에서 <strong>usesCleartextTraffic이 무엇인지</strong>, <strong>CORS와는 어떻게 다른지</strong>까지 정리해 본다.</p>
</blockquote>

<ul>
  <li><a href="#-1-배경-하고-싶었던-일">1. 배경: 하고 싶었던 일</a></li>
  <li><a href="#-2-usescleartexttraffic이란">2. usesCleartextTraffic이란?</a></li>
  <li><a href="#-3-실제로-한-설정-변경">3. 실제로 한 설정 변경</a></li>
  <li><a href="#-4-manifest-merger-충돌-카카오-sdk">4. Manifest merger 충돌 (카카오 SDK)</a></li>
  <li><a href="#-5-toolsreplace로-해결">5. tools:replace로 해결</a></li>
  <li><a href="#-6-혼동하기-쉬운-점-cors">6. 혼동하기 쉬운 점 (CORS)</a></li>
  <li><a href="#-7-배포-시-주의사항--정리">7. 배포 시 주의사항 · 정리</a></li>
</ul>

<hr />

<h2 id="-1-배경-하고-싶었던-일">🎯 1. 배경: 하고 싶었던 일</h2>

<ul>
  <li><strong>Capacitor</strong>로 웹 앱을 감싼 Android 앱을 쓰고 있음.</li>
  <li>백엔드/API는 모두 <strong>HTTPS</strong>(예: <code class="language-plaintext highlighter-rouge">chingoohaja.app</code>)만 사용함.</li>
  <li>그래서 앱에서 <strong>HTTP(암호화되지 않은 통신)는 허용하지 않고, HTTPS만 쓰도록</strong> 하고 싶었다.</li>
  <li>Android에서는 이걸 <strong><code class="language-plaintext highlighter-rouge">usesCleartextTraffic</code></strong> 과 <strong>Network Security Config</strong> 로 제어한다.</li>
</ul>

<hr />

<h2 id="-2-usescleartexttraffic이란">📌 2. usesCleartextTraffic이란?</h2>

<p><strong>Cleartext</strong> = 암호화되지 않은 평문 통신. 즉 <strong>HTTP</strong>를 의미한다.</p>

<table>
  <thead>
    <tr>
      <th style="text-align: left">값</th>
      <th style="text-align: left">의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">true</code></td>
      <td style="text-align: left">앱이 <strong>HTTP</strong> 주소로도 요청 가능 (개발/테스트용으로 자주 켜 둠)</td>
    </tr>
    <tr>
      <td style="text-align: left"><code class="language-plaintext highlighter-rouge">false</code></td>
      <td style="text-align: left"><strong>HTTPS만</strong> 허용, HTTP 요청은 <strong>차단</strong></td>
    </tr>
  </tbody>
</table>

<p>즉, <strong>“앱이 HTTP를 쓸 수 있게 할지 여부”</strong>만 정하는 설정이다.<br />
API가 전부 HTTPS라면 보안을 위해 <code class="language-plaintext highlighter-rouge">false</code>로 두는 것이 맞다.</p>

<hr />

<h2 id="-3-실제로-한-설정-변경">🔧 3. 실제로 한 설정 변경</h2>

<h3 id="31-androidmanifestxml">3.1 AndroidManifest.xml</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">android:usesCleartextTraffic="true"</code> → <strong><code class="language-plaintext highlighter-rouge">false</code></strong> 로 변경</li>
  <li>(이미 있다면) <code class="language-plaintext highlighter-rouge">android:networkSecurityConfig="@xml/network_security_config"</code> 유지</li>
</ul>

<h3 id="32-network_security_configxml">3.2 network_security_config.xml</h3>

<ul>
  <li><strong>base-config</strong> 의 <code class="language-plaintext highlighter-rouge">cleartextTrafficPermitted="true"</code> → <strong><code class="language-plaintext highlighter-rouge">false</code></strong></li>
  <li>API가 모두 HTTPS(<code class="language-plaintext highlighter-rouge">chingoohaja.app</code> 등)이므로, cleartext를 허용하던 <strong>domain-config</strong> 블록은 제거</li>
</ul>

<p>이렇게 하면 앱 전체에서 HTTP는 막히고, HTTPS만 허용된다.</p>

<hr />

<h2 id="️-4-manifest-merger-충돌-카카오-sdk">⚠️ 4. Manifest merger 충돌 (카카오 SDK)</h2>

<p>위처럼 바꾼 뒤 <strong>Android Studio에서 빌드</strong>하면 아래와 같은 에러가 날 수 있다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Manifest merger failed : Attribute application@usesCleartextTraffic value=(false) from AndroidManifest.xml:4:9-45
is also present at [com.kakao.sdk:v2-friend:2.20.1] AndroidManifest.xml:10:18-53 value=(true).
Suggestion: add 'tools:replace="android:usesCleartextTraffic"' to &lt;application&gt; element at AndroidManifest.xml:3:5-74:19 to override.
</code></pre></div></div>

<p><strong>원인</strong></p>

<ul>
  <li>우리 앱: <code class="language-plaintext highlighter-rouge">usesCleartextTraffic="false"</code></li>
  <li>카카오 SDK(<code class="language-plaintext highlighter-rouge">com.kakao.sdk:v2-friend</code>): 자체 Manifest에서 <code class="language-plaintext highlighter-rouge">usesCleartextTraffic="true"</code> 선언</li>
  <li><strong>Manifest merger</strong>가 여러 Manifest를 합칠 때, 같은 속성에 서로 다른 값이 있으면 충돌로 처리한다.</li>
</ul>

<p>그래서 “예전처럼 cleartext를 다시 켜야 하나?”라고 생각할 수 있지만, <strong>되돌릴 필요는 없다.</strong><br />
대신 <strong>우리 앱의 값(false)을 우선시키면</strong> 된다.</p>

<hr />

<h2 id="-5-toolsreplace로-해결">✅ 5. tools:replace로 해결</h2>

<p>에러 메시지에서 제안한 대로, <strong><code class="language-plaintext highlighter-rouge">&lt;application&gt;</code> 태그에 <code class="language-plaintext highlighter-rouge">tools:replace="android:usesCleartextTraffic"</code></strong> 를 추가하면 된다.</p>

<p><strong>의미</strong></p>

<ul>
  <li>머지 시 <strong>카카오 SDK의 <code class="language-plaintext highlighter-rouge">true</code></strong> 대신 <strong>우리 앱의 <code class="language-plaintext highlighter-rouge">false</code></strong> 가 적용된다.</li>
  <li><code class="language-plaintext highlighter-rouge">xmlns:tools</code> 는 보통 manifest 최상단에 이미 있으므로, 별도 선언 없이 <code class="language-plaintext highlighter-rouge">tools:replace</code>만 추가하면 된다.</li>
</ul>

<p><strong>변경 예시 (AndroidManifest.xml)</strong></p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;application</span>
    <span class="na">android:usesCleartextTraffic=</span><span class="s">"false"</span>
    <span class="na">tools:replace=</span><span class="s">"android:usesCleartextTraffic"</span>
    <span class="na">android:networkSecurityConfig=</span><span class="s">"@xml/network_security_config"</span>
    <span class="err">...</span><span class="nt">&gt;</span>
</code></pre></div></div>

<p>이렇게 하면 빌드는 통과하고, 앱은 계속 <strong>HTTPS만</strong> 사용하게 된다.<br />
카카오 로그인 등도 실제로 HTTPS를 쓰고 있다면 동작에는 문제 없다.</p>

<hr />

<h2 id="-6-혼동하기-쉬운-점-cors">🔗 6. 혼동하기 쉬운 점 (CORS)</h2>

<p><strong>“이 설정 바꾸면 CORS 나오지 않나?”</strong> 하고 헷갈릴 수 있는데, <strong>CORS와는 별개</strong>다.<br />
usesCleartextTraffic은 “앱이 HTTP를 허용할지”만 정하고, CORS는 “어떤 출처를 서버가 허용할지”를 서버가 정한다.<br />
그래서 이 값을 false로 둔다고 CORS가 생기거나 사라지지 않으며, CORS는 백엔드 허용 origin/헤더 설정으로만 처리하면 된다.</p>

<hr />

<h2 id="-7-배포-시-주의사항--정리">📋 7. 배포 시 주의사항 · 정리</h2>

<ul>
  <li><strong>usesCleartextTraffic=”false”</strong>
    <ul>
      <li>HTTP 차단, HTTPS만 허용 → 보안상 유리.</li>
      <li>API가 전부 HTTPS면 배포 후에도 그대로 두면 된다.</li>
    </ul>
  </li>
  <li><strong>Manifest merger 충돌</strong>
    <ul>
      <li>서드파티 SDK가 <code class="language-plaintext highlighter-rouge">usesCleartextTraffic="true"</code> 를 들고 오면,<br />
<strong>되돌리지 말고</strong> <code class="language-plaintext highlighter-rouge">tools:replace="android:usesCleartextTraffic"</code> 로 <strong>우리 값(false)</strong> 을 우선 적용하는 것이 좋다.</li>
    </ul>
  </li>
  <li><strong>CORS</strong>는 서버 쪽 허용 설정의 영역이라, 이 Android 설정과는 무관하다.</li>
</ul>

<p>정리하면, <strong>되돌릴 필요 없이</strong> <code class="language-plaintext highlighter-rouge">false</code> + <code class="language-plaintext highlighter-rouge">tools:replace</code> 로 두고 배포해도 되고,<br />
API와 카카오가 HTTPS만 사용한다면 그대로 두는 것이 맞다.</p>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="til" /><category term="TIL" /><category term="Android" /><category term="Capacitor" /><category term="보안" /><category term="HTTPS" /><category term="Manifest" /><summary type="html"><![CDATA[Capacitor Android 앱에서 HTTPS만 사용하도록 usesCleartextTraffic을 false로 설정할 때, 카카오 SDK와의 Manifest merger 충돌을 tools:replace로 해결하는 방법과, CORS와의 관계를 정리했습니다.]]></summary></entry><entry><title type="html">[친구하자] CI/CD 파이프라인 구축기: GitHub Actions + Docker + AWS ECR/EC2 배포 자동화</title><link href="https://nan0silver.github.io/projectdiary/2026-02-26-diary/" rel="alternate" type="text/html" title="[친구하자] CI/CD 파이프라인 구축기: GitHub Actions + Docker + AWS ECR/EC2 배포 자동화" /><published>2026-02-26T00:00:00+00:00</published><updated>2026-03-09T02:06:30+00:00</updated><id>https://nan0silver.github.io/projectdiary/diary</id><content type="html" xml:base="https://nan0silver.github.io/projectdiary/2026-02-26-diary/"><![CDATA[<blockquote>
  <p><a href="https://nan0silver.github.io/projectdiary/2026-02-24-diary/">이전 일기: Android 앱을 만들며 배운 CI/CD와 AWS EC2·ECS</a>에서 CI/CD, Docker, ECR, EC2 개념과 전체 흐름을 다뤘다.<br />
이번에는 <strong>실제로 파이프라인을 구축한 과정</strong>과 <strong>배포하면서 겪은 트러블슈팅</strong>을 중심으로 정리한다.</p>
</blockquote>

<ul>
  <li><a href="#1-전체-아키텍처">1. 전체 아키텍처</a></li>
  <li><a href="#2-aws-ecr에-이미지-푸시">2. AWS ECR에 이미지 푸시</a></li>
  <li><a href="#3-ec2-배포">3. EC2 배포</a></li>
  <li><a href="#4-트러블슈팅-실제로-겪은-5가지-문제">4. 트러블슈팅: 실제로 겪은 5가지 문제</a></li>
  <li><a href="#5-iam-instance-role-장기-자격증명-없이-ecr-접근">5. IAM Instance Role</a></li>
  <li><a href="#6-환경변수-관리-env-파일-전략">6. 환경변수 관리</a></li>
  <li><a href="#마무리-완성된-cd-흐름">마무리</a></li>
</ul>

<hr />

<h2 id="1-전체-아키텍처">1. 전체 아키텍처</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[ 개발자 ]
    │ git push (main 브랜치)
    ↓
[ GitHub ]
    │ CI 워크플로우 실행 (ci.yml)
    │ - Gradle 빌드
    │ - 테스트 실행
    ↓ (CI 성공 시)
[ CD 워크플로우 실행 (cd.yml) ]
    │
    ├─ Docker 이미지 빌드
    │
    ├─ AWS ECR(컨테이너 레지스트리)에 이미지 push
    │
    └─ EC2 서버에 SSH 접속
           │
           ├─ ECR에서 새 이미지 pull
           ├─ 기존 컨테이너 종료 &amp; 삭제
           └─ 새 컨테이너 실행
</code></pre></div></div>

<p><strong>왜 CI와 CD를 나눌까?</strong><br />
CI 워크플로우(ci.yml)는 push할 때마다 빌드·테스트만 하고, CD 워크플로우(cd.yml)는 <strong>CI가 성공했을 때만</strong> 배포를 맡는다. 이렇게 나누면 테스트가 실패한 코드는 절대 서버에 올라가지 않는다. 한 파일에 다 넣어도 되지만, <code class="language-plaintext highlighter-rouge">workflow_run</code>으로 “다른 워크플로우 완료”를 트리거로 쓰면 CI 결과에 따라 CD 실행 여부를 분리하기 쉽다.</p>

<hr />

<h2 id="2-aws-ecr에-이미지-푸시">2. AWS ECR에 이미지 푸시</h2>

<p>이전 글에서 ECR의 역할을 다뤘다. 여기서는 <strong>GitHub Actions에서 ECR에 푸시하는 방법</strong>을 본다.</p>

<p>GitHub Actions 워크플로우는 GitHub이 제공하는 <strong>Runner(가상 머신)</strong> 위에서 실행된다. 이 Runner에는 Docker가 미리 설치되어 있어서, 워크플로우 안에서 <code class="language-plaintext highlighter-rouge">docker build</code>, <code class="language-plaintext highlighter-rouge">docker tag</code>, <code class="language-plaintext highlighter-rouge">docker push</code> 같은 <strong>Docker CLI 명령어</strong>를 그대로 쓸 수 있다. 즉, “이미지 빌드”는 Runner에서 Docker로 하고, “저장”만 ECR에 하는 구조다. ECR은 이미지 보관소일 뿐, 빌드를 대신 해 주지 않는다. 그래서 CI 단계에서 Docker로 이미지를 만들고, 만든 이미지를 ECR 주소로 푸시하는 방식이 된다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>로컬/CI 서버          AWS ECR                  EC2
docker build  →  docker push  →  docker pull → docker run
(이미지 생성)    (업로드)         (다운로드)     (실행)
</code></pre></div></div>

<h3 id="github-actions에서-ecr에-push하는-코드">GitHub Actions에서 ECR에 push하는 코드</h3>

<p><strong>필요한 사전 설정</strong><br />
<code class="language-plaintext highlighter-rouge">secrets.AWS_ACCESS_KEY_ID</code>, <code class="language-plaintext highlighter-rouge">AWS_SECRET_ACCESS_KEY</code>, <code class="language-plaintext highlighter-rouge">AWS_REGION</code> 값은 <strong>GitHub 저장소 → Settings → Secrets and variables → Actions</strong>에서 등록해야 한다. 워크플로우 안에서는 <code class="language-plaintext highlighter-rouge">$</code>으로만 참조할 수 있고, 로그에는 값이 노출되지 않는다.</p>

<p><strong>ECR 로그인을 왜 하냐면</strong><br />
Docker는 이미지를 푸시할 때 레지스트리(ECR 포함)에 <strong>인증</strong>을 요구한다. <code class="language-plaintext highlighter-rouge">amazon-ecr-login</code> 액션은 위에서 설정한 AWS 자격증명으로 ECR에 로그인해 두어서, 그 다음 <code class="language-plaintext highlighter-rouge">docker push</code>가 비공개 레지스트리에 올라갈 수 있게 해 준다.</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">AWS 자격증명 설정</span>
  <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/configure-aws-credentials@v4</span>
  <span class="na">with</span><span class="pi">:</span>
    <span class="na">aws-access-key-id</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">aws-secret-access-key</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">aws-region</span><span class="pi">:</span> <span class="s">$</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">ECR 로그인</span>
  <span class="na">id</span><span class="pi">:</span> <span class="s">login-ecr</span>
  <span class="na">uses</span><span class="pi">:</span> <span class="s">aws-actions/amazon-ecr-login@v2</span>

<span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Docker 이미지 빌드 및 ECR 푸시</span>
  <span class="na">env</span><span class="pi">:</span>
    <span class="na">ECR_REGISTRY</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">ECR_REPOSITORY</span><span class="pi">:</span> <span class="s">chingoo-haja</span>
    <span class="na">IMAGE_TAG</span><span class="pi">:</span> <span class="s">$</span>
  <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
    <span class="s">docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .</span>
    <span class="s">docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG \</span>
               <span class="s">$ECR_REGISTRY/$ECR_REPOSITORY:latest</span>
    <span class="s">docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG</span>
    <span class="s">docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest</span>
</code></pre></div></div>

<p>이미지에 <strong>두 개의 태그</strong>를 붙인다:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">$</code></strong> (커밋 해시): “이 커밋에서 만든 이미지”를 정확히 가리킨다. 롤백할 때 <code class="language-plaintext highlighter-rouge">docker pull ...:abc123</code>처럼 특정 버전을 지정할 수 있고, 어떤 배포가 어떤 코드에서 나왔는지 추적하기 좋다.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">latest</code></strong>: “지금 최신” 이미지를 가리킨다. 매번 같은 이름을 쓰면 EC2에서 <code class="language-plaintext highlighter-rouge">docker pull ...:latest</code>만 해도 최신으로 갱신할 수 있다.</li>
</ul>

<hr />

<h2 id="3-ec2-배포">3. EC2 배포</h2>

<h3 id="cd-워크플로우-트리거-설정">CD 워크플로우 트리거 설정</h3>

<p>CD는 CI가 성공했을 때만 실행되어야 한다. <code class="language-plaintext highlighter-rouge">workflow_run</code>으로 설정:</p>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">on</span><span class="pi">:</span>
  <span class="na">workflow_run</span><span class="pi">:</span>
    <span class="na">workflows</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">CI"</span><span class="pi">]</span>
    <span class="na">types</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">completed</span><span class="pi">]</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">deploy</span><span class="pi">:</span>
    <span class="na">if</span><span class="pi">:</span> <span class="s">$</span>
</code></pre></div></div>

<p><strong>개발자가 꼭 알아둘 점</strong><br />
<code class="language-plaintext highlighter-rouge">types: [completed]</code>는 CI 워크플로우가 <strong>끝났을 때</strong>(성공이든 실패든) CD를 트리거한다는 뜻이다. 그래서 <strong>반드시</strong> <code class="language-plaintext highlighter-rouge">if: conclusion == 'success'</code>를 걸어야 한다. 이 조건이 없으면 CI가 실패(테스트 실패, 빌드 실패)해도 CD가 돌아가서, 깨진 코드가 서버에 배포될 수 있다.</p>

<h3 id="ssh로-ec2에-접속해서-배포">SSH로 EC2에 접속해서 배포</h3>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">EC2에 SSH 배포</span>
  <span class="na">uses</span><span class="pi">:</span> <span class="s">appleboy/ssh-action@v1.2.2</span>
  <span class="na">with</span><span class="pi">:</span>
    <span class="na">host</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">username</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">key</span><span class="pi">:</span> <span class="s">$</span>
    <span class="na">script</span><span class="pi">:</span> <span class="pi">|</span>
      <span class="s"># ECR에서 최신 이미지 다운로드 (아래 변수들은 job env에서 전달해야 함)</span>
      <span class="s">aws ecr get-login-password --region $AWS_REGION | \</span>
        <span class="s">docker login --username AWS --password-stdin $ECR_REGISTRY</span>

      <span class="s">docker pull $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG</span>

      <span class="s"># 기존 컨테이너 종료 &amp; 삭제</span>
      <span class="s">docker stop chingoo-haja || true</span>
      <span class="s">docker rm chingoo-haja || true</span>

      <span class="s"># 새 컨테이너 실행</span>
      <span class="s">docker run -d \</span>
        <span class="s">--name chingoo-haja \</span>
        <span class="s">--restart unless-stopped \</span>
        <span class="s">-p 8080:8080 \</span>
        <span class="s">--env-file $HOME/app/.env \</span>
        <span class="s">-v $HOME/app/firebase-service-account.json:/firebase-service-account.json:ro \</span>
        <span class="s">$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG</span>
</code></pre></div></div>

<p>각 옵션의 의미:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">-d</code>: 백그라운드 실행 (detached mode)</li>
  <li><code class="language-plaintext highlighter-rouge">--name chingoo-haja</code>: 컨테이너에 이름 부여</li>
  <li><code class="language-plaintext highlighter-rouge">--restart unless-stopped</code>: 서버 재시작 시 자동으로 컨테이너도 재시작</li>
  <li><code class="language-plaintext highlighter-rouge">-p 8080:8080</code>: 호스트의 8080 포트 → 컨테이너의 8080 포트로 연결</li>
  <li><code class="language-plaintext highlighter-rouge">--env-file</code>: 환경변수 파일 주입</li>
  <li><code class="language-plaintext highlighter-rouge">-v</code>: 파일 시스템 마운트 (볼륨). <code class="language-plaintext highlighter-rouge">:ro</code>는 read-only라서 컨테이너 안에서는 해당 경로를 수정할 수 없고, 보안·실수 방지에 유리하다.</li>
</ul>

<p><strong>EC2 script에서 쓰는 변수</strong><br />
<code class="language-plaintext highlighter-rouge">$ECR_REGISTRY</code>, <code class="language-plaintext highlighter-rouge">$ECR_REPOSITORY</code>, <code class="language-plaintext highlighter-rouge">$IMAGE_TAG</code>, <code class="language-plaintext highlighter-rouge">$AWS_REGION</code>은 CD job의 <code class="language-plaintext highlighter-rouge">env</code>에서 정의해 두어야 한다. ECR 푸시 단계에서 쓴 값과 동일하게, 같은 워크플로우 안에서 <code class="language-plaintext highlighter-rouge">env</code>나 <code class="language-plaintext highlighter-rouge">steps.xxx.outputs</code>로 넘겨 주면 된다. 이 값들이 없으면 EC2에 SSH로 들어가도 “어떤 이미지를 pull할지” 알 수 없다.</p>

<hr />

<h2 id="4-트러블슈팅-실제로-겪은-5가지-문제">4. 트러블슈팅: 실제로 겪은 5가지 문제</h2>

<h3 id="문제-1-docker-command-not-found">문제 1: <code class="language-plaintext highlighter-rouge">docker: command not found</code></h3>

<p><strong>증상</strong>: CD가 실패하면서 “docker: command not found” 에러</p>

<p><strong>원인</strong>: EC2 인스턴스에 Docker가 설치되어 있지 않았다.</p>

<p><strong>해결</strong>: EC2에 SSH 접속 후 Docker 직접 설치</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>yum <span class="nb">install</span> <span class="nt">-y</span> docker
<span class="nb">sudo </span>systemctl start docker
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>docker          <span class="c"># 서버 재시작 시 자동 시작</span>
<span class="nb">sudo </span>usermod <span class="nt">-aG</span> docker ec2-user      <span class="c"># sudo 없이 docker 명령 실행</span>
</code></pre></div></div>

<p><strong>참고</strong>: <code class="language-plaintext highlighter-rouge">usermod -aG docker ec2-user</code>를 적용한 뒤에는 <strong>해당 사용자로 한 번 재로그인</strong>해야 그룹 변경이 반영된다. SSH로 접속한 세션이라면 한 번 끊었다가 다시 접속하거나, 새 터미널에서 SSH로 들어가면 된다.</p>

<h3 id="문제-2-address-already-in-use-포트-8080-충돌">문제 2: <code class="language-plaintext highlighter-rouge">address already in use</code> (포트 8080 충돌)</h3>

<p><strong>증상</strong>: 새 컨테이너 실행 시 “bind: address already in use” 에러</p>

<p><strong>원인</strong>: 기존에 <code class="language-plaintext highlighter-rouge">java -jar</code> 방식으로 직접 실행 중이던 Spring Boot 프로세스가 아직 8080 포트를 점유하고 있었다.</p>

<p><strong>해결</strong>: Docker 배포 전 포트를 강제로 해제</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>fuser <span class="nt">-k</span> 8080/tcp <span class="o">||</span> <span class="nb">true</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">fuser</code>는 특정 포트를 사용 중인 프로세스를 찾아서 종료하는 명령이다. <code class="language-plaintext highlighter-rouge">|| true</code>는 “이미 아무도 없어도 에러 내지 마라”는 의미.</p>

<h3 id="문제-3-컨테이너가-created-상태에서-멈춤">문제 3: 컨테이너가 <code class="language-plaintext highlighter-rouge">created</code> 상태에서 멈춤</h3>

<p><strong>증상</strong>: <code class="language-plaintext highlighter-rouge">docker ps</code>에서 컨테이너가 <code class="language-plaintext highlighter-rouge">created</code> 상태 (실행 전 상태)로 멈춰있음</p>

<p><strong>원인</strong>: 포트 충돌로 인해 컨테이너가 시작 자체를 못 함</p>

<p><strong>해결</strong>: 헬스체크 코드에서 <code class="language-plaintext highlighter-rouge">created</code>도 실패로 처리</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">STATUS</span><span class="o">=</span><span class="si">$(</span>docker inspect <span class="nt">--format</span><span class="o">=</span><span class="s1">''</span> chingoo-haja<span class="si">)</span>
<span class="k">if</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$STATUS</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"exited"</span> <span class="o">]</span> <span class="o">||</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$STATUS</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"dead"</span> <span class="o">]</span> <span class="o">||</span> <span class="o">[</span> <span class="s2">"</span><span class="nv">$STATUS</span><span class="s2">"</span> <span class="o">=</span> <span class="s2">"created"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
  </span><span class="nb">echo</span> <span class="s2">"컨테이너 기동 실패 (status: </span><span class="nv">$STATUS</span><span class="s2">)"</span>
  docker logs chingoo-haja <span class="nt">--tail</span> 50
  <span class="nb">exit </span>1
<span class="k">fi</span>
</code></pre></div></div>

<p>이 검사는 <strong>CD 스크립트 안에서</strong> <code class="language-plaintext highlighter-rouge">docker run</code> 직후(또는 짧은 대기 후) 실행한다. <code class="language-plaintext highlighter-rouge">exit 1</code>로 빠지면 GitHub Actions job이 실패 처리되어 “배포는 했는데 컨테이너가 안 떴다”는 상황을 CI/CD 단계에서 바로 알 수 있다.</p>

<h3 id="문제-4-env-파일-수정-후에도-변경이-반영되지-않음">문제 4: <code class="language-plaintext highlighter-rouge">.env</code> 파일 수정 후에도 변경이 반영되지 않음</h3>

<p><strong>증상</strong>: EC2의 <code class="language-plaintext highlighter-rouge">.env</code> 파일을 수정하고 <code class="language-plaintext highlighter-rouge">docker restart</code>를 했는데 여전히 이전 값이 사용됨</p>

<p><strong>원인</strong>: <strong><code class="language-plaintext highlighter-rouge">docker restart</code>는 <code class="language-plaintext highlighter-rouge">--env-file</code>을 다시 읽지 않는다!</strong></p>

<p>이게 초보자가 가장 많이 실수하는 부분이다. Docker 컨테이너의 환경변수는 <code class="language-plaintext highlighter-rouge">docker run</code> 시점에 한 번만 읽혀서 컨테이너 내부에 고정된다. <code class="language-plaintext highlighter-rouge">docker restart</code>는 같은 환경변수로 재시작할 뿐이다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ❌ 이렇게 해도 .env 변경사항이 반영되지 않음</span>
docker restart chingoo-haja

<span class="c"># ✅ 컨테이너를 완전히 삭제하고 새로 실행해야 함</span>
docker stop chingoo-haja <span class="o">&amp;&amp;</span> docker <span class="nb">rm </span>chingoo-haja
docker run <span class="nt">-d</span> <span class="nt">--name</span> chingoo-haja <span class="nt">--env-file</span> ~/app/.env ...
</code></pre></div></div>

<h3 id="문제-5-firebase-서비스-계정-json-파일-누락">문제 5: Firebase 서비스 계정 JSON 파일 누락</h3>

<p><strong>증상</strong>: 컨테이너 시작 시 <code class="language-plaintext highlighter-rouge">FileNotFoundException: class path resource [firebase-service-account.json] cannot be opened</code></p>

<p><strong>원인</strong>: Firebase 인증에 필요한 JSON 파일은 보안상 <code class="language-plaintext highlighter-rouge">.gitignore</code>에 등록되어 있다. 따라서 Git에 커밋되지 않고, Docker 이미지에도 포함되지 않는다.</p>

<p><strong>잘못된 접근</strong>: 파일을 Git에 커밋 → 보안 사고</p>

<p><strong>올바른 접근</strong>: 두 가지 변경</p>

<ol>
  <li><strong>코드 수정</strong>: <code class="language-plaintext highlighter-rouge">ClassPathResource</code> → <code class="language-plaintext highlighter-rouge">ResourceLoader</code>로 변경</li>
</ol>

<p><code class="language-plaintext highlighter-rouge">ClassPathResource</code>는 JAR/classpath 안의 리소스만 찾을 수 있다. 서버에서는 파일을 컨테이너 밖(EC2 경로)에 두고 <code class="language-plaintext highlighter-rouge">file:</code> 경로로 넘기므로, Spring의 <code class="language-plaintext highlighter-rouge">Resource</code> 추상화를 쓰는 <code class="language-plaintext highlighter-rouge">ResourceLoader.getResource(path)</code>로 바꾸면 <code class="language-plaintext highlighter-rouge">classpath:</code>, <code class="language-plaintext highlighter-rouge">file:</code> 둘 다 지원한다. <code class="language-plaintext highlighter-rouge">path</code>에 <code class="language-plaintext highlighter-rouge">file:/firebase-service-account.json</code>처럼 넘기면 컨테이너 내부의 해당 경로(볼륨으로 마운트된 파일)를 읽게 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Before: JAR 내부에서만 파일을 찾음</span>
<span class="nc">ClassPathResource</span> <span class="n">serviceAccount</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ClassPathResource</span><span class="o">(</span><span class="n">path</span><span class="o">);</span>

<span class="c1">// After: classpath:, file: 등 다양한 위치 지원</span>
<span class="nc">Resource</span> <span class="n">serviceAccount</span> <span class="o">=</span> <span class="n">resourceLoader</span><span class="o">.</span><span class="na">getResource</span><span class="o">(</span><span class="n">path</span><span class="o">);</span>
</code></pre></div></div>

<ol>
  <li><strong>파일을 EC2에 직접 올리고 볼륨 마운트</strong></li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># EC2에 파일 업로드 (로컬에서)</span>
scp <span class="nt">-i</span> key.pem firebase-service-account.json ec2-user@&lt;EC2_IP&gt;:~/app/

<span class="c"># .env에서 경로 설정</span>
<span class="nv">FIREBASE_SERVICE_ACCOUNT_PATH</span><span class="o">=</span>file:/firebase-service-account.json
</code></pre></div></div>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># cd.yml에서 볼륨 마운트</span>
<span class="s">docker run -d \</span>
<span class="s">-v $HOME/app/firebase-service-account.json:/firebase-service-account.json:ro \</span>
<span class="nn">...</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">file:</code> 접두사는 파일 시스템 경로를, <code class="language-plaintext highlighter-rouge">classpath:</code> 접두사는 JAR 내부를 의미한다.</p>

<hr />

<h2 id="5-iam-instance-role-장기-자격증명-없이-ecr-접근">5. IAM Instance Role: 장기 자격증명 없이 ECR 접근</h2>

<p>처음에는 EC2의 배포 스크립트에 <code class="language-plaintext highlighter-rouge">AWS_ACCESS_KEY_ID</code>, <code class="language-plaintext highlighter-rouge">AWS_SECRET_ACCESS_KEY</code>를 환경변수로 넘기려 했다. 이는 보안상 좋지 않다.</p>

<p><strong>더 나은 방법: IAM Instance Role</strong></p>

<p>EC2 인스턴스 자체에 <strong>IAM 역할(Instance Profile)</strong>을 붙여 두면, 그 EC2 안에서 돌아가는 프로세스(배포 스크립트 포함)가 <strong>별도 액세스 키 없이</strong> 해당 역할의 권한을 쓰게 된다. AWS 콘솔에서 IAM 역할을 만들고 “EC2가 ECR에서 pull할 수 있음” 같은 정책을 붙인 뒤, EC2 인스턴스 설정에서 “해당 역할 연결”만 해 주면 된다. 그러면 EC2 안에서는 위처럼 액세스 키 없이 <code class="language-plaintext highlighter-rouge">aws ecr get-login-password</code>가 동작한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># EC2 내에서 자격증명 없이 ECR 로그인 가능</span>
aws ecr get-login-password <span class="nt">--region</span> ap-northeast-2 | <span class="se">\</span>
  docker login <span class="nt">--username</span> AWS <span class="nt">--password-stdin</span> &lt;ECR_URI&gt;
</code></pre></div></div>

<hr />

<h2 id="6-환경변수-관리-env-파일-전략">6. 환경변수 관리: .env 파일 전략</h2>

<p>운영 서버에는 수십 개의 민감한 설정값(DB URL, 비밀번호, API 키 등)이 필요하다. 이를 <code class="language-plaintext highlighter-rouge">~/app/.env</code> 파일에 한 줄에 <code class="language-plaintext highlighter-rouge">KEY=VALUE</code> 형태로 모아 두고, Docker 실행 시 <code class="language-plaintext highlighter-rouge">--env-file</code>로 넘기면 컨테이너 안의 프로세스(예: Spring Boot)가 그 값을 환경변수로 읽을 수 있다. Spring은 <code class="language-plaintext highlighter-rouge">SPRING_PROFILES_ACTIVE</code>, <code class="language-plaintext highlighter-rouge">MYSQL_URL</code> 같은 이름을 자동으로 인식한다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># ~/app/.env</span>
<span class="nv">SPRING_PROFILES_ACTIVE</span><span class="o">=</span>prod
<span class="nv">MYSQL_URL</span><span class="o">=</span>jdbc:mysql://db.amazonaws.com:3306/chingoo_db?...
<span class="nv">MYSQL_USERNAME</span><span class="o">=</span>admin
<span class="nv">MYSQL_PASSWORD</span><span class="o">=</span>secret
<span class="nv">REDIS_HOST</span><span class="o">=</span>my-redis.cache.amazonaws.com
<span class="nv">JWT_SECRET</span><span class="o">=</span>very-long-secret-key
<span class="nv">FIREBASE_SERVICE_ACCOUNT_PATH</span><span class="o">=</span>file:/firebase-service-account.json
</code></pre></div></div>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Docker 실행 시 주입</span>
docker run <span class="nt">--env-file</span> ~/app/.env ...
</code></pre></div></div>

<p><strong>주의할 점</strong>: 이 <code class="language-plaintext highlighter-rouge">.env</code> 파일에는 비밀번호·API 키가 들어가므로 <strong>절대 Git에 올리면 안 된다.</strong> 저장소 루트의 <code class="language-plaintext highlighter-rouge">.gitignore</code>에 <code class="language-plaintext highlighter-rouge">.env</code>를 추가하고, 운영 서버용 값은 EC2에만 직접 만들어 두거나 시크릿 관리 도구를 쓰는 것이 안전하다.</p>

<hr />

<h2 id="마무리-완성된-cd-흐름">마무리: 완성된 CD 흐름</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git push main
     │
     ▼
GitHub Actions CI (ci.yml)
  - Gradle 빌드
  - 단위/통합 테스트 실행
     │
     │ 성공 시
     ▼
GitHub Actions CD (cd.yml)
  - Docker 이미지 빌드
  - AWS ECR에 push
  - EC2에 SSH 접속
       ├─ ECR에서 새 이미지 pull
       ├─ 기존 컨테이너 종료 &amp; 삭제
       ├─ 포트 해제 (fuser)
       ├─ 새 컨테이너 실행 (볼륨 마운트 포함)
       └─ 기동 상태 확인 (최대 60초)
</code></pre></div></div>

<p>이 과정을 직접 구축하면서 가장 많이 배운 것은 <strong>“왜 안 되는지”를 로그에서 찾는 능력</strong>이다. 에러 메시지를 끝까지 읽고, 그 원인을 이해하고, 해결하는 과정이 인프라 공부의 핵심이라고 생각한다.</p>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="projectdiary" /><summary type="html"><![CDATA[이전 글에서 다룬 CI/CD·Docker·ECR·EC2에 이어, 실제 파이프라인 구축 과정과 EC2 배포 시 겪은 트러블슈팅을 정리했습니다.]]></summary></entry><entry><title type="html">AWS ECS 배포 공식 문서 정리 (개발자용)</title><link href="https://nan0silver.github.io/aws/2026-02-24-ecs-deployment-developer-guide/" rel="alternate" type="text/html" title="AWS ECS 배포 공식 문서 정리 (개발자용)" /><published>2026-02-24T00:00:00+00:00</published><updated>2026-03-09T02:06:30+00:00</updated><id>https://nan0silver.github.io/aws/ecs-deployment-developer-guide</id><content type="html" xml:base="https://nan0silver.github.io/aws/2026-02-24-ecs-deployment-developer-guide/"><![CDATA[<p>Amazon ECS 서비스 배포와 관련된 AWS 공식 문서를 개발자 관점에서 정리한 문서다.<br />
기준 문서: <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-type-ecs.html">Deploy Amazon ECS services by replacing tasks</a> 및 연관 문서.</p>

<hr />

<h2 id="목차">목차</h2>

<ol>
  <li><a href="#1-배포-컨트롤러와-전략-개요">배포 컨트롤러와 전략 개요</a></li>
  <li><a href="#2-롤링-업데이트-ecs-배포-타입">롤링 업데이트 (ECS 배포 타입)</a></li>
  <li><a href="#3-배포-실패-감지-circuit-breaker-vs-cloudwatch-alarms">배포 실패 감지: Circuit Breaker vs CloudWatch Alarms</a></li>
  <li><a href="#4-컨테이너-이미지-해석-image-resolution">컨테이너 이미지 해석 (Image Resolution)</a></li>
  <li><a href="#5-bluegreen-배포">Blue/Green 배포</a></li>
  <li><a href="#6-배포-중단-및-강제-새-배포">배포 중단 및 강제 새 배포</a></li>
  <li><a href="#7-deploymentconfiguration-api-요약">DeploymentConfiguration API 요약</a></li>
  <li><a href="#8-참고-링크">참고 링크</a></li>
</ol>

<hr />

<h2 id="1-배포-컨트롤러와-전략-개요">1. 배포 컨트롤러와 전략 개요</h2>

<h3 id="11-스케줄링-전략-scheduling-strategy">1.1 스케줄링 전략 (Scheduling strategy)</h3>

<table>
  <thead>
    <tr>
      <th>전략</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>REPLICA</strong></td>
      <td>원하는 태스크 수를 클러스터에 유지. AZ 간 균형 유지. 기본값.</td>
    </tr>
    <tr>
      <td><strong>DAEMON</strong></td>
      <td>클러스터 내 활성 컨테이너 인스턴스마다 정확히 1개 태스크. <code class="language-plaintext highlighter-rouge">desiredCount</code> 불필요. <strong>Fargate 미지원.</strong></td>
    </tr>
  </tbody>
</table>

<ul>
  <li>REPLICA: 태스크 배치 전략/제약으로 배치 결정 가능. 지정 없으면 AZ에 분산.</li>
  <li>DAEMON: 로깅·모니터링·추적용 지원 컨테이너, 또는 인스턴스당 1개 앱 실행 시 사용. <code class="language-plaintext highlighter-rouge">maximumPercent</code>는 <strong>100</strong>만 허용.</li>
</ul>

<h3 id="12-배포-컨트롤러-deployment-controller">1.2 배포 컨트롤러 (Deployment controller)</h3>

<table>
  <thead>
    <tr>
      <th>컨트롤러</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>ECS</strong></td>
      <td>ECS 네이티브. 롤링/블루그린/리니어/카나리 전략 선택 가능.</td>
    </tr>
    <tr>
      <td><strong>CODE_DEPLOY</strong></td>
      <td>AWS CodeDeploy 기반 블루/그린.</td>
    </tr>
    <tr>
      <td><strong>EXTERNAL</strong></td>
      <td>서드파티 배포 컨트롤러.</td>
    </tr>
  </tbody>
</table>

<h3 id="13-ecs-컨트롤러의-배포-전략-deployment-strategy">1.3 ECS 컨트롤러의 배포 전략 (Deployment strategy)</h3>

<table>
  <thead>
    <tr>
      <th>전략</th>
      <th>요약</th>
      <th>적합한 경우</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>ROLLING</strong></td>
      <td>기존 태스크를 새 태스크로 점진 교체. <code class="language-plaintext highlighter-rouge">minimumHealthyPercent</code>, <code class="language-plaintext highlighter-rouge">maximumPercent</code>로 제어.</td>
      <td>점진적 업데이트, 리소스 절약, 롤백이 수 분 단위여도 됨, 로드밸런서 불필요, 상태 유지 앱, 비용 민감. <strong>기본값.</strong></td>
    </tr>
    <tr>
      <td><strong>BLUE_GREEN</strong></td>
      <td>Blue(현재) / Green(신규) 환경을 두고 트래픽 전환.</td>
      <td>배포 전 검증, 무중단 배포, 즉시 롤백, ALB/NLB/Service Connect 사용 시.</td>
    </tr>
    <tr>
      <td><strong>LINEAR</strong></td>
      <td>일정 시간에 걸쳐 트래픽을 균등 비율로 신규로 이동.</td>
      <td>점진적 검증, 배포 중 메트릭 모니터링, 위험 최소화. ALB/NLB/Service Connect 필요.</td>
    </tr>
    <tr>
      <td><strong>CANARY</strong></td>
      <td>소량 트래픽만 먼저 신규로 보내고, 일정 시간 후 나머지 일괄 전환.</td>
      <td>소수 사용자로 기능/성능 검증, 폭발 반경 제한. ALB/NLB/Service Connect 필요.</td>
    </tr>
  </tbody>
</table>

<ul>
  <li><strong>Task set</strong>: 한 서비스 배포에서 동일 태스크 정의로 돌아가는 태스크 묶음.</li>
  <li><strong>Rollback</strong>: 문제 감지 시 이전 버전으로 되돌리기. Circuit Breaker / CloudWatch Alarms에서 옵션으로 설정 가능.</li>
</ul>

<hr />

<h2 id="2-롤링-업데이트-ecs-배포-타입">2. 롤링 업데이트 (ECS 배포 타입)</h2>

<p>롤링 업데이트는 <strong>현재 돌아가는 태스크를 새 태스크로 교체</strong>하는 방식이다. 교체 속도와 동시 실행 수는 배포 설정 두 파라미터로 결정된다.</p>

<h3 id="21-minimumhealthypercent">2.1 minimumHealthyPercent</h3>

<ul>
  <li><strong>의미</strong>: 배포 중 <strong>RUNNING 상태로 유지해야 하는 태스크 수</strong>의 하한(desired count 대비 %).</li>
  <li><strong>계산</strong>: <code class="language-plaintext highlighter-rouge">ceil(desiredCount × minimumHealthyPercent / 100)</code>.</li>
  <li><strong>기본값</strong>: REPLICA — CLI/API/SDK <strong>100%</strong>, 콘솔 <strong>50%</strong>. DAEMON — <strong>0%</strong>.</li>
  <li><strong>역할</strong>: “이만큼은 반드시 살아 있어야 한다” → 그만큼만 내리고, 그 자리에 새 태스크를 띄울 수 있다. <strong>추가 클러스터 용량 없이 배포</strong>하려면 100% 미만으로 낮춘다.</li>
</ul>

<p><strong>예시</strong></p>

<ul>
  <li>desiredCount=4, minimumHealthyPercent=50% → 최소 2개 RUNNING. 스케줄러는 기존 2개를 먼저 중지한 뒤 새 2개를 띄울 수 있다.</li>
  <li>desiredCount=2, minimumHealthyPercent=75% → 최소 2개(ceil(1.5)=2). 기존 태스크를 하나도 중지하지 못하므로, <strong>새 태스크를 띄울 여유가 있으면</strong> 먼저 새 것을 띄우고 나서 기존 것을 중지한다.</li>
</ul>

<h3 id="22-maximumpercent">2.2 maximumPercent</h3>

<ul>
  <li><strong>의미</strong>: 배포 중 <strong>RUNNING + PENDING</strong> 태스크 수의 상한(desired count 대비 %).</li>
  <li><strong>계산</strong>: <code class="language-plaintext highlighter-rouge">floor(desiredCount × maximumPercent / 100)</code>.</li>
  <li><strong>기본값</strong>: REPLICA 스케줄러 <strong>200%</strong>.</li>
  <li><strong>역할</strong>: “이만큼까지는 동시에 돌려도 된다” → 배치 크기(batch size) 결정. 200%면 desiredCount=4일 때 최대 8개까지 동시에 있을 수 있어, <strong>새 4개를 먼저 띄운 뒤 기존 4개를 중지</strong>하는 식으로 무중단에 가깝게 배포할 수 있다.</li>
</ul>

<p><strong>예시</strong></p>

<ul>
  <li>desiredCount=4, maximumPercent=200% → 최대 8개. 새 4개 기동 후 기존 4개 중지 가능.</li>
  <li>desiredCount=3, maximumPercent=125% → floor(3.75)=3. 새 태스크를 하나도 더 띄우지 못하므로, 기존 것을 하나 중지한 뒤 새 것을 하나 띄우는 식으로만 진행된다.</li>
</ul>

<h3 id="23-중요-제약">2.3 중요 제약</h3>

<ul>
  <li><strong>최소 1개는 중지/시작 가능해야 함</strong>:<br />
<code class="language-plaintext highlighter-rouge">minimumHealthyPercent</code>/<code class="language-plaintext highlighter-rouge">maximumPercent</code> 조합 때문에 “하나도 중지할 수 없음” 또는 “하나도 시작할 수 없음”이 되면 배포가 <strong>진행되지 않고</strong> 서비스 이벤트 메시지가 발생한다.<br />
→ <a href="https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-event-messages-list.html#service-event-messages-7">service (service-name) was unable to stop or start tasks during a deployment…</a> 참고.</li>
</ul>

<h3 id="24-unhealthy-태스크-교체">2.4 Unhealthy 태스크 교체</h3>

<ul>
  <li>배포 중 태스크가 unhealthy가 되면, <code class="language-plaintext highlighter-rouge">minimumHealthyPercent</code>를 지키기 위해 <strong>같은 서비스 리비전</strong>으로 대체 태스크를 띄운다.</li>
  <li><code class="language-plaintext highlighter-rouge">maximumPercent</code> 여유가 있으면 <strong>먼저 대체 태스크를 띄우고</strong> 그 다음 unhealthy 태스크를 중지한다.<br />
여유가 없으면 <strong>unhealthy를 한 개씩 중지</strong>해 용량을 확보한 뒤 대체 태스크를 띄운다.</li>
</ul>

<hr />

<h2 id="3-배포-실패-감지-circuit-breaker-vs-cloudwatch-alarms">3. 배포 실패 감지: Circuit Breaker vs CloudWatch Alarms</h2>

<p>롤링 업데이트(<code class="language-plaintext highlighter-rouge">ECS</code> 배포 타입)에서 <strong>배포 실패</strong>를 판단하는 방법은 두 가지다. 둘 다 사용하면 <strong>둘 중 하나만 만족해도</strong> 배포가 실패로 처리된다.</p>

<h3 id="31-circuit-breaker-배포-서킷-브레이커">3.1 Circuit Breaker (배포 서킷 브레이커)</h3>

<ul>
  <li><strong>용도</strong>: “태스크가 기동되지 않을 때” 또는 “헬스체크가 계속 실패할 때” 배포를 실패로 간주하고, 옵션으로 <strong>자동 롤백</strong>.</li>
  <li><strong>지원</strong>: <strong>롤링 업데이트(ECS)</strong> 전용.</li>
</ul>

<p><strong>동작 단계</strong></p>

<ol>
  <li><strong>Stage 1</strong>
    <ul>
      <li>태스크가 <strong>RUNNING</strong>으로 전이되는지 감시.</li>
      <li><strong>성공</strong>: RUNNING으로 전이된 태스크가 1개 이상 있으면 실패 조건은 건너뛰고 Stage 2로.</li>
      <li><strong>실패</strong>: RUNNING으로 가지 못한 태스크가 <strong>연속으로</strong> threshold개 있으면 배포를 <strong>FAILED</strong>로 전환.</li>
    </ul>
  </li>
  <li><strong>Stage 2</strong>
    <ul>
      <li>RUNNING인 태스크가 1개 이상 있을 때 진입.</li>
      <li>ELB, AWS Cloud Map, <strong>컨테이너 헬스체크</strong>로 상태 검사.</li>
      <li><strong>성공</strong>: 1개 이상 태스크가 헬스체크 통과.</li>
      <li><strong>실패</strong>: 헬스체크 실패로 교체된 태스크 수가 <strong>threshold</strong>에 도달하면 배포 <strong>FAILED</strong>.</li>
    </ul>
  </li>
</ol>

<p><strong>Failure threshold 계산</strong></p>

<ul>
  <li>공식: <code class="language-plaintext highlighter-rouge">min(200, max(3, ceil(0.5 × desiredCount)))</code>.
    <ul>
      <li>desiredCount=1 → 3</li>
      <li>desiredCount=25 → 13</li>
      <li>desiredCount=400 → 200</li>
      <li>desiredCount=800 → 200</li>
    </ul>
  </li>
  <li>이 값은 <strong>변경 불가</strong>.</li>
</ul>

<p><strong>롤백</strong></p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">rollback=true</code>로 두면, 실패한 배포를 중단하고 <strong>가장 최근 COMPLETED 상태 배포</strong>로 롤백한다.</li>
  <li>롤백 대상이 COMPLETED → IN_PROGRESS로 바뀌므로, 그 배포가 다시 COMPLETED가 되기 전에는 그 배포로의 추가 롤백은 불가.</li>
  <li>COMPLETED인 배포가 하나도 없으면 서킷 브레이커는 새 태스크를 띄우지 않고 배포는 정지된 상태로 둔다.</li>
</ul>

<p><strong>CLI 예시 (Circuit Breaker + 롤백)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>aws ecs create-service <span class="se">\</span>
  <span class="nt">--service-name</span> MyService <span class="se">\</span>
  <span class="nt">--deployment-controller</span> <span class="nb">type</span><span class="o">=</span>ECS <span class="se">\</span>
  <span class="nt">--desired-count</span> 3 <span class="se">\</span>
  <span class="nt">--deployment-configuration</span> <span class="s2">"deploymentCircuitBreaker={enable=true,rollback=true}"</span> <span class="se">\</span>
  <span class="nt">--task-definition</span> sample-fargate:1 <span class="se">\</span>
  <span class="nt">--launch-type</span> FARGATE <span class="se">\</span>
  ...
</code></pre></div></div>

<h3 id="32-cloudwatch-alarms">3.2 CloudWatch Alarms</h3>

<ul>
  <li><strong>용도</strong>: <strong>앱 메트릭</strong> 기준으로 배포 실패 판단(예: 5xx 증가, CPU/메모리 스파이크).</li>
  <li><strong>지원</strong>: <strong>롤링 업데이트(ECS)</strong> 전용.</li>
</ul>

<p><strong>동작</strong></p>

<ul>
  <li>지정한 알람이 <strong>ALARM</strong> 상태가 되면 해당 배포를 <strong>FAILED</strong>로 전환.</li>
  <li><code class="language-plaintext highlighter-rouge">rollback=true</code>면 이전 완료된 배포로 롤백.</li>
</ul>

<p><strong>Bake time</strong></p>

<ul>
  <li>트래픽이 신규 리비전으로 옮겨진 <strong>이후</strong>, 두 리비전이 동시에 돌아가는 기간.</li>
  <li>이 기간이 지나야 배포가 <strong>COMPLETED</strong>로 바뀌고, 그동안 알람을 모니터링한다.</li>
  <li>기본 bake time은 5분 미만. 알람으로 실패 감지 시 bake time을 조정할 수 있으며, ECS 기본값으로 되돌리려면 수동으로 다시 설정해야 한다.</li>
</ul>

<p><strong>권장 알람 예</strong></p>

<ul>
  <li>ALB 사용 시: <code class="language-plaintext highlighter-rouge">HTTPCode_ELB_5XX_Count</code>, <code class="language-plaintext highlighter-rouge">HTTPCode_ELB_4XX_Count</code></li>
  <li>기존 앱 메트릭: <code class="language-plaintext highlighter-rouge">CPUUtilization</code>, <code class="language-plaintext highlighter-rouge">MemoryUtilization</code> (클러스터/서비스)</li>
  <li>SQS 사용 시: <code class="language-plaintext highlighter-rouge">ApproximateNumberOfMessagesNotVisible</code></li>
</ul>

<p><strong>주의</strong></p>

<ul>
  <li>배포 <strong>시작 시점</strong>에 이미 알람이 ALARM이면, 그 배포에서는 알람 감지를 <strong>하지 않는다</strong> (기존 실패를 고치는 배포를 막지 않기 위함).</li>
  <li>ECS가 <code class="language-plaintext highlighter-rouge">DescribeAlarms</code>를 호출하므로 CloudWatch 할당량에 포함된다. 할당량 초과 시 알람 폴링이 지연·스로틀되어 롤백이 늦어질 수 있다.</li>
</ul>

<p><strong>CLI 예시 (Alarms + 롤백)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>aws ecs create-service <span class="se">\</span>
  ... <span class="se">\</span>
  <span class="nt">--deployment-configuration</span> <span class="s2">"alarms={alarmNames=[alarm1Name,alarm2Name],enable=true,rollback=true}"</span> <span class="se">\</span>
  ...
</code></pre></div></div>

<h3 id="33-eventbridge">3.3 EventBridge</h3>

<ul>
  <li>배포 상태 변경 시 EventBridge로 이벤트 전송.</li>
  <li><code class="language-plaintext highlighter-rouge">SERVICE_DEPLOYMENT_FAILED</code> 등으로 규칙 만들어 모니터링·수동 대응 권장.</li>
</ul>

<hr />

<h2 id="4-컨테이너-이미지-해석-image-resolution">4. 컨테이너 이미지 해석 (Image Resolution)</h2>

<ul>
  <li>태스크 정의에는 보통 <strong>이미지 태그</strong>(예: <code class="language-plaintext highlighter-rouge">myrepo:latest</code>)를 쓰는데, ECS는 기본적으로 이 태그를 <strong>이미지 digest</strong>로 해석(고정)한다.</li>
  <li><strong>한 번 digest가 정해지면</strong>, 그 서비스의 모든 태스크와 이후 배포는 <strong>같은 digest</strong>를 사용해 <strong>버전 일관성</strong>을 유지한다.</li>
</ul>

<p><strong>동작</strong></p>

<ul>
  <li>서비스 태스크 1개: 그 태스크로 digest 확정.</li>
  <li>태스크 여러 개: 배포 시 <strong>스케줄러가 맨 처음 띄운 태스크</strong>로 digest 확정.</li>
  <li><strong>3번 연속</strong> digest 확정에 실패하면:
    <ul>
      <li>Circuit Breaker 사용 시 → 배포 실패·롤백.</li>
      <li>미사용 시 → digest 없이 배포 계속 진행할 수 있음.</li>
    </ul>
  </li>
</ul>

<p><strong>제약·참고</strong></p>

<ul>
  <li>ECS Agent: 1.31.0 미만 — digest 미지원. 1.31.0~1.69.0 — <strong>ECR 이미지만</strong> digest 해석. 1.70.0+ — 모든 레지스트리.</li>
  <li>Fargate Linux: 플랫폼 버전 1.3.0 이상. Windows: 1.0.0 이상.</li>
  <li>Sidecar(GuardDuty, Service Connect 프록시 등)는 digest를 캡처하지 않음.</li>
  <li>태스크가 많은 서비스에서 지연을 줄이려면: EC2 인스턴스에서는 Agent 1.83.0 이상 사용, 또는 태스크 정의에 <strong>이미지 digest를 직접 지정</strong>.</li>
  <li>desiredCount=0인 서비스는 digest를 확정할 수 없음. 나중에 desiredCount&gt;0으로 배포해야 확정됨.</li>
  <li><strong>같은 태그의 새 이미지</strong>를 배포하려면 <strong>force new deployment</strong>를 해야 새로 pull한 이미지 digest가 반영된다.</li>
</ul>

<p><strong>버전 일관성</strong></p>

<ul>
  <li>컨테이너 정의에서 <code class="language-plaintext highlighter-rouge">versionConsistency</code>로 컨테이너별 동작 설정 가능.</li>
  <li>EC2 capacity provider에서 초기 배포 시 용량 부족으로 태스크 기동이 실패하면, 버전 일관성이 깨질 수 있다. 용량이 제한적일 때는 <code class="language-plaintext highlighter-rouge">versionConsistency: "enabled"</code>를 명시해 두는 것이 좋다.</li>
</ul>

<hr />

<h2 id="5-bluegreen-배포">5. Blue/Green 배포</h2>

<h3 id="51-요약">5.1 요약</h3>

<ul>
  <li><strong>Blue</strong>: 현재 프로덕션. <strong>Green</strong>: 새 리비전.</li>
  <li>Green을 띄우고 검증한 뒤, 트래픽을 Blue → Green으로 전환.</li>
  <li><strong>ALB, NLB 또는 Service Connect</strong> 필요.</li>
</ul>

<h3 id="52-6단계-워크플로">5.2 6단계 워크플로</h3>

<table>
  <thead>
    <tr>
      <th>단계</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Preparation</strong></td>
      <td>Blue 옆에 Green 환경·타겟 그룹 준비.</td>
    </tr>
    <tr>
      <td><strong>Deployment</strong></td>
      <td>Green에 새 서비스 리비전 배포. Blue는 그대로 트래픽 처리.</td>
    </tr>
    <tr>
      <td><strong>Testing</strong></td>
      <td>ALB로 테스트 트래픽만 Green으로 보내 검증.</td>
    </tr>
    <tr>
      <td><strong>Traffic shifting</strong></td>
      <td>프로덕션 트래픽을 Blue → Green으로 전환. ECS 블루/그린은 <strong>한 번에 100%</strong> 전환.</td>
    </tr>
    <tr>
      <td><strong>Monitoring</strong></td>
      <td>Bake time 동안 헬스·알람 모니터링. 문제 시 롤백.</td>
    </tr>
    <tr>
      <td><strong>Completion</strong></td>
      <td>Blue 종료 또는 롤백 대비해 유지.</td>
    </tr>
  </tbody>
</table>

<h3 id="53-라이프사이클-스테이지">5.3 라이프사이클 스테이지</h3>

<p>각 스테이지는 최대 <strong>24시간</strong>. 24시간 초과 시 타임아웃 후 실패·롤백. CloudFormation은 <strong>전체 배포 36시간</strong> 제한.</p>

<table>
  <thead>
    <tr>
      <th>스테이지</th>
      <th>설명</th>
      <th>Lifecycle hook 사용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>RECONCILE_SERVICE</td>
      <td>ACTIVE 리비전이 2개 이상일 때만 발생.</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>PRE_SCALE_UP</td>
      <td>Green 미시작, Blue 100%.</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>SCALE_UP</td>
      <td>Green 0→100% 스케일 업, 트래픽 없음.</td>
      <td>No</td>
    </tr>
    <tr>
      <td>POST_SCALE_UP</td>
      <td>Green 기동됨, Blue 100%.</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>TEST_TRAFFIC_SHIFT</td>
      <td>Blue 100% 프로덕션, Green 0→100% 테스트 트래픽.</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>POST_TEST_TRAFFIC_SHIFT</td>
      <td>Green 100% 테스트 트래픽.</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>PRODUCTION_TRAFFIC_SHIFT</td>
      <td>프로덕션 트래픽이 Green으로 이동 중.</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>POST_PRODUCTION_TRAFFIC_SHIFT</td>
      <td>프로덕션 트래픽 전환 완료.</td>
      <td>Yes</td>
    </tr>
    <tr>
      <td>BAKE_TIME</td>
      <td>Blue·Green 동시 실행 기간.</td>
      <td>No</td>
    </tr>
    <tr>
      <td>CLEAN_UP</td>
      <td>Blue 0개, Green이 프로덕션.</td>
      <td>No</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="6-배포-중단-및-강제-새-배포">6. 배포 중단 및 강제 새 배포</h2>

<h3 id="61-배포-중단-stop-deployment">6.1 배포 중단 (Stop deployment)</h3>

<ul>
  <li>Circuit Breaker나 CloudWatch Alarms로 자동 감지되지 않은 <strong>실패 배포</strong>를 수동으로 멈추고 롤백할 때 사용.</li>
  <li><strong>Stop type</strong>: <strong>ROLLBACK</strong> — 이전 서비스 리비전으로 롤백. (서비스 생성 시 rollback 옵션을 안 켜도 수동 롤백 가능.)</li>
</ul>

<p><strong>대상 상태</strong>: PENDING, IN_PROGRESS, STOP_REQUESTED, ROLLBACK_REQUESTED, ROLLBACK_IN_PROGRESS.</p>

<p><strong>CLI</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1) 배포 ARN 조회</span>
aws ecs list-service-deployments <span class="nt">--cluster</span> cluster-name <span class="nt">--service</span> service-name

<span class="c"># 2) 해당 배포 롤백</span>
aws ecs stop-service-deployment <span class="se">\</span>
  <span class="nt">--service-deployment-arn</span> &lt;serviceDeploymentArn&gt; <span class="se">\</span>
  <span class="nt">--stop-type</span> ROLLBACK
</code></pre></div></div>

<h3 id="62-강제-새-배포-force-new-deployment">6.2 강제 새 배포 (Force new deployment)</h3>

<ul>
  <li>태스크 정의는 그대로 두고 <strong>같은 태그의 최신 이미지</strong>를 다시 pull 해서 배포하고 싶을 때(예: <code class="language-plaintext highlighter-rouge">latest</code> 태그).</li>
  <li><code class="language-plaintext highlighter-rouge">UpdateService</code> 시 <code class="language-plaintext highlighter-rouge">forceNewDeployment: true</code> 사용.<br />
새 태스크가 기동될 때 현재 이미지(태그)를 다시 pull하고, 필요 시 <strong>이미지 digest도 다시 확정</strong>된다.</li>
</ul>

<hr />

<h2 id="7-deploymentconfiguration-api-요약">7. DeploymentConfiguration API 요약</h2>

<table>
  <thead>
    <tr>
      <th>필드</th>
      <th>타입</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>minimumHealthyPercent</strong></td>
      <td>Integer</td>
      <td>롤링 업데이트 시 RUNNING 유지 하한(%). REPLICA 기본 100%(CLI/API), 콘솔 50%. DAEMON 기본 0%.</td>
    </tr>
    <tr>
      <td><strong>maximumPercent</strong></td>
      <td>Integer</td>
      <td>롤링 업데이트 시 RUNNING+PENDING 상한(%). REPLICA 기본 200%. DAEMON은 100 고정.</td>
    </tr>
    <tr>
      <td><strong>deploymentCircuitBreaker</strong></td>
      <td>Object</td>
      <td>enable, rollback. 롤링(ECS) 전용.</td>
    </tr>
    <tr>
      <td><strong>alarms</strong></td>
      <td>DeploymentAlarms</td>
      <td>alarmNames, enable, rollback. 롤링(ECS) 전용.</td>
    </tr>
    <tr>
      <td><strong>strategy</strong></td>
      <td>String</td>
      <td>ROLLING | BLUE_GREEN | LINEAR | CANARY.</td>
    </tr>
    <tr>
      <td><strong>bakeTimeInMinutes</strong></td>
      <td>Integer</td>
      <td>BLUE_GREEN 시 트래픽 전환 후 blue·green 동시 실행 시간(분).</td>
    </tr>
    <tr>
      <td><strong>canaryConfiguration</strong></td>
      <td>Object</td>
      <td>CANARY 전략일 때만.</td>
    </tr>
    <tr>
      <td><strong>linearConfiguration</strong></td>
      <td>Object</td>
      <td>LINEAR 전략일 때만.</td>
    </tr>
    <tr>
      <td><strong>lifecycleHooks</strong></td>
      <td>Array</td>
      <td>배포 라이프사이클 특정 단계에서 실행할 훅.</td>
    </tr>
  </tbody>
</table>

<p>CODE_DEPLOY/EXTERNAL + EC2 launch type에서는 <code class="language-plaintext highlighter-rouge">minimumHealthyPercent</code>/<code class="language-plaintext highlighter-rouge">maximumPercent</code>를 사용자 지정할 수 없고, DRAINING 시 동작에만 쓰인다. Fargate + CODE_DEPLOY/EXTERNAL에서는 이 값들이 배포 제어에 쓰이지 않지만 Describe 시에는 반환된다.</p>

<hr />

<h2 id="8-참고-링크">8. 참고 링크</h2>

<table>
  <thead>
    <tr>
      <th>문서</th>
      <th>URL</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Deploy by replacing tasks (롤링 업데이트)</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-type-ecs.html</td>
    </tr>
    <tr>
      <td>Deployment circuit breaker</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-circuit-breaker.html</td>
    </tr>
    <tr>
      <td>CloudWatch alarms for deployment failure</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-alarm-failure.html</td>
    </tr>
    <tr>
      <td>Service deployment controllers and strategies</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/ecs_service-options.html</td>
    </tr>
    <tr>
      <td>Blue/green deployment workflow</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/blue-green-deployment-how-it-works.html</td>
    </tr>
    <tr>
      <td>Stopping service deployments</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/stop-service-deployment.html</td>
    </tr>
    <tr>
      <td>DeploymentConfiguration (API)</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/APIReference/API_DeploymentConfiguration.html</td>
    </tr>
    <tr>
      <td>Service event messages (e.g. deployment stuck)</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-event-messages-list.html</td>
    </tr>
    <tr>
      <td>versionConsistency (task definition)</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task_definition_parameters.html#ContainerDefinition-versionconsistency</td>
    </tr>
    <tr>
      <td>forceNewDeployment (UpdateService)</td>
      <td>https://docs.aws.amazon.com/AmazonECS/latest/APIReference/API_UpdateService.html</td>
    </tr>
  </tbody>
</table>

<hr />

<p><em>이 문서는 AWS 공식 문서를 바탕으로 정리했으며, 실제 구현 시 최신 Developer Guide와 API Reference를 반드시 확인하는 것이 좋다.</em></p>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="aws" /><summary type="html"><![CDATA[ECS 서비스 배포(롤링 업데이트, Blue/Green, Circuit Breaker, CloudWatch Alarms, 이미지 해석 등) 관련 AWS 공식 문서를 개발자 관점에서 정리했다.]]></summary></entry><entry><title type="html">[친구하자] Android 앱을 만들며 배운 CI/CD와 AWS EC2·ECS 이야기</title><link href="https://nan0silver.github.io/projectdiary/2026-02-24-diary/" rel="alternate" type="text/html" title="[친구하자] Android 앱을 만들며 배운 CI/CD와 AWS EC2·ECS 이야기" /><published>2026-02-24T00:00:00+00:00</published><updated>2026-03-09T02:06:30+00:00</updated><id>https://nan0silver.github.io/projectdiary/diary</id><content type="html" xml:base="https://nan0silver.github.io/projectdiary/2026-02-24-diary/"><![CDATA[<ul>
  <li><a href="#시작하기-전에">시작하기 전에</a></li>
  <li><a href="#1-cicd란-무엇인가요">1. CI/CD란 무엇인가요?</a></li>
  <li><a href="#2-docker가-왜-필요한가요">2. Docker가 왜 필요한가요?</a></li>
  <li><a href="#3-aws-ec2란-무엇인가요">3. AWS EC2란 무엇인가요?</a></li>
  <li><a href="#4-aws-ecs란-무엇인가요">4. AWS ECS란 무엇인가요?</a></li>
  <li><a href="#5-ecr이란-무엇인가요">5. ECR이란 무엇인가요?</a></li>
  <li><a href="#6-지금-내-프로젝트의-전체-흐름">6. 지금 내 프로젝트의 전체 흐름</a></li>
  <li><a href="#7-정리">7. 정리</a></li>
</ul>

<hr />

<h2 id="시작하기-전에">시작하기 전에</h2>

<p>현재 Android 앱 “친구하자”의 백엔드를 Spring Boot로 개발하고 있다. 소규모로 시작해서 테스트 유저 10명 이하로 운영 중이며, 곧 Play Store 출시를 앞두고 있다.</p>

<p>출시를 준비하면서 지금까지 당연하게 해왔던 배포 방식을 돌아보게 됐다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-i</span> <span class="s2">"my-key.pem"</span> ec2-user@xxx.xxx.xxx.xxx
<span class="c"># 접속 후 직접 명령어 실행...</span>
</code></pre></div></div>

<p>매번 터미널을 열고, SSH로 접속하고, 직접 명령어를 치는 방식이었다. 테스트 유저가 적을 때는 괜찮았지만 출시가 다가오니 이런 생각이 들었다.</p>

<blockquote>
  <p>“코드 push하고 나서 배포까지 내가 직접 해야 하나? 이거 자동화할 수 없나?”</p>
</blockquote>

<p>그래서 CI/CD를 도입하게 됐고, 그 과정에서 배운 내용을 정리해 본다.</p>

<p><strong>이 글에서 다루는 것</strong><br />
CI/CD만 이야기하면 “push하면 자동으로 테스트하고 배포한다”로 끝나는데, <strong>실제로 그 배포가 돌아가려면</strong> 몇 가지가 필요하다. (1) <strong>뭘</strong> 배포할지 정하는 단위 → Docker 이미지, (2) 그 이미지를 <strong>어디에</strong> 보관할지 → ECR, (3) 그 이미지를 <strong>어디서</strong> 실행할지 → EC2, (4) 나중에 트래픽이 늘면 <strong>어떻게</strong> 관리·확장할지 → ECS. 아래에서는 이 순서대로 “왜 이걸 쓰는지” 맥락을 붙여서 정리한다.</p>

<hr />

<h2 id="1-cicd란-무엇인가요">1. CI/CD란 무엇인가요?</h2>

<h3 id="수동-배포의-문제점">수동 배포의 문제점</h3>

<p>내가 처음에 하던 배포 방식:</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">./gradlew build</code> 로컬에서 빌드</li>
  <li><code class="language-plaintext highlighter-rouge">scp</code>로 JAR 파일 EC2에 전송</li>
  <li>EC2 SSH 접속</li>
  <li>기존 프로세스 종료</li>
  <li><code class="language-plaintext highlighter-rouge">java -jar app.jar &amp;</code> 실행</li>
</ol>

<p>매번 이걸 반복해야 한다. 실수할 가능성도 높고, 시간도 낭비다.</p>

<h3 id="cicd-개념">CI/CD 개념</h3>

<ul>
  <li><strong>CI (Continuous Integration, 지속적 통합)</strong>: 코드를 push할 때마다 자동으로
빌드하고 테스트를 실행</li>
  <li><strong>CD (Continuous Deployment, 지속적 배포)</strong>: CI가 성공하면 자동으로 서버에 배포</li>
</ul>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자 코드 push
       ↓
CI: 빌드 + 테스트 실행 (자동)
       ↓ (성공 시)
CD: 서버에 자동 배포 (자동)
       ↓
사용자는 새 버전 접속
</code></pre></div></div>

<p>이 프로젝트에서는 <strong>GitHub Actions</strong>를 CI/CD 도구로 사용했다.</p>

<h3 id="1️⃣-ci-continuous-integration--지속적-통합">1️⃣ CI (Continuous Integration) — 지속적 통합</h3>

<blockquote>
  <p>“코드를 push하면 테스트까지 자동으로”</p>
</blockquote>

<p>코드를 GitHub에 push할 때마다 자동으로 <strong>빌드 + 테스트</strong>를 돌려서, 작성한 코드가 기존 코드를 망가뜨리지 않았는지 바로 확인한다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>개발자가 push
    ↓
GitHub Actions가 자동으로 실행
    ↓
빌드 → 테스트
    ↓
통과 ✅  or  실패 ❌ (즉시 알림)
</code></pre></div></div>

<p>이 프로젝트에서는 Redis를 활용한 매칭 큐, 이벤트 리스너 등 여러 컴포넌트를 테스트해야 했는데, 매번 로컬에서 직접 테스트를 돌리다 보니 빠뜨리는 경우가 생겼다. CI를 붙이고 나서는 push할 때마다 자동으로 전체 테스트가 돌아가니 훨씬 안심이 됐다.</p>

<p><strong>핵심 가치</strong>: 문제를 빠르게 발견한다. 나중에 발견할수록 고치기 어렵다.</p>

<hr />

<h3 id="2️⃣-cd-continuous-deployment--지속적-배포">2️⃣ CD (Continuous Deployment) — 지속적 배포</h3>

<blockquote>
  <p>“테스트까지 통과하면 서버 배포도 자동으로”</p>
</blockquote>

<p>CI가 성공하면 자동으로 <strong>서버에 새 버전을 배포</strong>한다. 매번 SSH로 직접 접속해서 배포하던 방식이 완전히 사라진다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CI 성공 ✅
    ↓
Docker 이미지 빌드
    ↓
ECR(이미지 저장소)에 업로드
    ↓
EC2 서버에 새 버전 자동 배포
    ↓
개발자는 push만 했을 뿐인데 배포 완료 🎉
</code></pre></div></div>

<p><strong>도입 전 vs 도입 후</strong></p>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>도입 전</th>
      <th>도입 후</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>배포 방법</td>
      <td>SSH 접속 → 직접 명령어 실행</td>
      <td>git push 한 번</td>
    </tr>
    <tr>
      <td>테스트</td>
      <td>까먹으면 안 함</td>
      <td>항상 자동 실행</td>
    </tr>
    <tr>
      <td>실수 가능성</td>
      <td>있음 (손으로 하니까)</td>
      <td>없음</td>
    </tr>
    <tr>
      <td>배포 시간</td>
      <td>10~15분</td>
      <td>기다리면 자동 완료</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="2-docker가-왜-필요한가요">2. Docker가 왜 필요한가요?</h2>

<p>CD는 “테스트 통과한 코드를 <strong>서버에 그대로</strong> 올리는 것”이다. 이때 “그대로”를 보장하려면 <strong>배포 단위</strong>가 정해져 있어야 한다. 로컬에서 돌리던 걸 그냥 서버에 복사하면 환경이 달라서 실패하는 경우가 많다. 그래서 CI/CD 흐름에서는 <strong>Docker 이미지</strong>를 그 단위로 쓰고, 그 이미지를 서버에서 실행하는 방식이 일반적이다. 즉 Docker는 “CD가 <strong>뭘</strong> 배포할지 정의하는 방식”이라서 빠질 수 없다.</p>

<p>개발자라면 한 번쯤 들어봤을 말이다. 로컬에서는 잘 되던 코드가 서버에 올리면 안 되는 이유는
뭘까?</p>

<ul>
  <li>로컬: Java 17, MySQL 8.0, Ubuntu 22.04</li>
  <li>서버: Java 11, MySQL 5.7, Amazon Linux 2</li>
</ul>

<p><strong>환경이 다르기 때문이다.</strong></p>

<p>Docker는 이 문제를 해결한다. 애플리케이션과 그것이 실행되는 환경(런타임, 라이브러리, 설정)
을 <strong>하나의 박스(컨테이너)</strong>에 묶어서 어디서든 똑같이 실행되게 만든다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dockerfile (조리법)
    ↓  빌드
Docker 이미지 (완성된 통조림)
    ↓  실행
컨테이너 (실제로 접시에 담아 먹는 상태)
</code></pre></div></div>

<p><img src="https://github.com/nan0silver/nan0silver.github.io/blob/main/assets/img/blog/2026-02-24-pic2.png?raw=true" alt="예시 이미지1" /></p>

<p>어디서 실행해도 똑같이 동작하기 때문에 환경 차이로 인한 문제가 사라진다.</p>

<h3 id="실제-dockerfile-예시-spring-boot">실제 Dockerfile 예시 (Spring Boot)</h3>

<div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">FROM</span><span class="s"> eclipse-temurin:17-jre-alpine</span>

<span class="k">WORKDIR</span><span class="s"> /app</span>

<span class="k">COPY</span><span class="s"> build/libs/app.jar app.jar</span>

<span class="k">ENTRYPOINT</span><span class="s"> ["java", "-jar", "app.jar"]</span>
</code></pre></div></div>

<p>읽으면 직관적이다. “Java 17 JRE가 설치된 Alpine Linux 위에서, <code class="language-plaintext highlighter-rouge">app.jar</code>를
실행해라.”</p>

<hr />

<h2 id="3-aws-ec2란-무엇인가요">3. AWS EC2란 무엇인가요?</h2>

<p>CD가 “Docker 이미지를 서버에 배포한다”고 했을 때, <strong>그 서버가 어디인지</strong>가 정해져 있어야 한다. 친구하자 프로젝트에서는 그 역할을 <strong>EC2 한 대</strong>가 맡고 있다. EC2 위에서 빌드된 Docker 이미지를 받아서 컨테이너로 실행하면, 사용자 요청을 처리하는 백엔드 서버가 되는 식이다.</p>

<p><strong>EC2 (Elastic Compute Cloud)</strong>는 AWS에서 빌려 쓰는 가상 컴퓨터이다.</p>

<p>Spring Boot 백엔드 서버를 EC2 위에서 Docker 컨테이너로 돌리고 있다. 처음에는 아래처럼 직접 SSH로 접속해서 배포했다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 매번 이걸 직접 했었음</span>
ssh <span class="nt">-i</span> <span class="s2">"my-key.pem"</span> ec2-user@서버IP

docker stop my-app
docker <span class="nb">rm </span>my-app
docker pull 새이미지
docker run <span class="nt">-d</span> <span class="nt">--name</span> my-app <span class="nt">-p</span> 8080:8080 새이미지
</code></pre></div></div>

<p>한두 번은 괜찮지만, 기능 추가할 때마다 반복하다 보면 꽤 번거롭다. 게다가 <code class="language-plaintext highlighter-rouge">docker stop</code> 하고 <code class="language-plaintext highlighter-rouge">docker run</code> 하는 사이에 서비스가 잠깐 끊기는 <strong>순단</strong>이 발생한다. 테스트 유저 10명일 때는 몰랐는데, Play Store 출시 후 실 사용자가 생기면 문제가 될 수 있겠다 싶었다.</p>

<hr />

<h2 id="4-aws-ecs란-무엇인가요">4. AWS ECS란 무엇인가요?</h2>

<p>지금은 EC2 한 대에서 컨테이너를 직접 올리고 내리는 방식으로 CD가 동작한다. 이 방식은 단순하지만, 배포 시 잠깐 서비스가 끊기는 <strong>순단</strong>이 있고, 사용자가 몰렸을 때 서버를 늘리는 것도 수동이다. <strong>트래픽이 늘어나면</strong> “배포할 때도 끊기지 않게(무중단 배포)” “부하가 크면 컨테이너를 자동으로 늘리게(오토스케일링)”가 필요해진다. 그때 CD의 <strong>배포 대상</strong>을 EC2 직접 제어에서 <strong>ECS</strong>로 바꾸면, 컨테이너 실행·롤링 업데이트·스케일링을 AWS가 관리해 준다.</p>

<p><strong>ECS (Elastic Container Service)</strong>는 Docker 컨테이너를 AWS가 대신 관리해 주는 서비스이다.</p>

<p>EC2가 “컴퓨터를 빌려 드릴게요”라면, ECS는 “컨테이너 실행·관리까지 다 해 드릴게요”이다.</p>

<h3 id="핵심-개념-3가지">핵심 개념 3가지</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>클러스터 (Cluster)
└── 컨테이너들이 실행되는 공간 전체 (운동장)

서비스 (Service)
└── "이 컨테이너를 항상 N개 유지해줘" (운동장 관리자)

태스크 정의 (Task Definition)
└── 컨테이너 실행 설정 명세서
    (어떤 이미지, CPU/메모리 얼마, 포트는 몇 번 등)
</code></pre></div></div>

<h3 id="ecs의-핵심-장점-롤링-업데이트-무중단-배포">ECS의 핵심 장점: 롤링 업데이트 (무중단 배포)</h3>

<p>EC2 직접 배포는 이렇게 된다:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>기존 컨테이너 중지 → 💀 서비스 잠깐 중단 → 새 컨테이너 시작
</code></pre></div></div>

<p>ECS 롤링 업데이트는 이렇게 된다:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>새 컨테이너 먼저 시작 &amp; 정상 확인
         ↓
기존 컨테이너 중지  ← 사용자는 아무것도 못 느낌 ✅
</code></pre></div></div>

<hr />

<h2 id="5-ecr이란-무엇인가요">5. ECR이란 무엇인가요?</h2>

<p>CD 흐름을 다시 보면, “CI 통과 → Docker 이미지 빌드 → <strong>어딘가에 보관</strong> → EC2(또는 ECS)가 그걸 가져가서 실행”이다. 이미지를 <strong>보관하는 곳</strong>이 없으면 EC2나 ECS가 “어떤 이미지로 컨테이너를 띄울지” 가져올 수 없다. 그 보관소 역할을 AWS에서는 <strong>ECR</strong>이 한다. GitHub Actions에서 이미지를 빌드한 뒤 ECR에 푸시하고, EC2(또는 ECS)는 ECR에서 이미지를 pull 해서 컨테이너를 띄우는 식으로 연결된다.</p>

<p><strong>ECR (Elastic Container Registry)</strong>은 Docker 이미지를 저장하는 AWS 전용 창고이다.</p>

<p><img src="https://github.com/nan0silver/nan0silver.github.io/blob/main/assets/img/blog/2026-02-24-pic1.png?raw=true" alt="예시 이미지1" /></p>

<hr />

<h2 id="6-지금-내-프로젝트의-전체-흐름">6. 지금 내 프로젝트의 전체 흐름</h2>

<h3 id="현재-구조-ec2--자동-배포">현재 구조 (EC2 + 자동 배포)</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>main 브랜치에 push
        ↓
[CI]
  Redis 컨테이너 띄우기
  → 빌드 + 전체 테스트 자동 실행
  → 실패 시 여기서 중단 ❌

        ↓ CI 성공

[CD]
  Docker 이미지 빌드
  → ECR에 푸시
  → EC2에 SSH로 자동 접속
  → 새 이미지로 컨테이너 재시작

        ↓
사용자가 새 버전 사용 🎉
</code></pre></div></div>

<p>예전에는 이 과정을 손으로 직접 했는데, 이제는 push 한 번이면 끝이다.</p>

<h3 id="play-store-출시-이후엔-ecs로-전환-예정">Play Store 출시 이후엔 ECS로 전환 예정</h3>

<p>지금은 소규모라 EC2 직접 배포로도 충분하다. 하지만 출시 후 트래픽이 늘어나면 아래 두 가지가 필요해질 것이다.</p>

<ul>
  <li><strong>무중단 배포</strong>: 배포할 때 앱이 잠깐이라도 끊기면 사용자 경험이 나빠짐</li>
  <li><strong>오토스케일링</strong>: 갑자기 사용자가 몰려도 자동으로 서버를 늘려줌</li>
</ul>

<p>그때 ECS로 전환할 예정이며, CD 파이프라인에서 ECS 배포 스텝만 교체하면 되기 때문에 마이그레이션 비용도 크지 않다.</p>

<hr />

<h2 id="7-정리">7. 정리</h2>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>EC2 직접 배포 (현재)</th>
      <th>ECS (출시 후)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>배포 방식</strong></td>
      <td>컨테이너 재시작</td>
      <td>롤링 업데이트</td>
    </tr>
    <tr>
      <td><strong>서비스 중단</strong></td>
      <td>순단 가능</td>
      <td>없음</td>
    </tr>
    <tr>
      <td><strong>스케일링</strong></td>
      <td>수동</td>
      <td>자동</td>
    </tr>
    <tr>
      <td><strong>비용</strong></td>
      <td>저렴</td>
      <td>상대적으로 높음</td>
    </tr>
    <tr>
      <td><strong>적합한 시점</strong></td>
      <td>초기 소규모</td>
      <td>트래픽 증가 후</td>
    </tr>
  </tbody>
</table>

<p><strong>한 줄 요약:</strong></p>

<blockquote>
  <p>push 한 번으로 테스트부터 배포까지. 지금은 EC2로 가볍게, 트래픽 늘면 ECS로 전환.</p>
</blockquote>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="projectdiary" /><summary type="html"><![CDATA[친구하자 프로젝트 출시를 준비하며 CI/CD를 도입하고, Docker, EC2, ECS, ECR 등 AWS 인프라를 활용한 배포 흐름을 정리했다.]]></summary></entry><entry><title type="html">[Algorithm] Filter vs Interceptor vs AOP - 요청이 어떻게 처리되는가</title><link href="https://nan0silver.github.io/spring/2026-02-23-filter-interceptor-aop/" rel="alternate" type="text/html" title="[Algorithm] Filter vs Interceptor vs AOP - 요청이 어떻게 처리되는가" /><published>2026-02-23T00:00:00+00:00</published><updated>2026-03-09T02:06:30+00:00</updated><id>https://nan0silver.github.io/spring/filter-interceptor-aop</id><content type="html" xml:base="https://nan0silver.github.io/spring/2026-02-23-filter-interceptor-aop/"><![CDATA[<blockquote>
  <p>PATCH/PUT, DTO, Controller 같은 Spring 글을 읽었다면, “요청이 어디서 걸리고, 어떤 순서로 처리될까?”가 궁금할 수 있다.<br />
이 글에서는 <strong>Filter</strong>, <strong>Interceptor</strong>, <strong>AOP</strong>가 각각 어디에 위치하고, 무엇을 할 때 쓰면 좋은지** 한 번에 정리한다.</p>
</blockquote>

<ul>
  <li><a href="#-1-한눈에-보는-요청-처리-흐름">1. 한눈에 보는 요청 처리 흐름</a></li>
  <li><a href="#-15-비유로-이해하기">1.5 비유로 이해하기</a></li>
  <li><a href="#-2-filter">2. Filter</a></li>
  <li><a href="#-3-interceptor">3. Interceptor</a></li>
  <li><a href="#-4-aop">4. AOP</a></li>
  <li><a href="#-5-비교표">5. 비교표</a></li>
  <li><a href="#-6-언제-무엇을-쓸까">6. 언제 무엇을 쓸까?</a></li>
  <li><a href="#-7-요약">7. 요약</a></li>
</ul>

<hr />

<h2 id="-1-한눈에-보는-요청-처리-흐름">🎯 1. 한눈에 보는 요청 처리 흐름</h2>

<p>HTTP 요청이 들어오면 대략 아래 순서로 지나간다.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[클라이언트]
     │
     ▼
┌─────────────────────────────────────────────────────────────┐
│  Filter 1 → Filter 2 → ... (Servlet 컨테이너 레벨)           │
└─────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────┐
│  DispatcherServlet (Spring 진입점)                           │
└─────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────┐
│  Interceptor (preHandle)                                     │
└─────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────┐
│  @Controller / @RestController (핸들러 메서드)               │
│  └─ 이 안에서 @Transactional, AOP 등 적용 가능              │
└─────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────┐
│  Interceptor (postHandle → afterCompletion)                  │
└─────────────────────────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────────────────────────┐
│  Filter (응답 처리)                                          │
└─────────────────────────────────────────────────────────────┘
     │
     ▼
[클라이언트]
</code></pre></div></div>

<ul>
  <li><strong>Filter</strong>: Servlet API 일부. <strong>DispatcherServlet 이전/이후</strong>에서 동작. 모든 요청(정적 자원 포함)에 적용될 수 있음.</li>
  <li><strong>Interceptor</strong>: Spring이 제공. <strong>DispatcherServlet 안</strong>에서, <strong>Controller 호출 전/후</strong>에 동작. Spring이 매핑한 요청만 처리.</li>
  <li><strong>AOP</strong>: Spring이 제공. <strong>특정 메서드(예: Service, Controller) 호출 전/후/예외</strong>에 동작. 비즈니스 로직·트랜잭션·로깅 등에 많이 사용.</li>
</ul>

<hr />

<h3 id="-15-비유로-이해하기">🏢 1.5 비유로 이해하기</h3>

<p><strong>“빌딩에 방문객(HTTP 요청)이 들어와서 업무를 보는 과정”</strong>으로 생각해 보자.</p>

<p><img src="https://github.com/nan0silver/nan0silver.github.io/blob/main/assets/img/blog/2026-02-23-pic1.png?raw=true" alt="예시 이미지1" /></p>

<table>
  <thead>
    <tr>
      <th>구성 요소</th>
      <th>비유</th>
      <th>하는 일</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Filter</strong></td>
      <td><strong>빌딩 1층 경비원</strong></td>
      <td>모든 방문객이 <strong>반드시</strong> 거치는 곳. 출입증 확인, 짐 검사(인코딩·CORS 등), 위험물 차단. Spring 빌딩 <strong>밖</strong>에서 막춰서, “어느 부서로 갈지”는 모른다.</td>
    </tr>
    <tr>
      <td><strong>DispatcherServlet</strong></td>
      <td><strong>안내 데스크(리셉션)</strong></td>
      <td>“어디로 가시나요?”라고 물어보고, <strong>URL(요청 경로)</strong>을 보고 “이 방문객은 3층 영업팀(Controller A)으로 보내야 해”라고 <strong>배정</strong>한다. 실제 업무는 안 하고, <strong>누가 처리할지만 결정</strong>한다.</td>
    </tr>
    <tr>
      <td><strong>Interceptor</strong></td>
      <td><strong>해당 층/회의실 앞 도어맨</strong></td>
      <td>“3층 영업팀이요”라고 온 사람만 검사. <strong>이 부서 들어가기 전</strong>에 “예약 있으세요? 권한 있으세요?”(인증·권한) 확인. 들어간 <strong>후</strong>에 출입 기록 남기기(로깅)도 여기서 할 수 있다.</td>
    </tr>
    <tr>
      <td><strong>Controller</strong></td>
      <td><strong>실제 업무를 보는 부서</strong></td>
      <td>“회원 정보 주세요”, “주문 등록해 주세요” 같은 <strong>요청에 맞는 일</strong>을 한다. 여기서 비즈니스 로직이 실행된다.</td>
    </tr>
    <tr>
      <td><strong>AOP</strong></td>
      <td><strong>부서 안의 공통 규칙</strong></td>
      <td>“누군가 이 메서드(업무)를 할 때마다 <strong>항상</strong> 실행되는 규칙”. 예: “출금할 때마다 로그 남기기”, “이 메서드 실행 시 트랜잭션 시작/종료”. HTTP 요청과 무관하게 <strong>메서드 호출</strong> 단위로 붙는다.</td>
    </tr>
  </tbody>
</table>

<p><strong>한 줄 요약</strong></p>

<ul>
  <li><strong>Filter</strong> → 빌딩 <strong>입구</strong>에서 모든 사람에게 적용되는 검사.</li>
  <li><strong>DispatcherServlet</strong> → <strong>“이 요청은 저 컨트롤러로”</strong> 배정만 하는 안내 데스크.</li>
  <li><strong>Interceptor</strong> → <strong>그 컨트롤러로 가기 직전/직후</strong>에만 거치는 층별 검사.</li>
  <li><strong>Controller</strong> → 실제 <strong>일을 처리하는 부서</strong>.</li>
  <li><strong>AOP</strong> → <strong>그 부서 안에서</strong> “이런 일 할 때마다 공통으로 치는 규칙”.</li>
</ul>

<hr />

<h2 id="-2-filter">🔷 2. Filter</h2>

<h3 id="무엇인가">무엇인가?</h3>

<ul>
  <li><strong>Servlet 스펙</strong>에 정의된 컴포넌트.</li>
  <li><code class="language-plaintext highlighter-rouge">javax.servlet.Filter</code> 인터페이스를 구현한다.</li>
  <li><strong>요청/응답</strong>을 감싸서 전후 처리(인코딩, 로깅, 인증 등)를 할 수 있다.</li>
</ul>

<h3 id="어디서-동작하는가">어디서 동작하는가?</h3>

<ul>
  <li><strong>DispatcherServlet 앞뒤</strong>. 즉 Spring 진입 <strong>전</strong>에 한 번, Spring 처리 <strong>후</strong> 응답 나가기 <strong>전</strong>에 한 번씩 거친다.</li>
  <li>URI로 “어떤 요청에만 적용할지” 지정할 수 있지만, 기본적으로 <strong>모든 요청</strong>이 Filter 체인을 통과한다 (설정에 따라 정적 자원 제외 가능).</li>
</ul>

<h3 id="특징">특징</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>소속</strong></td>
      <td>Servlet API (Spring이 아님)</td>
    </tr>
    <tr>
      <td><strong>요청/응답</strong></td>
      <td><code class="language-plaintext highlighter-rouge">ServletRequest</code>, <code class="language-plaintext highlighter-rouge">ServletResponse</code> (Servlet API 타입)</td>
    </tr>
    <tr>
      <td><strong>Spring Bean</strong></td>
      <td>등록 가능하지만, 동작 자체는 Servlet 컨테이너가 관리</td>
    </tr>
    <tr>
      <td><strong>Controller 정보</strong></td>
      <td>모름 (아직 DispatcherServlet을 거치지 않았거나, 이미 지나간 뒤)</td>
    </tr>
  </tbody>
</table>

<h3 id="예시-인코딩-필터">예시: 인코딩 필터</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="nd">@Order</span><span class="o">(</span><span class="nc">Ordered</span><span class="o">.</span><span class="na">HIGHEST_PRECEDENCE</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">EncodingFilter</span> <span class="kd">implements</span> <span class="nc">Filter</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">doFilter</span><span class="o">(</span><span class="nc">ServletRequest</span> <span class="n">request</span><span class="o">,</span> <span class="nc">ServletResponse</span> <span class="n">response</span><span class="o">,</span>
                         <span class="nc">FilterChain</span> <span class="n">chain</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">IOException</span><span class="o">,</span> <span class="nc">ServletException</span> <span class="o">{</span>
        <span class="n">request</span><span class="o">.</span><span class="na">setCharacterEncoding</span><span class="o">(</span><span class="s">"UTF-8"</span><span class="o">);</span>
        <span class="n">response</span><span class="o">.</span><span class="na">setCharacterEncoding</span><span class="o">(</span><span class="s">"UTF-8"</span><span class="o">);</span>
        <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>  <span class="c1">// 다음 Filter 또는 Servlet으로 전달</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">chain.doFilter()</code> 를 호출하기 <strong>전</strong>: 요청 전처리 (인코딩 설정 등).</li>
  <li><code class="language-plaintext highlighter-rouge">chain.doFilter()</code> 를 호출한 <strong>후</strong>: 응답 후처리 (필요 시).</li>
  <li><code class="language-plaintext highlighter-rouge">doFilter()</code> 를 호출하지 않으면 요청이 Controller까지 가지 않는다 (요청 차단 가능).</li>
</ul>

<h3 id="쓰기-좋은-경우">쓰기 좋은 경우</h3>

<ul>
  <li>전역 <strong>인코딩 설정</strong></li>
  <li><strong>CORS</strong> 처리 (응답 헤더 추가)</li>
  <li><strong>XSS</strong> 방지용 요청/응답 래핑</li>
  <li><strong>로깅</strong> (요청 URI, 메서드, IP 등) — 단, Interceptor나 AOP로도 가능</li>
</ul>

<hr />

<h2 id="-3-interceptor">🔷 3. Interceptor</h2>

<h3 id="무엇인가-1">무엇인가?</h3>

<ul>
  <li><strong>Spring</strong>이 제공하는 <strong>HandlerInterceptor</strong>.</li>
  <li><code class="language-plaintext highlighter-rouge">DispatcherServlet</code>이 요청을 받은 <strong>이후</strong>, <strong>Controller(핸들러)를 호출하기 전/후/완료 후</strong>에 끼어든다.</li>
</ul>

<h3 id="어디서-동작하는가-1">어디서 동작하는가?</h3>

<ul>
  <li><strong>DispatcherServlet 내부</strong>.</li>
  <li><strong>HandlerMapping</strong>으로 “이 URL은 이 Controller 메서드로” 결정된 <strong>이후</strong>에만 동작한다.</li>
  <li>따라서 <strong>Spring이 매핑한 요청</strong>에만 적용할 수 있고, 어떤 <strong>Handler(Controller 메서드)</strong> 에 붙었는지 알 수 있다.</li>
</ul>

<h3 id="세-시점">세 시점</h3>

<table>
  <thead>
    <tr>
      <th>메서드</th>
      <th>시점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>preHandle</strong></td>
      <td>Controller 실행 <strong>전</strong></td>
    </tr>
    <tr>
      <td><strong>postHandle</strong></td>
      <td>Controller 실행 <strong>후</strong>, View 렌더링 전 (RestController면 보통 응답 이미 생성됨)</td>
    </tr>
    <tr>
      <td><strong>afterCompletion</strong></td>
      <td>View 렌더링 <strong>후</strong> 또는 예외 발생 <strong>후</strong> (정리·로깅에 유용)</td>
    </tr>
  </tbody>
</table>

<h3 id="특징-1">특징</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>소속</strong></td>
      <td>Spring Framework</td>
    </tr>
    <tr>
      <td><strong>요청/응답</strong></td>
      <td><code class="language-plaintext highlighter-rouge">HttpServletRequest</code>, <code class="language-plaintext highlighter-rouge">HttpServletResponse</code> + <strong>Handler</strong> 정보</td>
    </tr>
    <tr>
      <td><strong>Spring Bean</strong></td>
      <td>완전한 Spring Bean (DI 가능)</td>
    </tr>
    <tr>
      <td><strong>Controller 정보</strong></td>
      <td><code class="language-plaintext highlighter-rouge">Handler</code>로 “어떤 컨트롤러 메서드가 실행되는지” 알 수 있음</td>
    </tr>
  </tbody>
</table>

<h3 id="예시-인증-체크-인터셉터">예시: 인증 체크 인터셉터</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">AuthInterceptor</span> <span class="kd">implements</span> <span class="nc">HandlerInterceptor</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="kt">boolean</span> <span class="nf">preHandle</span><span class="o">(</span><span class="nc">HttpServletRequest</span> <span class="n">request</span><span class="o">,</span> <span class="nc">HttpServletResponse</span> <span class="n">response</span><span class="o">,</span>
                             <span class="nc">Object</span> <span class="n">handler</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">handler</span> <span class="k">instanceof</span> <span class="nc">HandlerMethod</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">HandlerMethod</span> <span class="n">hm</span> <span class="o">=</span> <span class="o">(</span><span class="nc">HandlerMethod</span><span class="o">)</span> <span class="n">handler</span><span class="o">;</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">hm</span><span class="o">.</span><span class="na">hasMethodAnnotation</span><span class="o">(</span><span class="nc">LoginRequired</span><span class="o">.</span><span class="na">class</span><span class="o">))</span> <span class="o">{</span>
                <span class="c1">// 로그인 여부 확인 후 미로그인 시 false 반환 → Controller 미실행</span>
                <span class="k">if</span> <span class="o">(!</span><span class="n">isLoggedIn</span><span class="o">(</span><span class="n">request</span><span class="o">))</span> <span class="o">{</span>
                    <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_UNAUTHORIZED</span><span class="o">);</span>
                    <span class="k">return</span> <span class="kc">false</span><span class="o">;</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="k">return</span> <span class="kc">true</span><span class="o">;</span>  <span class="c1">// true면 다음 Interceptor / Controller 실행</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><strong>등록</strong>은 <code class="language-plaintext highlighter-rouge">WebMvcConfigurer.addInterceptors()</code> 에서 <code class="language-plaintext highlighter-rouge">addPathPatterns</code>, <code class="language-plaintext highlighter-rouge">excludePathPatterns</code> 로 URI 패턴 지정.</li>
</ul>

<h3 id="쓰기-좋은-경우-1">쓰기 좋은 경우</h3>

<ul>
  <li><strong>인증/권한</strong> 체크 (특정 URL만)</li>
  <li><strong>로그인 사용자 정보</strong>를 request에 넣기</li>
  <li><strong>접근 로그</strong> (어떤 URL, 어떤 메서드가 호출됐는지)</li>
  <li>Controller <strong>실행 전/후</strong>에만 하고 싶은 공통 처리</li>
</ul>

<hr />

<h2 id="-4-aop">🔷 4. AOP</h2>

<h3 id="무엇인가-2">무엇인가?</h3>

<ul>
  <li><strong>Aspect-Oriented Programming</strong>. “여러 메서드에 공통으로 들어가는 부가 기능”을 한곳에서 정의하는 방식.</li>
  <li>Spring AOP는 <strong>프록시 기반</strong>이라, <strong>Spring이 관리하는 Bean의 메서드 호출</strong>에만 적용된다.</li>
</ul>

<h3 id="어디서-동작하는가-2">어디서 동작하는가?</h3>

<ul>
  <li><strong>Controller, Service, Repository</strong> 등 <strong>Bean의 메서드가 호출될 때</strong>.</li>
  <li>Filter/Interceptor와 달리 “HTTP 요청”에 묶이지 않고, <strong>메서드 호출</strong> 단위로 동작한다.</li>
  <li>같은 메서드라도 <strong>Spring 컨텍스트 밖에서 호출</strong>되면 AOP가 적용되지 않는다 (예: 같은 클래스 내부에서 <code class="language-plaintext highlighter-rouge">this.method()</code> 호출).</li>
</ul>

<h3 id="특징-2">특징</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>소속</strong></td>
      <td>Spring Framework (AOP 모듈)</td>
    </tr>
    <tr>
      <td><strong>단위</strong></td>
      <td><strong>메서드</strong> (클래스·메서드 단위로 적용 대상 지정)</td>
    </tr>
    <tr>
      <td><strong>요청/응답</strong></td>
      <td>직접 접근하지 않음. 메서드 <strong>인자, 반환값, 예외</strong>에 접근 가능</td>
    </tr>
    <tr>
      <td><strong>Spring Bean</strong></td>
      <td>완전한 Spring Bean (DI 가능)</td>
    </tr>
  </tbody>
</table>

<h3 id="예시-메서드-실행-시간-로깅">예시: 메서드 실행 시간 로깅</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Aspect</span>
<span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">LoggingAspect</span> <span class="o">{</span>

    <span class="nd">@Around</span><span class="o">(</span><span class="s">"@annotation(org.springframework.web.bind.annotation.GetMapping)"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">Object</span> <span class="nf">logExecutionTime</span><span class="o">(</span><span class="nc">ProceedingJoinPoint</span> <span class="n">joinPoint</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Throwable</span> <span class="o">{</span>
        <span class="kt">long</span> <span class="n">start</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">currentTimeMillis</span><span class="o">();</span>
        <span class="nc">Object</span> <span class="n">result</span> <span class="o">=</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">proceed</span><span class="o">();</span>  <span class="c1">// 실제 메서드 실행</span>
        <span class="kt">long</span> <span class="n">elapsed</span> <span class="o">=</span> <span class="nc">System</span><span class="o">.</span><span class="na">currentTimeMillis</span><span class="o">()</span> <span class="o">-</span> <span class="n">start</span><span class="o">;</span>
        <span class="n">log</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"{} executed in {} ms"</span><span class="o">,</span> <span class="n">joinPoint</span><span class="o">.</span><span class="na">getSignature</span><span class="o">(),</span> <span class="n">elapsed</span><span class="o">);</span>
        <span class="k">return</span> <span class="n">result</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">@Around</code>: 메서드 <strong>앞뒤</strong> 모두 제어.</li>
  <li><code class="language-plaintext highlighter-rouge">@Before</code>, <code class="language-plaintext highlighter-rouge">@After</code>, <code class="language-plaintext highlighter-rouge">@AfterReturning</code>, <code class="language-plaintext highlighter-rouge">@AfterThrowing</code> 등으로 “전/후/반환 후/예외 시”만 넣을 수도 있다.</li>
</ul>

<h3 id="쓰기-좋은-경우-2">쓰기 좋은 경우</h3>

<ul>
  <li><strong>로깅</strong> (메서드명, 인자, 실행 시간, 예외)</li>
  <li><strong>트랜잭션</strong> (<code class="language-plaintext highlighter-rouge">@Transactional</code> 도 AOP로 동작)</li>
  <li><strong>권한 체크</strong> (메서드 단위, 예: <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> 와 유사한 커스텀)</li>
  <li><strong>예외 변환</strong> (특정 예외를 공통 API 응답으로 감싸기)</li>
  <li><strong>캐싱</strong> (메서드 결과 캐시)</li>
</ul>

<hr />

<h2 id="-5-비교표">📊 5. 비교표</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>Filter</th>
      <th>Interceptor</th>
      <th>AOP</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>위치</strong></td>
      <td>DispatcherServlet <strong>밖</strong> (앞·뒤)</td>
      <td>DispatcherServlet <strong>안</strong>, Controller <strong>앞·뒤</strong></td>
      <td><strong>메서드</strong> 호출 전/후/예외</td>
    </tr>
    <tr>
      <td><strong>적용 단위</strong></td>
      <td>URI (전체 요청)</td>
      <td>URI + Handler</td>
      <td>메서드(클래스·어노테이션 등)</td>
    </tr>
    <tr>
      <td><strong>스펙</strong></td>
      <td>Servlet API</td>
      <td>Spring</td>
      <td>Spring AOP</td>
    </tr>
    <tr>
      <td><strong>요청/응답 타입</strong></td>
      <td>ServletRequest/Response</td>
      <td>HttpServletRequest/Response</td>
      <td>없음 (메서드 인자·반환값)</td>
    </tr>
    <tr>
      <td><strong>Handler/Controller 정보</strong></td>
      <td>없음</td>
      <td>있음 (preHandle 등에서)</td>
      <td>타겟 메서드 정보만</td>
    </tr>
    <tr>
      <td><strong>Spring Bean</strong></td>
      <td>등록 가능, 동작은 서블릿 컨테이너</td>
      <td>O</td>
      <td>O</td>
    </tr>
    <tr>
      <td><strong>용도 예</strong></td>
      <td>인코딩, CORS, XSS, 전역 로깅</td>
      <td>인증·인가, URL 단위 로깅</td>
      <td>트랜잭션, 로깅, 예외 처리, 캐시</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-6-언제-무엇을-쓸까">🧩 6. 언제 무엇을 쓸까?</h2>

<ul>
  <li>
    <p><strong>요청/응답 자체를 바꾸거나, Spring 진입 전에 막고 싶을 때</strong><br />
→ <strong>Filter</strong> (인코딩, CORS, 전역 보안 래퍼 등)</p>
  </li>
  <li>
    <p><strong>“이 URL은 로그인 필요해”처럼 URL·Handler 기준으로 막거나 가공할 때</strong><br />
→ <strong>Interceptor</strong> (인증, 권한, URL 단위 로깅)</p>
  </li>
  <li>
    <p><strong>“이 메서드 실행 전/후/예외 시 항상 이 로직”을 넣고 싶을 때</strong><br />
→ <strong>AOP</strong> (로깅, 트랜잭션, 예외 변환, 캐시 등)</p>
  </li>
  <li>
    <p><strong>이미 Spring 글들(PATCH/PUT, DTO 등)을 읽었다면</strong><br />
→ 요청은 <strong>Filter → DispatcherServlet → Interceptor → Controller</strong> 순으로 들어가고, <strong>Controller/Service 메서드 안</strong>에서 <strong>AOP</strong>가 도는 구간이라고 보면 된다.</p>
  </li>
</ul>

<hr />

<h2 id="-7-요약">✅ 7. 요약</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>처리 순서</strong></td>
      <td>Filter → DispatcherServlet → Interceptor(pre) → Controller → Interceptor(post, after) → Filter(응답)</td>
    </tr>
    <tr>
      <td><strong>Filter</strong></td>
      <td>Servlet 레벨, DispatcherServlet 앞뒤, 전역 인코딩·CORS·로깅 등</td>
    </tr>
    <tr>
      <td><strong>Interceptor</strong></td>
      <td>Spring 레벨, Controller 전/후, URL·Handler 기준 인증·권한·로깅</td>
    </tr>
    <tr>
      <td><strong>AOP</strong></td>
      <td>메서드 단위, 트랜잭션·로깅·예외 처리·캐시 등</td>
    </tr>
    <tr>
      <td><strong>선택</strong></td>
      <td>요청/응답·진입 전 차단 → Filter, URL/Handler 기준 → Interceptor, 메서드 공통 로직 → AOP</td>
    </tr>
  </tbody>
</table>

<p>이렇게 세 가지를 구분해 두면, “요청이 어떻게 처리되는지”를 Spring 글들(PATCH/PUT, DTO, Controller)과 함께 한 번에 그릴 수 있다.</p>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="spring" /><summary type="html"><![CDATA[Spring에서 HTTP 요청이 Controller에 도달하기까지 거치는 Filter, Interceptor, AOP의 역할과 차이, 그리고 요청 처리 순서를 정리했습니다.]]></summary></entry><entry><title type="html">[Algorithm] 다익스트라 알고리즘</title><link href="https://nan0silver.github.io/algorithm/2026-02-21-dijkstra/" rel="alternate" type="text/html" title="[Algorithm] 다익스트라 알고리즘" /><published>2026-02-21T00:00:00+00:00</published><updated>2026-03-09T02:06:30+00:00</updated><id>https://nan0silver.github.io/algorithm/dijkstra</id><content type="html" xml:base="https://nan0silver.github.io/algorithm/2026-02-21-dijkstra/"><![CDATA[<blockquote>
  <p><strong>최단 경로 시리즈</strong> – <a href="/algorithm/2025-12-29-floyd-warshall/">플로이드-워셜</a>(모든 쌍), <a href="/algorithm/2026-02-09-bellman-ford/">벨만-포드</a>(음수 간선·음수 사이클)에 이어, <strong>한 출발점에서 나머지 모든 정점까지</strong>의 최단 경로를 빠르게 구하는 알고리즘이다.</p>
</blockquote>

<ol>
  <li><a href="#-1️⃣-다익스트라란">다익스트라란?</a></li>
  <li><a href="#-2️⃣-핵심-개념-이해하기">핵심 개념 이해하기</a></li>
  <li><a href="#-3️⃣-간선-완화relaxation">간선 완화(Relaxation)</a></li>
  <li><a href="#-4️⃣-우선순위-큐와-동작-과정">우선순위 큐와 동작 과정</a></li>
  <li><a href="#-5️⃣-코드-구현">코드 구현</a></li>
  <li><a href="#-6️⃣-시간복잡도와-공간복잡도">시간복잡도와 공간복잡도</a></li>
  <li><a href="#-7️⃣-벨만-포드플로이드-워셜과의-비교">벨만-포드·플로이드-워셜과의 비교</a></li>
  <li><a href="#-8️⃣-실전-문제-적용">실전 문제 적용</a></li>
</ol>

<hr />

<h2 id="-1️⃣-다익스트라란">🎯 1️⃣ 다익스트라란?</h2>

<h3 id="-한-문장-정의">💡 한 문장 정의</h3>

<p><strong>“음수 간선이 없을 때, 한 출발점에서 모든 정점까지의 최단 경로를 구하는 그리디 알고리즘”</strong></p>

<h3 id="-언제-사용하는가">💡 언제 사용하는가?</h3>

<ul>
  <li><strong>한 정점(출발점)</strong>에서 <strong>나머지 모든 정점</strong>까지의 최단 거리가 필요할 때</li>
  <li>간선 가중치가 <strong>모두 0 이상(비음수)</strong>일 때</li>
  <li>정점·간선이 많아서 플로이드-워셜 O(V³)을 쓰기 어려울 때</li>
</ul>

<h3 id="-특징">💡 특징</h3>

<table>
  <thead>
    <tr>
      <th>특징</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>시간복잡도</strong></td>
      <td>O((V+E)log V) (우선순위 큐 사용 시)</td>
    </tr>
    <tr>
      <td><strong>공간복잡도</strong></td>
      <td>O(V)</td>
    </tr>
    <tr>
      <td><strong>음수 간선</strong></td>
      <td><strong>불가능</strong> (잘못된 결과 가능)</td>
    </tr>
    <tr>
      <td><strong>그래프 종류</strong></td>
      <td>방향/무방향 모두 가능</td>
    </tr>
    <tr>
      <td><strong>구현 난이도</strong></td>
      <td>중간 (우선순위 큐 활용)</td>
    </tr>
    <tr>
      <td><strong>성격</strong></td>
      <td>그리디 (매 순간 가장 가까운 정점 확정)</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="-2️⃣-핵심-개념-이해하기">🧠 2️⃣ 핵심 개념 이해하기</h2>

<h3 id="-알고리즘의-본질">💡 알고리즘의 본질</h3>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"매 단계마다 '가장 거리가 짧은 정점'을 하나씩 확정하고,
그 정점을 통해 갈 수 있는 이웃들의 거리를 갱신해 나가는 알고리즘"
</code></pre></div></div>

<h3 id="-distv의-의미">💡 dist[v]의 의미</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">dist</span><span class="o">[</span><span class="n">v</span><span class="o">]</span> <span class="o">=</span> <span class="n">출발점</span><span class="o">(</span><span class="n">start</span><span class="o">)</span><span class="n">으로부터</span>
          <span class="n">정점</span> <span class="n">v까지</span> <span class="n">도달하는</span> <span class="n">현재까지의</span> <span class="n">최소</span> <span class="n">비용</span>
</code></pre></div></div>

<ul>
  <li>한 번 <strong>확정(방문 처리)</strong>한 정점의 dist 값은 더 이상 바뀌지 않는다.</li>
  <li>음수 간선이 없기 때문에, “지금 가장 작은 dist를 가진 정점”은 이미 최단 거리가 보장된다.</li>
</ul>

<h3 id="-그리디한-선택">💡 그리디한 선택</h3>

<ul>
  <li><strong>“현재 시점에서 dist가 가장 작은 정점”</strong>을 골라서 확정한다.</li>
  <li>그 정점을 거쳐 가는 경로가 더 짧아질 수 있는 <strong>이웃만</strong> 완화(relax)한다.</li>
  <li>이렇게 하면 각 정점을 <strong>최대 한 번씩만</strong> 확정 처리하면 되고, 우선순위 큐로 “가장 작은 dist”를 빠르게 꺼낼 수 있다.</li>
</ul>

<hr />

<h2 id="-3️⃣-간선-완화relaxation">🔁 3️⃣ 간선 완화(Relaxation)</h2>

<h3 id="-간선-완화란">💡 간선 완화란?</h3>

<p><strong>“간선 u → v를 이용했을 때, 출발점에서 v까지 가는 거리가 더 짧아지면 dist[v]를 갱신하는 과정”</strong></p>

<p>(벨만-포드, 플로이드-워셜에서 쓰는 것과 같은 개념이다.)</p>

<h3 id="-완화-조건">💡 완화 조건</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="o">(</span><span class="n">dist</span><span class="o">[</span><span class="n">v</span><span class="o">]</span> <span class="o">&gt;</span> <span class="n">dist</span><span class="o">[</span><span class="n">u</span><span class="o">]</span> <span class="o">+</span> <span class="n">w</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">dist</span><span class="o">[</span><span class="n">v</span><span class="o">]</span> <span class="o">=</span> <span class="n">dist</span><span class="o">[</span><span class="n">u</span><span class="o">]</span> <span class="o">+</span> <span class="n">w</span><span class="o">;</span>
    <span class="c1">// 우선순위 큐 사용 시: (dist[v], v) 다시 넣거나 갱신</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">u</code>: 방금 확정한 정점</li>
  <li><code class="language-plaintext highlighter-rouge">v</code>: u의 이웃 정점</li>
  <li><code class="language-plaintext highlighter-rouge">w</code>: 간선 (u, v)의 가중치</li>
</ul>

<hr />

<h2 id="-4️⃣-우선순위-큐와-동작-과정">📌 4️⃣ 우선순위 큐와 동작 과정</h2>

<h3 id="-왜-우선순위-큐를-쓰는가">💡 왜 우선순위 큐를 쓰는가?</h3>

<ul>
  <li><strong>매번 “dist가 가장 작은 정점”</strong>을 골라야 한다.</li>
  <li>배열로 매번 최솟값을 찾으면 O(V) → 전체 O(V²).</li>
  <li><strong>우선순위 큐(최소 힙)</strong>를 쓰면 최솟값 추출 O(log V), 간선당 최대 1번 갱신 → <strong>O((V+E)log V)</strong>.</li>
</ul>

<h3 id="-동작-흐름-요약">💡 동작 흐름 (요약)</h3>

<ol>
  <li>출발점 <code class="language-plaintext highlighter-rouge">start</code>의 <code class="language-plaintext highlighter-rouge">dist[start] = 0</code>, 나머지는 <strong>무한대(INF)</strong>로 초기화.</li>
  <li>우선순위 큐에 <code class="language-plaintext highlighter-rouge">(0, start)</code> 넣기. (비용, 정점 번호)</li>
  <li>큐가 빌 때까지:
    <ul>
      <li><strong>비용이 가장 작은 (d, u)</strong> 꺼내기.</li>
      <li>이미 <code class="language-plaintext highlighter-rouge">d &gt; dist[u]</code> 이면 <strong>스킵</strong> (예전에 넣은 쓸모없는 정보).</li>
      <li>u를 <strong>확정</strong>했다고 보고, u와 연결된 각 간선 (u → v, 가중치 w)에 대해 <strong>완화</strong>.</li>
      <li>완화로 <code class="language-plaintext highlighter-rouge">dist[v]</code>가 줄어들면 <code class="language-plaintext highlighter-rouge">(dist[v], v)</code> 를 큐에 넣기.</li>
    </ul>
  </li>
  <li>종료 후 <code class="language-plaintext highlighter-rouge">dist[]</code>가 <strong>출발점에서 각 정점까지의 최단 거리</strong>.</li>
</ol>

<h3 id="-확정의-의미">💡 “확정”의 의미</h3>

<ul>
  <li>음수 간선이 없으므로, <strong>현재 dist가 가장 작은 정점</strong>은 더 이상 다른 경로로 더 짧아질 수 없다.</li>
  <li>그래서 그 정점은 <strong>한 번만 꺼내서 이웃만 완화</strong>하면 되고, 다시 큐에 넣을 필요 없다.</li>
  <li><code class="language-plaintext highlighter-rouge">d &gt; dist[u]</code> 인 경우는 “예전에 넣었던 더 나쁜 값”이므로 무시한다.</li>
</ul>

<hr />

<h2 id="-5️⃣-코드-구현">💻 5️⃣ 코드 구현</h2>

<h3 id="-인접-리스트--우선순위-큐-java">💡 인접 리스트 + 우선순위 큐 (Java)</h3>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.util.*</span><span class="o">;</span>

<span class="kd">class</span> <span class="nc">Dijkstra</span> <span class="o">{</span>
    <span class="kd">static</span> <span class="kd">final</span> <span class="kt">int</span> <span class="no">INF</span> <span class="o">=</span> <span class="mi">987654321</span><span class="o">;</span>
    <span class="nc">List</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">[]&gt;[]</span> <span class="n">graph</span><span class="o">;</span>  <span class="c1">// graph[v] = { {next, cost}, ... }</span>
    <span class="kt">int</span><span class="o">[]</span> <span class="n">dist</span><span class="o">;</span>
    <span class="kt">int</span> <span class="no">V</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">int</span><span class="o">[]</span> <span class="nf">dijkstra</span><span class="o">(</span><span class="kt">int</span> <span class="n">start</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">dist</span> <span class="o">=</span> <span class="k">new</span> <span class="kt">int</span><span class="o">[</span><span class="no">V</span> <span class="o">+</span> <span class="mi">1</span><span class="o">];</span>
        <span class="nc">Arrays</span><span class="o">.</span><span class="na">fill</span><span class="o">(</span><span class="n">dist</span><span class="o">,</span> <span class="no">INF</span><span class="o">);</span>
        <span class="n">dist</span><span class="o">[</span><span class="n">start</span><span class="o">]</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>

        <span class="c1">// (비용, 정점) - 비용 기준 최소 힙</span>
        <span class="nc">PriorityQueue</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">[]&gt;</span> <span class="n">pq</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">PriorityQueue</span><span class="o">&lt;&gt;((</span><span class="n">a</span><span class="o">,</span> <span class="n">b</span><span class="o">)</span> <span class="o">-&gt;</span> <span class="n">a</span><span class="o">[</span><span class="mi">0</span><span class="o">]</span> <span class="o">-</span> <span class="n">b</span><span class="o">[</span><span class="mi">0</span><span class="o">]);</span>
        <span class="n">pq</span><span class="o">.</span><span class="na">offer</span><span class="o">(</span><span class="k">new</span> <span class="kt">int</span><span class="o">[]{</span><span class="mi">0</span><span class="o">,</span> <span class="n">start</span><span class="o">});</span>

        <span class="k">while</span> <span class="o">(!</span><span class="n">pq</span><span class="o">.</span><span class="na">isEmpty</span><span class="o">())</span> <span class="o">{</span>
            <span class="kt">int</span><span class="o">[]</span> <span class="n">cur</span> <span class="o">=</span> <span class="n">pq</span><span class="o">.</span><span class="na">poll</span><span class="o">();</span>
            <span class="kt">int</span> <span class="n">d</span> <span class="o">=</span> <span class="n">cur</span><span class="o">[</span><span class="mi">0</span><span class="o">],</span> <span class="n">u</span> <span class="o">=</span> <span class="n">cur</span><span class="o">[</span><span class="mi">1</span><span class="o">];</span>

            <span class="k">if</span> <span class="o">(</span><span class="n">d</span> <span class="o">&gt;</span> <span class="n">dist</span><span class="o">[</span><span class="n">u</span><span class="o">])</span> <span class="k">continue</span><span class="o">;</span>  <span class="c1">// 이미 더 좋은 경로로 갱신된 경우 스킵</span>

            <span class="k">for</span> <span class="o">(</span><span class="kt">int</span><span class="o">[]</span> <span class="n">edge</span> <span class="o">:</span> <span class="n">graph</span><span class="o">[</span><span class="n">u</span><span class="o">])</span> <span class="o">{</span>
                <span class="kt">int</span> <span class="n">v</span> <span class="o">=</span> <span class="n">edge</span><span class="o">[</span><span class="mi">0</span><span class="o">],</span> <span class="n">w</span> <span class="o">=</span> <span class="n">edge</span><span class="o">[</span><span class="mi">1</span><span class="o">];</span>
                <span class="k">if</span> <span class="o">(</span><span class="n">dist</span><span class="o">[</span><span class="n">v</span><span class="o">]</span> <span class="o">&gt;</span> <span class="n">dist</span><span class="o">[</span><span class="n">u</span><span class="o">]</span> <span class="o">+</span> <span class="n">w</span><span class="o">)</span> <span class="o">{</span>
                    <span class="n">dist</span><span class="o">[</span><span class="n">v</span><span class="o">]</span> <span class="o">=</span> <span class="n">dist</span><span class="o">[</span><span class="n">u</span><span class="o">]</span> <span class="o">+</span> <span class="n">w</span><span class="o">;</span>
                    <span class="n">pq</span><span class="o">.</span><span class="na">offer</span><span class="o">(</span><span class="k">new</span> <span class="kt">int</span><span class="o">[]{</span><span class="n">dist</span><span class="o">[</span><span class="n">v</span><span class="o">],</span> <span class="n">v</span><span class="o">});</span>
                <span class="o">}</span>
            <span class="o">}</span>
        <span class="o">}</span>

        <span class="k">return</span> <span class="n">dist</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<h3 id="-인접-행렬-버전-정점-수가-작을-때">💡 인접 행렬 버전 (정점 수가 작을 때)</h3>

<ul>
  <li>정점이 적고 간선이 많을 때는 <strong>인접 행렬 + 매번 최솟값 탐색</strong>으로도 구현 가능.</li>
  <li>시간복잡도 <strong>O(V²)</strong>. 플로이드-워셜보다는 V가 클 때 유리할 수 있다.</li>
</ul>

<hr />

<h2 id="️-6️⃣-시간복잡도와-공간복잡도">⏱️ 6️⃣ 시간복잡도와 공간복잡도</h2>

<h3 id="-우선순위-큐-버전">💡 우선순위 큐 버전</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>복잡도</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>시간복잡도</strong></td>
      <td>O((V+E) log V)</td>
      <td>각 간선 최대 1번 완화, PQ 삽입/삭제 log V</td>
    </tr>
    <tr>
      <td><strong>공간복잡도</strong></td>
      <td>O(V)</td>
      <td>dist[], PQ에 정점 개수만큼</td>
    </tr>
  </tbody>
</table>

<h3 id="-인접-행렬--선형-탐색-버전">💡 인접 행렬 + 선형 탐색 버전</h3>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>복잡도</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>시간복잡도</strong></td>
      <td>O(V²)</td>
      <td>V번 반복, 매번 O(V) 최솟값</td>
    </tr>
    <tr>
      <td><strong>공간복잡도</strong></td>
      <td>O(V²)</td>
      <td>인접 행렬</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="️-7️⃣-벨만-포드플로이드-워셜과의-비교">⚖️ 7️⃣ 벨만-포드·플로이드-워셜과의 비교</h2>

<table>
  <thead>
    <tr>
      <th>구분</th>
      <th>다익스트라</th>
      <th>벨만-포드</th>
      <th>플로이드-워셜</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>목적</strong></td>
      <td>한 출발점 → 모든 정점</td>
      <td>한 출발점 → 모든 정점</td>
      <td>모든 쌍 최단 경로</td>
    </tr>
    <tr>
      <td><strong>음수 간선</strong></td>
      <td><strong>불가능</strong></td>
      <td>가능</td>
      <td>가능 (음수 사이클 제외)</td>
    </tr>
    <tr>
      <td><strong>음수 사이클</strong></td>
      <td>해당 없음</td>
      <td>탐지 가능</td>
      <td>있으면 사용 불가</td>
    </tr>
    <tr>
      <td><strong>시간복잡도</strong></td>
      <td>O((V+E)log V) 또는 O(V²)</td>
      <td>O(VE)</td>
      <td>O(V³)</td>
    </tr>
    <tr>
      <td><strong>공간복잡도</strong></td>
      <td>O(V)</td>
      <td>O(V)</td>
      <td>O(V²)</td>
    </tr>
    <tr>
      <td><strong>적용 상황</strong></td>
      <td>출발점 하나, 음수 간선 없음</td>
      <td>음수 간선/사이클</td>
      <td>정점 적을 때, 모든 쌍</td>
    </tr>
  </tbody>
</table>

<h3 id="-언제-무엇을-쓸까">💡 언제 무엇을 쓸까?</h3>

<p><img src="https://github.com/nan0silver/nan0silver.github.io/blob/main/assets/img/blog/2026-02-21-pic1.png?raw=true" alt="예시 이미지1" /></p>

<hr />

<h2 id="-8️⃣-실전-문제-적용">🧪 8️⃣ 실전 문제 적용</h2>

<h3 id="-대표-문제-유형">💡 대표 문제 유형</h3>

<table>
  <thead>
    <tr>
      <th>유형</th>
      <th>설명</th>
      <th>예시 키워드</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>단일 출발 최단 경로</strong></td>
      <td>한 도시에서 다른 모든 도시까지</td>
      <td>“1번 노드에서”, “한 점에서”</td>
    </tr>
    <tr>
      <td><strong>가중치가 양수</strong></td>
      <td>거리, 시간, 비용이 0 이상</td>
      <td>“최소 비용”, “최단 거리”</td>
    </tr>
    <tr>
      <td><strong>경로 복원</strong></td>
      <td>최단 경로에 포함된 정점/간선 출력</td>
      <td>“경로 출력”, “경로 추적”</td>
    </tr>
    <tr>
      <td><strong>2차원 격자</strong></td>
      <td>칸 이동 비용을 간선으로 두고 적용</td>
      <td>“다익스트라 + BFS/격자”</td>
    </tr>
  </tbody>
</table>

<h3 id="-체크리스트">💡 체크리스트</h3>

<p>다익스트라를 쓸 때 확인할 것:</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><strong>음수 간선이 없는가?</strong> (있으면 벨만-포드)</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><strong>한 출발점에서 나머지 전체</strong>가 필요한가? (모든 쌍이면 플로이드-워셜 고려)</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">INF</code> 값을 충분히 크게 설정했는가?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />같은 정점에 대한 <strong>중복 간선</strong>이 있으면 더 작은 가중치만 남겼는가?</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">d &gt; dist[u]</code> 스킵 처리로 <strong>이미 갱신된 노드</strong>를 건너뛰었는가?</li>
</ul>

<hr />

<p>이렇게 <strong>다익스트라</strong>까지 정리하면, <a href="/algorithm/2025-12-29-floyd-warshall/">플로이드-워셜</a>·<a href="/algorithm/2026-02-09-bellman-ford/">벨만-포드</a>와 함께 <strong>최단 경로 시리즈</strong>를 한 세트로 다룰 수 있다.</p>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="algorithm" /><summary type="html"><![CDATA[다익스트라 알고리즘에 대해 정리해보았습니다. 한 정점에서 모든 정점까지의 최단 경로를 구하는 대표 알고리즘입니다.]]></summary></entry><entry><title type="html">[Algorithm] Integer ArrayList을 int 배열로 변환하는 방법</title><link href="https://nan0silver.github.io/algorithm/2026-02-18-integer-arraylist-to-int-array/" rel="alternate" type="text/html" title="[Algorithm] Integer ArrayList을 int 배열로 변환하는 방법" /><published>2026-02-18T00:00:00+00:00</published><updated>2026-03-09T02:06:30+00:00</updated><id>https://nan0silver.github.io/algorithm/integer-arraylist-to-int-array</id><content type="html" xml:base="https://nan0silver.github.io/algorithm/2026-02-18-integer-arraylist-to-int-array/"><![CDATA[<p>알고리즘 문제를 풀다 보면 <code class="language-plaintext highlighter-rouge">List&lt;Integer&gt;</code>로 모아둔 결과를 최종적으로 <code class="language-plaintext highlighter-rouge">int[]</code> 배열로 반환해야 하는 경우가 많다. <code class="language-plaintext highlighter-rouge">String</code> 리스트는 <code class="language-plaintext highlighter-rouge">toArray()</code>로 쉽게 변환할 수 있지만, <strong>primitive 타입인 int는 <code class="language-plaintext highlighter-rouge">toArray()</code>를 그대로 쓸 수 없다.</strong> 이번 글에서는 Integer ArrayList를 int 배열로 바꾸는 방법을 정리해보자.</p>

<ol>
  <li><a href="#-1️⃣-string-list-vs-integer-list">String List vs Integer List</a></li>
  <li><a href="#-2️⃣-방법-1-반복문-사용">방법 1: 반복문 사용</a></li>
  <li><a href="#-3️⃣-방법-23-stream--maptoint">방법 2·3: Stream + mapToInt</a></li>
  <li><a href="#-4️⃣-방법-4-null-필터링이-필요할-때">방법 4: null 필터링이 필요할 때</a></li>
  <li><a href="#-5️⃣-정리">정리</a></li>
</ol>

<hr />

<h2 id="-1️⃣-string-list-vs-integer-list">📌 1️⃣ String List vs Integer List</h2>

<p><strong>String 타입의 List</strong>는 <code class="language-plaintext highlighter-rouge">toArray()</code>로 바로 배열로 만들 수 있다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"a"</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"b"</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"c"</span><span class="o">);</span>

<span class="c1">// toArray() - 배열 선언과 동시에 할당</span>
<span class="nc">String</span><span class="o">[]</span> <span class="n">arr</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">toArray</span><span class="o">(</span><span class="k">new</span> <span class="nc">String</span><span class="o">[</span><span class="mi">0</span><span class="o">]);</span>  <span class="c1">// ["a", "b", "c"]</span>

<span class="c1">// toArray() - 배열 선언 후 값 할당</span>
<span class="nc">String</span><span class="o">[]</span> <span class="n">arr2</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">String</span><span class="o">[</span><span class="n">list</span><span class="o">.</span><span class="na">size</span><span class="o">()];</span>
<span class="n">list</span><span class="o">.</span><span class="na">toArray</span><span class="o">(</span><span class="n">arr2</span><span class="o">);</span>  <span class="c1">// ["a", "b", "c"]</span>
</code></pre></div></div>

<p>반면 <strong>int는 primitive 타입</strong>이라 <code class="language-plaintext highlighter-rouge">List&lt;int&gt;</code> 같은 제네릭이 불가능하고, <code class="language-plaintext highlighter-rouge">List&lt;Integer&gt;</code>를 쓴다. 이때 <code class="language-plaintext highlighter-rouge">toArray()</code>만으로는 <code class="language-plaintext highlighter-rouge">int[]</code>를 얻을 수 없어서 아래와 같은 변환 방법을 사용해야 한다.</p>

<hr />

<h2 id="-2️⃣-방법-1-반복문-사용">🔄 2️⃣ 방법 1: 반복문 사용</h2>

<p>가장 직관적인 방법이다. 리스트 크기만큼 <code class="language-plaintext highlighter-rouge">int[]</code>를 만들고, 각 요소를 <code class="language-plaintext highlighter-rouge">intValue()</code>로 unboxing해서 넣는다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">1</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">2</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">3</span><span class="o">);</span>

<span class="kt">int</span><span class="o">[]</span> <span class="n">arr</span> <span class="o">=</span> <span class="k">new</span> <span class="kt">int</span><span class="o">[</span><span class="n">list</span><span class="o">.</span><span class="na">size</span><span class="o">()];</span>
<span class="k">for</span> <span class="o">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">list</span><span class="o">.</span><span class="na">size</span><span class="o">();</span> <span class="n">i</span><span class="o">++)</span> <span class="o">{</span>
    <span class="n">arr</span><span class="o">[</span><span class="n">i</span><span class="o">]</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="n">i</span><span class="o">).</span><span class="na">intValue</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<ul>
  <li><strong>장점</strong>: 코드가 단순하고 의도가 분명함.</li>
  <li><strong>단점</strong>: 한 줄로 쓰기 어렵고, null이 있으면 NPE 가능.</li>
</ul>

<hr />

<h2 id="-3️⃣-방법-23-stream--maptoint">🚀 3️⃣ 방법 2·3: Stream + mapToInt</h2>

<p>Stream으로 리스트를 <code class="language-plaintext highlighter-rouge">IntStream</code>으로 바꾼 뒤 <code class="language-plaintext highlighter-rouge">toArray()</code>로 <code class="language-plaintext highlighter-rouge">int[]</code>를 만드는 방법이다. 알고리즘/코테 풀이에서 한 줄로 쓰기 좋다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">1</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">2</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">3</span><span class="o">);</span>

<span class="c1">// 방법 2: 람다로 자동 unboxing</span>
<span class="kt">int</span><span class="o">[]</span> <span class="n">arr2</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">mapToInt</span><span class="o">(</span><span class="n">i</span> <span class="o">-&gt;</span> <span class="n">i</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toArray</span><span class="o">();</span>

<span class="c1">// 방법 3: 메서드 레퍼런스로 명시적 변환</span>
<span class="kt">int</span><span class="o">[]</span> <span class="n">arr3</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">mapToInt</span><span class="o">(</span><span class="nl">Integer:</span><span class="o">:</span><span class="n">intValue</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toArray</span><span class="o">();</span>
</code></pre></div></div>

<ul>
  <li><strong>방법 2</strong>: <code class="language-plaintext highlighter-rouge">i -&gt; i</code>에서 Java가 <code class="language-plaintext highlighter-rouge">Integer</code>를 <code class="language-plaintext highlighter-rouge">int</code>로 자동 unboxing 해준다.</li>
  <li><strong>방법 3</strong>: <code class="language-plaintext highlighter-rouge">Integer::intValue</code>로 직접 int로 변환한다.</li>
  <li><strong>공통</strong>: 코드가 짧고, 리스트에 null이 없을 때 사용하기 적합하다.</li>
</ul>

<hr />

<h2 id="️-4️⃣-방법-4-null-필터링이-필요할-때">⚠️ 4️⃣ 방법 4: null 필터링이 필요할 때</h2>

<p>리스트에 <strong>null이 들어갈 수 있는 경우</strong>에는 그대로 <code class="language-plaintext highlighter-rouge">mapToInt</code>를 쓰면 NPE가 난다. 이때는 <code class="language-plaintext highlighter-rouge">filter</code>로 null을 제거한 뒤 변환하면 된다.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Integer</span><span class="o">&gt;</span> <span class="n">list</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">1</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>
<span class="n">list</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="mi">3</span><span class="o">);</span>

<span class="c1">// null 제거 후 int[] 로 변환</span>
<span class="kt">int</span><span class="o">[]</span> <span class="n">arr4</span> <span class="o">=</span> <span class="n">list</span><span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">filter</span><span class="o">(</span><span class="n">i</span> <span class="o">-&gt;</span> <span class="n">i</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span>
        <span class="o">.</span><span class="na">mapToInt</span><span class="o">(</span><span class="n">i</span> <span class="o">-&gt;</span> <span class="n">i</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toArray</span><span class="o">();</span>
<span class="c1">// 결과: [1, 3]</span>
</code></pre></div></div>

<hr />

<h2 id="-5️⃣-정리">📋 5️⃣ 정리</h2>

<table>
  <thead>
    <tr>
      <th>상황</th>
      <th>추천 방법</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>단순 변환, null 없음</td>
      <td><code class="language-plaintext highlighter-rouge">list.stream().mapToInt(i -&gt; i).toArray()</code></td>
    </tr>
    <tr>
      <td>변환 로직을 명시하고 싶을 때</td>
      <td><code class="language-plaintext highlighter-rouge">list.stream().mapToInt(Integer::intValue).toArray()</code></td>
    </tr>
    <tr>
      <td>null이 포함될 수 있을 때</td>
      <td><code class="language-plaintext highlighter-rouge">filter(i -&gt; i != null)</code> 후 <code class="language-plaintext highlighter-rouge">mapToInt(i -&gt; i).toArray()</code></td>
    </tr>
    <tr>
      <td>Stream 없이 명시적으로 쓰고 싶을 때</td>
      <td>반복문 + <code class="language-plaintext highlighter-rouge">list.get(i).intValue()</code></td>
    </tr>
  </tbody>
</table>

<p>알고리즘 문제에서 <strong>반환 타입이 <code class="language-plaintext highlighter-rouge">int[]</code>인데 중간에 <code class="language-plaintext highlighter-rouge">List&lt;Integer&gt;</code>로 처리한 경우</strong> 위 방법 중 하나로 마지막에 배열로 바꿔주면 된다.</p>

<hr />

<p><strong>참고</strong></p>
<ul>
  <li><a href="https://hoehen-flug.tistory.com/49">내가 개발자라니 - Integer ArrayList을 int 배열로 변환하는 방법</a></li>
</ul>]]></content><author><name>Nahyun Eun 은나현</name></author><category term="algorithm" /><summary type="html"><![CDATA[알고리즘 문제 풀이 시 Integer ArrayList를 int[] 배열로 변환해야 할 때 사용할 수 있는 방법을 정리했습니다.]]></summary></entry></feed>